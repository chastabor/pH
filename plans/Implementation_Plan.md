# pH Implementation Plan

**Status:** v1.0 — 2026-08-26
**Companion to:** [Python_Harness_Port_Plan.md](Python_Harness_Port_Plan.md) (the specification — 21 decisions D1–D21, 13 closed questions Q1–Q13, §10 roadmap) · [Prime_Agent_Feature_Map.md](Prime_Agent_Feature_Map.md) (the governance analysis) · [DeepSeek_to_Prime_Intellect_Integration.md](DeepSeek_to_Prime_Intellect_Integration.md) (the originating thesis, annotated)

---

## 0. What this document is

The port plan is the *specification*: it says what pH is and why each decision fell where it did. This is the *work plan*: it says what to build, in what order, and what proves each piece is done.

It is organised in two directions on purpose:

- **§2 is feature-first.** Every safety and stability property pH promises is a row with an ID, the failure it prevents, the mechanism, the phase, and the test that gates it. This is the checklist a reviewer uses to ask "is the harness actually safe in the way the docs claim?"
- **§4 is phase-first.** Work items small enough to become tickets, each carrying the feature IDs it delivers and its gate. This is what an implementer uses.

The two are joined by ID: a §2 row names the §4 items that build it; a §4 item names the §2 rows it delivers. **A feature with no work item, or a work item with no gate, is a defect in this document.**

Where the port plan wins on *what*, this document wins on *when* and *how proven*. Where they disagree on *what*, the port plan wins.

---

## 1. The invariants everything else serves

Eight statements. Every §2 row protects one of them; every §4 gate tests one of them. Five come from dsh (port plan §2); three were added by this design.

| # | Invariant | Origin | Why it is load-bearing |
|---|---|---|---|
| **I1** | **Everything is a plugin; there is no privileged core to patch.** The loop, the adapter, the registry and the log are rows in a profile. | dsh | Swapping a provider changes the product without forking consumers. Every safety feature below is a row, which is why it can be audited, disabled, or replaced. |
| **I2** | **Registrations *and acquired resources* are effects that unwind.** Every `ctx.on`, `ctx.tools.register`, and every child process, worktree, temp path or lock returns a disposer torn down with its scope. | dsh, extended §4.9 | Cleanup is structural, not remembered. No plugin holds an artifact outside the seam. |
| **I3** | **Model-visible means logged.** Any content reaching a model request is reconstructable from `Session.events` via `derive_messages()`; a runtime invariant asserts it. | dsh | The session log *is* the trace. Anything the model saw can be audited, replayed, and offloaded. |
| **I4** | **The log is append-only; the surface is what changes.** Compaction, pruning and offloading append events whose `surfaceOp` replaces nodes in the derivation; history is never rewritten. | dsh | One mechanism for compaction, offload, rollback and prefix-cache stability, and the reason a checkpointer could not substitute (port plan §8). |
| **I5** | **Seams have three roles** — Definition, Provider, Consumer. | dsh | Containment tiers, runtimes and persistence backends are provider swaps, not redesigns. |
| **I6** | **Every projection equals its fold.** `harness_state.json` equals the `harness/*` fold; `kernel-state.dill` equals the `kernel/snapshot` chain; a runtime invariant asserts both. | D14, D17 | The log is the single source of truth; files are for humans. Fork inherits state *as of the boundary*, which a file cannot express. |
| **I7** | **The knowledge layer may only reference capability that already exists.** `/refine` writes procedure, never capability; an unresolvable `reference` is rejected. | Q13 | Keeps the model from expanding its own governed surface. Forces it to *ask* for a plugin rather than assert one. |
| **I8** | **Containment is not interception, and no document may blur them.** Bindings are governed per call; model-authored raw Python is bounded by tier, never gated. | D20, Q10 | The one place the harness cannot enforce, stated so a deployment does not write a `deny` row and believe it is enforced. |

---

## 2. The safety and stability surface

Every row: **ID · feature · what failure it prevents · mechanism · phase · gate**. Gates are named tests; the §4 item that owns each gate is listed under **Built by**.

### 2.A Log and state integrity *(I3, I4, I6)*

| ID | Feature | Prevents | Mechanism | Phase | Gate |
|---|---|---|---|---|---|
| A1 | Append-only `SessionEvent` log | silent history rewriting; unreconstructable sessions | `append()` snapshots data losslessly, assigns `seq = len(log)`, stamps `time`, deep-freezes, validates `surfaceOp`; listener failure cannot un-append | 0 | `seq == len(log)` property; JSON losslessness (BigInt/NaN/-0/cycles rejected); frozen after append |
| A2 | `derive_messages()` is the only path to model context | content reaching the model that is not in the log | per-node projection cache keyed on `replaceGeneration`; the loop asserts `messages == derive_messages()` on every request | 0 | invariant fires on a fake request that bypasses derivation |
| A3 | Surface `replace` for compaction and offload | destroying history to save context | surface events carry `surfaceOp: append \| {op: replace, start, end}`; shadowed nodes leave the derivation, never the log | 0/4 | `replace` op leaves `log` intact and `derive_messages()` shows the summary; originals in `conversation_history/` |
| A4 | Checkpoint-policy flush barriers | losing work between a model request and a crash | `session/flush` (parallel) before each model request, before top-level tool dispatch, at step end | 1 | crash injected after each barrier; resume shows everything before it |
| A5 | Crash repair | corrupt open turns on resume | `interruptedTurnClosers` synthesizes `tool/result {TOOL_NOT_STARTED \| TOOL_OUTCOME_UNKNOWN}` + `step/end` + `turn/end{interrupted}` | 1 | open-turn fixtures resume cleanly; synthetic results match dsh's vocabulary |
| A6 | Fork only at closed-turn boundaries | forking mid-turn inconsistency | `ctx.sessions.fork(source, boundary)` rejects `OPEN_TURN`; `session/end-seed` marks the seed; `header.seedLength` records lineage | 0 | fork inside an open turn is refused; fork at a boundary replays identically |
| A7 | Runtime state lives in the log (D17) | cross-call state invisible to the log — dsh's stated reason for refusing a persistent kernel | `kernel/snapshot {kind: snap \| patch \| clear \| recipe}` per variable, digest-skipped when unchanged, HMAC-SHA256 tag bound to **session id**, blobs > 64 KiB to `ctx.spill_store` | 3 | fork at a boundary restores *that* namespace, not the parent's latest; tampered or cross-session blob fails verification, is not unpickled |
| A8 | `recipe` snapshots for harness-loaded corpora (Q4) | an over-cap variable silently vanishing after restart | `{loader, sources, digest}` recorded instead of dropping; re-resolved on restore with rebuilt/unavailable notices | 3 | over-cap corpus rehydrates on digest match; changed sources produce the rebuilt notice; never an undefined name |
| A9 | Harness state is a fold (D14, Q5) | two-writer races; prompt content not in the log | local: `harness/*` events; global: `$PH_HOME/harness/events.jsonl` under `filelock`; `harness_state.json` written after apply, **never read back** | 3 | delete the file, re-derive, byte-identical; two concurrent sessions do not corrupt the global log; incremental cache does not re-fold from zero |
| A10 | Write-ahead ordering for blob-bearing events (§4.9) | orphaned blobs after a crash between blob write and event append | append the event carrying digest + locator **first**, write the blob **second**; missing blob → recoverable `kernel/restored {failed}` | 3 | `SIGKILL` mid-snapshot leaves no unreferenced blob after the next session open |
| A11 | "Every projection equals its fold" invariant (I6) | projection drift | runtime invariant plugin compares file to fold for harness and kernel state | 6 | invariant fires on a hand-edited projection file |
| A12 | Prefix-cache stability | silent cache-hit regressions billed rather than caught | static sections precede `context()` snapshots; `request/header` changes are explicit events; a recorded session asserts consecutive requests share the predicted prefix | 1 | prefix-stability test on a recorded session |

### 2.B Tool pipeline governance *(I1, I3)*

| ID | Feature | Prevents | Mechanism | Phase | Gate |
|---|---|---|---|---|---|
| B1 | The full pipeline on every tool call | any action skipping policy | `tools/pre-execute` → monotonic guards → `ctx.approval` on `ask` → `tools/execute` → body → `tools/post-execute` → normalize → `finalize_content` → `tools/result` → `tool/result` event | 1 | dsh's ordering invariants; a listener that vetoes stops everything downstream |
| B2 | Monotonic guards | a later listener re-permitting a denial | `ctx.tools.guard()` returns a reason or `None`; a denial cannot be turned back | 1 | guard denial survives a later `allow` listener |
| B3 | Approval fails closed | proceeding on an unanswered or unavailable approval | `request()` → `allowed-once \| rejected \| cancelled \| unavailable`; only `allowed-once` proceeds; `approval/asked` then `approval/decided` logged | 1 | `unavailable` and `cancelled` both deny; pending approval is re-asked on resume because `asked` without `decided` is visible in the log |
| B4 | `tool/call` logged **before** execution | an action with no durable record | the event is appended at pipeline entry with losslessly-snapshotted arguments | 1 | a tool body that crashes still leaves its `tool/call` |
| B5 | Lossless result normalization | a throwing tool corrupting the loop | throws become `is_error` results; only `content/error/meta` persist | 1 | a raising tool body yields a structured `is_error` result and the turn continues |
| B6 | Execution-mode barriers | concurrency-unsafe tools overlapping | `execution_mode: parallel \| exclusive`; exclusive calls drain the pool and bar later calls; results commit in model order | 1 | an exclusive call never overlaps; results commit in model order regardless of completion order |
| B7 | Scoped tool visibility | one agent seeing another's tools | `ctx.tools.register` on `agent.ctx` shadows by name; `ctx.tools.restrict(filter)` masks per scope; `schemas(scope)` reports what that scope sees | 1 | a restricted-away global reads as absent to that agent and present to others |

### 2.C Code Mode governance *(C1–C3, D18, Q9)*

| ID | Feature | Prevents | Mechanism | Phase | Gate |
|---|---|---|---|---|---|
| C1 | One transport, bindings re-enter the pipeline | one cell = one governance evaluation regardless of what it does | `present_as("code")`; every `await tools.<name>(...)` is a `call` frame dispatched through **B1** as a sub-call with the outer token as `parent` | 3 | a cell calling `tools.edit` fires `fs/write-intent` and produces `tool/code-dispatch-start`/`tool/code-dispatch` |
| C2 | Per-dispatch durable records | forty writes recorded as one stdout blob | `tool/code-dispatch-start {id: <parent>:code:<n>}` at entry; `tool/code-dispatch {content, is_error}` at settle; log-only, offloadable via `tools/code-dispatch-log` | 3 | 3 binding calls → 3 dispatch pairs; `ToolCallLimit` ticks 3 times |
| C3 | Denial fails the run (Q9) | a program catching a denial and routing around it | first `deny` or rejected `ask` settles the run as `CodeRunFailure {kind: "denied"}`; run-scoped abort fires; queue drains inside the open turn. A *failed* call keeps `ToolCallError` | 3 | denied `tools.edit` fails the whole run, file unwritten; a timeout raises `ToolCallError` inside the program |
| C4 | Per-cell dispatch budgets | one approved cell issuing unbounded governed calls | `max_dispatches_per_run = 256`, `max_subagent_spawns_per_run = 32`, enforced in the bridge as `CodeRunFailure` | 3 | a runaway loop fails at the budget with the budget named |
| C5 | Per-binding offload | oversized results melted into stdout | each dispatch result passes `tools/post-execute`; the spill policy replaces it *individually* with preview + locator | 3/4 | an oversized `tools.read` result is spilled while its siblings stay inline |
| C6 | `UNKNOWN_TOOL` for model-direct calls under `code` | policy bypass via a native call the prompt did not offer | resolved at execution creation, before `tools/pre-execute`; denial text names the route back | 1 | a native `edit` call under `mode: code` is refused before any listener runs |
| C7 | Family boundary as a monotonic guard | a child messaging outside parent/siblings/children | `ctx.tools.guard` on `agent_message.send` and on `ctx.subagents.followup` | 3 | send to a non-family target refused; a later listener cannot re-permit it |
| C8 | Message rate limits | a child flooding a sibling | `tools/pre-execute` policy: 16 384 chars, 20 pending/session, token bucket 3 / 1 s per sender→target | 3 | the 21st pending message queues; the 4th in a second is delayed |
| C9 | `access` policy-capped at pre-execute | a child obtaining a writable repo by asking | the `rlm.run` binding's `access` kwarg is subject to a `tools/pre-execute` policy; a deployment can forbid `write` for children without prompt changes | 3 | with the policy row, `access="write"` is denied and the spawn fails the run |
| C10 | The program is a hostile peer | forged frames reaching the host | every inbound fd-3 frame is shape-validated and **rebuilt**; extra fields dropped; non-numeric ids never echoed; junk → `None`, never a raise in the handler | 3 | fuzzed frames: none raise in the host; none forge a reply id |

### 2.D Runtime safety *(D19)*

| ID | Feature | Prevents | Mechanism | Phase | Gate |
|---|---|---|---|---|---|
| D1 | Out-of-process CPython | model code crashing or monkeypatching the harness | `code-runtime-python`: a subprocess with fd 3 as the framed channel; nothing shared but the pipe | 3 | `os._exit(1)` in a cell → `kernel/reset`, host unaffected, next run succeeds |
| D2 | No magics | `%%bash` as an unregistered shell tool (feature map item 4) | pH's runtime executes plain Python via `PyCF_ALLOW_TOP_LEVEL_AWAIT`; there is no IPython to supply magics | 3 | `%%bash` in a cell is a `SyntaxError` with the SDK's `tools.bash` guidance |
| D3 | Resource limits | a runaway cell consuming the host | `boot {cpu_seconds, address_space_bytes, max_log_bytes, max_value_bytes}` applied in the child before `boot-ack` (`RLIMIT_CPU`, `RLIMIT_AS`; Job Object limits on Windows) | 3 | a `while True` cell hits `cpu_seconds`; a memory bomb hits `address_space_bytes`; both settle as `CodeRunFailure` |
| D4 | Output caps with a shared marker | unbounded stdout into context | each stream capped at `max_log_bytes` (65 536) with a byte-identical truncation marker on both sides | 3 | marker text equals on host and guest (mirror test) |
| D5 | Cancellation without deadlock | an in-flight run holding the channel | `cancel` frame + `SIGINT`; fd 3 is not the channel the run occupies, so no control-channel workaround | 3 | `cancel` aborts a running cell; the next `run` succeeds |
| D6 | Persistence obligation asserted at registration | a persistent provider that forgets to snapshot | `persistence: "namespace"` requires `kernel/snapshot` emission; checked at `ctx.provide`, not at runtime | 1 | registering a namespace provider with no snapshot hook fails at registration |
| D7 | Protocol drift guard | host and guest disagreeing on a frame shape | versioned vocabulary; `boot-ack` from a mismatched guest refused; cross-implementation e2e asserts constants and each frame's required/optional field set | 3 | mirror test spawns the real child and diffs field sets |
| D8 | Runtime venv is cache | a rebuildable multi-GB venv in backups | lives at `$PH_CACHE/runtime-venv`; no `ipykernel`/`jupyter_client`/`nest_asyncio`; bootstrap marker detects staleness | 3 | deleting `$PH_CACHE` costs a rebuild and nothing else |

### 2.E Containment *(D20, D21, Q9–Q11, I8)*

| ID | Feature | Prevents | Mechanism | Phase | Gate |
|---|---|---|---|---|---|
| E1 | Three-tier ladder, honestly described | a tier *name* overstating what it does | `advisory` → `worktree` → `sandbox`; §4.8's table states per tier what is bounded, what is **not**, and which property is bought (isolation+revertibility vs confinement) | 4 | docs test: no tier is described as bounding writes it does not bound |
| E2 | Per-agent worktree (D21) | fan-out children trampling one tree | `workspace-git-worktree`: `git worktree add` on `ph/<session>/<agent>`; `ctx.fs` root **and** `ctx.subprocess` cwd resolve to `workspace.root` | 4 | two children on separate branches; parent merges both diffs without collision |
| E3 | `access: write \| read` with honest kinds | a caller assuming "read-only" a tier cannot enforce | `worktree` + `read` → `worktree-ephemeral` (writable, discarded, never merged; `repo_writable: True`); `sandbox` + `read` → `readonly-scratch` (`repo_writable: False`) | 4/6 | an ephemeral child's dirty worktree is discarded; the parent's branch is untouched; `repo_writable` reports the truth per tier |
| E4 | `access` defaults to `read` for children (Q11) | a writing child that should not have written | the recoverable default; the RLM prompt states it; one default across all front-ends | 3 | omitted `access` yields a research-shaped child |
| E5 | `scratch` always writable | read-only that is safe and useless | `<agent artifacts>/scratch/`, on every kind and tier, survives disposal as an artifact | 4 | a `readonly-scratch` child writes notes to `scratch` and nowhere else |
| E6 | Default write scope = worktree + scratch (Q9) | an approval queue interrupting every cell | `SandboxExecutionPolicy {mode: workspace-write, workspace_root: <worktree>}` + a `permissions-fs` row; prompt only for writes outside | 4 | a cell writing inside its worktree prompts zero times; one write outside prompts once |
| E7 | Per-run checkpoint and `/revert` (Q9) | an unrecoverable partial state after a denied run | `git add -A && git write-tree` under `refs/ph/<session>/<agent>/pre-run/<seq>` before a mutating run; `workspace/checkpoint` event; `/revert <seq>` restores tracked + untracked-not-ignored, never `.gitignore`d paths or `scratch` | 4 | a denied run reverts exactly; pre-denial writes gone; ignored paths untouched |
| E8 | `containment.strict` (Q10) | a host without a sandbox silently running an `advisory` agent that believes it is confined | refuses to start unless tier is `sandbox` **and** `enforcement: full`; `partial` is a refusal, not a downgrade — dsh's `SANDBOX_UNAVAILABLE` posture at profile start | 4 | strict on a host with no backend refuses, naming the missing one |
| E9 | Permission rows self-report reach (Q10) | a `deny` row believed to cover raw Python | row validation and `ph doctor` both say "applies to tool calls; raw `open()`/`subprocess` is not covered" when no confining provider is mounted | 4 | the message appears without a sandbox and disappears with one |
| E10 | `ph doctor` reports the *effective* tier | configured `worktree` silently degrading to `shared` on a non-repo cwd | provider declines, loader falls back with a logged notice, doctor prints effective tier + per-agent `kind` + `repo_writable` | 4 | non-repo cwd → doctor says `advisory`, not `worktree` |
| E11 | `readonly-scratch` enforced by the sandbox (Q6) | a research child writing the repo | `ctx.sandbox.confine()` with `workspace_root: <scratch>` — repo readable, only scratch writable, at the kernel | 6 | an absolute-path `open("/repo/x", "w")` is refused; scratch write succeeds |
| E12 | Build-tool redirection env | `pytest` failing under a read-only repo | `TMPDIR`, `PYTHONPYCACHEPREFIX`, `PYTEST_ADDOPTS=-p no:cacheprovider --basetemp=<scratch>/pytest`, `PIP_CACHE_DIR`, `UV_CACHE_DIR`, `GIT_CONFIG_GLOBAL` → scratch; documented best-effort | 4 | `pytest` runs green inside a `readonly-scratch` child, writing nothing outside scratch |
| E13 | The `sandbox` tier is the only confinement (Q10) | `worktree` mistaken for a boundary | an absolute-path raw write escapes `worktree` and is refused under `sandbox`, asserted so the tier table cannot regress | 4/6 | the paired assertion runs in CI |
| E14 | Worktree provisioning | a contained child that cannot run the project: `git worktree add` carries no ignored files, so `.env` and every dependency directory are absent and the child reads the failures as its own bug | `copy` (reflink where the filesystem has it) / `hardlink` / `symlink` lists applied by the **seam** after `acquire`, on every kind with a fresh root; **materials only — no command runs**, so provisioning is offline and cannot fail on a registry outage; `from` relative to the *base* (the parent's tree), `to` relative to the new root, **absolute paths refused outright** and every resolved path re-checked to be inside its base, SameFile-guarded, symlink refuses to clobber; provisioned paths appended to the worktree's own `info/exclude` so they never make it dirty; `workspace/provisioned {agentId, copied, linked, failed}`; a failure reaches the child's workspace prompt line, not a warning | 4 | a child reads the parent's gitignored `.env` and runs `pytest` green against a symlinked dependency dir; a provisioned path leaves a clean tree clean; a repo-supplied `from: ../x` or an absolute `from` is refused; a symlink cannot be planted pointing outside the base; provisioning opens no socket |
| E15 | Kept worktrees are a human's to finish | orphaned worktrees and `ph/*` branches accumulating — the keep-dirty policy's own by-product, and the accumulation wtp's README opens with | `/workspaces` lists kept trees (agent, session, branch, dirty); `merge <agent>`; `remove <agent> [--with-branch]` with `-d` by default and `-D` only on `--force-branch`; refuses a tree a live agent holds; `ph doctor` counts them; `workspace/acquired` carries a decline reason (`not-a-repository \| branch-in-use \| path-exists \| git-failed`) the doctor prints | 4 | `remove --with-branch` on a merged branch removes both and on an unmerged one refuses naming the flag; `remove` on a held tree refuses; doctor shows the decline reason on a non-repo cwd |

### 2.F Resource lifecycle *(§4.9, I2)*

| ID | Feature | Prevents | Mechanism | Phase | Gate |
|---|---|---|---|---|---|
| F1 | All artifacts acquired via `ctx.effect()` | a plugin holding a handle outside the seam | child processes, worktrees, temp paths, locks all registered as effects over `AsyncExitStack`; LIFO, children first | 1 | agent-scope disposal releases every artifact the agent took; a plugin holding one outside the seam fails a lint |
| F2 | Graceful shutdown | `ph` quitting with live children | `atexit`; `SIGTERM`/`SIGINT` → orderly `dispose()` with a 10 s grace then self-`SIGKILL` | 1 | `SIGTERM` unwinds the scope within the grace period |
| F3 | Die-with-parent, per platform | orphaned runtime children after a host `SIGKILL` | Linux `PR_SET_PDEATHSIG`; macOS `os.getppid()` poll in the guest; Windows Job Object `KILL_ON_JOB_CLOSE` | 3 | `SIGKILL` the host on each platform; no surviving child |
| F4 | Zombie reaping | exited children never reaped by a live parent | `await proc.wait()` in a `finally` on every spawn path | 3 | a normally-exited run leaves no zombie |
| F5 | Process-level orphan journal | strays from a session nobody reopens | `$PH_RUNTIME/processes.jsonl`, `fsync` on append (pid, start time, argv digest, session id); swept at **every** pH start | 3 | a journalled stray from a prior hard kill is killed at next start; a reused pid with a different start time is not |
| F6 | Paired events reconciled at session open | a leaked worktree | `workspace/acquired` without `disposed` is detected on open | 4 | a crash between acquire and dispose is reconciled on the next open |
| F7 | Blob GC at session open | unreferenced spill blobs accumulating | sweep `ctx.spill_store` for blobs no event references | 3 | orphaned blob removed on next open |
| F8 | Ephemeral scratch is `TemporaryDirectory` in an effect | GC-timed cleanup | `mkdtemp` (0700, unguessable) wrapped in `ctx.effect()`; known to be layer 1 (its cleanup is a `weakref.finalize`) | 1 | disposal removes it before GC would |
| F9 | `$PH_RUNTIME` is **not** scratch | randomising the socket and journal path | well-known location, contents outlive the writer; tier-3 `/tmp/ph-$UID` checked for dir/uid/0700/not-symlink | 0 | wrong-owner `/tmp/ph-$UID` refuses to start |

### 2.G Context stability *(Deep Agents features on dsh waterfalls — D12)*

| ID | Feature | Prevents | Mechanism | Phase | Gate |
|---|---|---|---|---|---|
| G1 | Todo planning | goal drift in a long recursive loop | `tool-todo` + `todo/write` event + prompt section; opt-in row | 4 | `write_todos` parallel-call error; reminder text matches |
| G2 | Large-result offload | context exhaustion by one tool result | `tools/post-execute` at ≥ 20 000 tokens (× 4 chars) → spill + preview (head 5 / tail 5 / 1 000-char clip); excluded tools untouched | 4 | replaces at 80 001 chars, not 80 000 |
| G3 | Input offload | a pasted blob exhausting context | ≥ 50 000 tokens on a human message → spill + preview | 4 | threshold test |
| G4 | Threshold summarization | context overflow mid-task | `compaction-summarize` on `agent/pre-step` (pressure) and `agent/request-error` (overflow): trigger `0.85` fraction / keep `0.10`; window unknown → 170 000 tokens / keep 6 messages; cutoff never splits a call/result pair; summary is a surface `replace` | 4 | triggers at 0.85 with a known window; never splits a pair; log intact |
| G5 | Model / tool call limits | infinite loops | `ModelCallLimit` / `ToolCallLimit` as `agent/pre-step` reject and `tools/pre-execute` deny; end / continue / error behaviours; breaker on repeated identical failures | 4 | each behaviour tested; the breaker trips on N identical failures |
| G6 | Human-in-the-loop | destructive actions without a human | `tools/pre-execute` `ask` → `ctx.approval`; decisions `approve \| edit \| reject \| respond`; modes `MANUAL \| AUTO \| YOLO`; destructive classifier for `run_code` and mutating bindings | 4 | edit and respond flows via pilot tests; a destructive cell prompts in `MANUAL` |
| G7 | Filesystem permissions | writes outside an allowed set | `permissions-fs`: `{operations, paths, mode: allow \| deny \| interrupt}` first-match-wins; recursive `delete` fails closed | 4 | first-match tests; recursive delete with a possibly-matching descendant refused |
| G8 | Memory | re-learning across sessions | `memory-agents-md` from `$PH_HOME/AGENTS.md` + `<project>/AGENTS.md`; placed **after** caching so edits do not bust the static prefix | 4 | prefix test unchanged by a memory edit |
| G9 | Progressive skills | prompt bloat from every skill's body | `skills-progressive`: catalog in prompt, body on demand; limits name 1–64, description ≤ 1 024, file ≤ 10 MiB | 4 | limits enforced; body absent until requested |
| G10 | Compaction is RLM-aware | REPL variables lost across compaction | the summary prompt lists live runtime variables from the snapshot manifest; runtime state itself is untouched | 4 | variables survive compaction; summary names them |

### 2.H Knowledge-layer safety *(Q13, I7)*

| ID | Feature | Prevents | Mechanism | Phase | Gate |
|---|---|---|---|---|---|
| H1 | Reference resolved before apply | `/refine` teaching the model to call something that does not exist | `reference.import`/`callable` resolved via `ctx.code_runtime` in a silent cell; unresolvable → edit rejected, failure on the `harness/refined` event | 3 | an entry naming a missing import is rejected with the failure recorded |
| H2 | Call patterns rendered as bindings | `/refine` steering the model onto the raw-namespace path | `call_pattern` rendered as `await tools.<name>(...)` wherever a binding exists | 3 | a skill entry for `edit` renders the binding form |
| H3 | `scope: global` approval-gated | the model editing every future session including other projects | a global edit is a `tools/pre-execute` `ask` on the `refine` tool | 3 | a global edit prompts; a local one does not |
| H4 | No promotion command | refinement output treated as proto-tools | `ph tool scaffold` explicitly rejected; `ph harness report` is analytics only | 6 | (design gate — the command does not exist) |
| H5 | `base_system_prompt` immutable | `/refine` rewriting the doctrine | validator rejects the id | 3 | rejected with prime-agent's error text |
| H6 | Rollback by event id | an unrecoverable bad refinement | `/refine --rollback <id>` builds the inverse from the event's before/after snapshots | 3 | apply + rollback returns the fold to its prior state |
| H7 | Auto-refine gated and cooled | runaway self-modification | every 25 turns or after compaction; cheap LLM review gate; 20-minute cooldown; `session_before_refine` waterfall veto | 3 | cooldown respected; veto honoured |

### 2.I Process, path, wire and credential security *(Q1, Q2, Q7)*

| ID | Feature | Prevents | Mechanism | Phase | Gate |
|---|---|---|---|---|---|
| I-1 | `$PH_RUNTIME` resolution order | a socket or PID journal in a synced dotdir | `$XDG_RUNTIME_DIR/ph` → per-user `$TMPDIR/ph` → `/tmp/ph-$UID` (checked) | 0 | `ph doctor` prints the resolved tier; the tier-3 check refuses a bad dir |
| I-2 | One daemon per user (Q7) | one root OOMing another user's agents | documented line; isolation between users is the operator's layer | 5 | doctor reports the worker model; docs state the non-guarantees |
| I-3 | Credentials never as values | a secret reaching a child, a log or a container | `ctx.credentials` passes `CredentialRef`; adapters resolve at the edge | 1 | no credential value appears in any event, frame or child env |
| I-4 | Subprocess env scrubbed | secrets inherited by model-run code | parent env scrubbed of `*KEY*`/`*SECRET*`/`*TOKEN*`/`*PASSWORD*` before spawn | 1 | a planted `FOO_API_KEY` is absent in the child |
| I-5 | Session leases | two writers on one JSONL | in-process lock per root; `filelock` on the canonical path against a *second daemon* | 5 | concurrent open → `session_already_active` |
| I-6 | Lingering detection | a daemon that outlives logout but loses its socket | `ph doctor` reports `$XDG_RUNTIME_DIR` derivation and `loginctl` linger state, naming `enable-linger` when a daemon is configured without it | 5 | simulated session end → clear "socket path removed" diagnostic |
| I-7 | Wire casing: declare, never derive (Q2) | field-name drift between Python and JSON | `WireModel` base with `alias_generator=to_camel` + `populate_by_name=True`; `SessionEvent.to_wire()` pinned to `to_camel(field)` by test | 0 | round-trip property; no field reaches the wire un-aliased |
| I-9 | **Attachment ingest is split by who asked** | a tool that attaches `~/.ssh/id_rsa` as a "document" and lets the provider OCR it | human-initiated ingest may read any path the person can (they are the one attaching); **model-initiated ingest goes through `ctx.fs`**, so `permissions-fs` and the workspace tier bound it like every other read | 7 | a tool-initiated attach of a path outside the workspace is refused; the same path attached by the human is not |
| I-8 | Config is data, not code (D9) | `eval` on a user-controlled config | YAML rows with `${env:VAR:-default}` only; no `!!js`/`!!py` | 0 | a tag that would evaluate is rejected |

---

## 3. Non-goals — stated so nobody assumes otherwise

Each is documented in the plan, the first-run notice, and where relevant in `ph doctor`. **A reviewer finding any of these implied as covered should file it as a defect.**

| # | pH does **not** | Because | Bounded instead by |
|---|---|---|---|
| N1 | intercept model-authored raw Python (`open()`, `subprocess.run()`) | a deny-list needs a registered name; the model is authoring its own tool (§4.8, §6a) | the containment ladder — `worktree` for location, `sandbox` for confinement |
| N2 | treat `worktree` as confinement | an absolute-path write never consults cwd | only `sandbox` refuses it; the tier table says so |
| N3 | undo the world with `/revert` | git restores the tree, not a published package or a dropped table | `/revert` lists the run's non-filesystem dispatches so irreversible actions are visible |
| N4 | replay raw writes | they are bounded by the worktree but not recorded | the checkpoint is the recovery mechanism; replay is explanation |
| N5 | cap per-root memory or contain crashes between roots in the daemon | one `anyio` task per root (Q7) | one daemon per user; process-per-root is a later provider swap |
| N6 | manage containers | environment decision, operator's layer (Q12) | a "running pH in a container" page; fd 3 crosses the boundary as an inherited descriptor |
| N7 | run any cleanup on `SIGKILL` | no finalizer does, on any platform | the crash-recovery layer: paired events + the orphan journal |
| N8 | provide provenance for raw reads | a raw `open()` has no `tool/call` | bindings, which the SDK advertises as the path of least resistance |
| N9 | reconstruct Code Mode intermediate values from the log | dsh's own stated limitation | `kernel/snapshot` + `recipe` keep *harness-created* bulk state reconstructable |
| N10 | enforce a `deny` row without a confining provider | see N1 | rows self-report their reach; `containment.strict` refuses to start until confinement is real |

---

## 4. Work breakdown

IDs are `P<phase>-<nn>`. **Delivers** names §2 rows; **Gate** is the test that closes the item. **Depends** lists blocking items only. Items within a phase are in dependency order; unlisted pairs are parallelisable.

### Phase 0 — Spike the core *(1–2 weeks)*

| ID | Work item | Depends | Delivers | Gate |
|---|---|---|---|---|
| P0-01 | Repo scaffold: `uv` workspace with `ph-core`, `ph-app`, `ph-rlm`, `ph-stabilize`; `ruff` (line 100), `mypy --strict` on `ph-core`, `pytest` + `anyio`; CI matrix Linux / macOS / Windows; `[project.entry-points."ph.plugins"]` group reserved | — | I1 | CI green on an empty package set on all three OSes |
| P0-02 | `ph.cordis` Context: `plugin()`, `inject()`, `provide()`, `__getattr__` most-specific-wins, `effect()` returning a disposer, `scope()`, `dispose()` LIFO children-first | P0-01 | I1, I2 | disposal unwinds every effect in reverse; a disposed scope's services are gone |
| P0-03 | `ph.cordis` dispatch: `emit`, `waterfall` (outermost-first, `next`, veto-by-return), `parallel` (`allSettled` + `AggregateError`), `serial` (bail on non-null), `bail`; `prepend`, `global`, scope filtering | P0-02 | I1 | waterfall veto stops built-in behaviour; `next()` ordering; parallel aggregates rejections |
| P0-04 | Event registry: `events.declare(name, mode, payload)`; `ctx.<mode>` raises on mismatch; producer/consumer matrix export | P0-03 | I1 | dispatching an event under the wrong mode raises |
| P0-05 | Loader: YAML rows in file order, id-addressed patches (whole-config replace, insert), `${env:VAR:-default}`, `disabled:` predicates, entry-point discovery, inject-driven activation/deactivation, **no code evaluation** | P0-04 | I1, I-8 | `--dump-config` shows composed rows; a `!!js`-style tag is rejected; a plugin activates only when its `inject` keys are provided |
| P0-06 | `WireModel` base (`alias_generator=to_camel`, `populate_by_name=True`); round-trip property test; "no un-aliased field" assertion | P0-01 | I-7 | property passes over every model; a model without the base fails the assertion |
| P0-07 | `SessionEvent` frozen `dataclass(slots=True)`: `type, seq, time, data, ignorable?, sourceEventSeqs?, surfaceOp?`; `to_wire()`/`from_wire()`; test pinning each mapping to `to_camel(field)` | P0-06 | A1, I-7 | pin test; wire form equals dsh's envelope byte-for-byte on a fixture |
| P0-08 | `ph.session.append()`: lossless snapshot (`snapshotJsonValue` rejecting BigInt/undefined/-0/NaN/Map/Date/instances/cycles), `seq = len(log)`, `time`, deep-freeze, `SurfaceManager.validateNext`, push, publish `session/event` with listener-failure isolation | P0-07 | A1 | `seq == len(log)` property; losslessness rejections; a raising listener does not un-append |
| P0-09 | `SurfaceManager`: surface types require `surfaceOp`; `replace {start, end}` shadows nodes; `ignorable` skipped by unknown readers; unknown non-ignorable type refuses the log | P0-08 | A3 | replace leaves the log intact; unknown type refuses load |
| P0-10 | `derive_messages()` with per-node cache keyed on `replaceGeneration`; `request_header()` / `request_context()` incremental folds | P0-09 | A2 | cache invalidates only on generation change; folds are incremental |
| P0-11 | Seed / fork / resume: `session/end-seed`, `header.seedLength`, `fork(source, boundary)` rejecting `OPEN_TURN` | P0-10 | A6 | fork inside an open turn refused; fork at boundary replays identically |
| P0-12 | `ph.llm`: `Message`/`ContentBlock`/`StreamChunk` types, `BlockAssembler`, fake adapter emitting text/thinking/tool-call start/delta/end | P0-07 | — | assembler reconstructs a recorded stream |
| P0-13 | Minimal `ReactLoopAgent`: `turn/start`, inbox claim (`agent/inbox/claimed`), `system-prompt/assemble`, `agent/pre-step` (`reject \| enter`), `step/start`, `user/message`, `agent/request` → `llm/stream` → `assistant/chunk*` → `assistant/message`, `step/end`, `agent/turn-stopping`, `turn/end`; no tools | P0-10, P0-12 | A2 | the lifecycle events appear in order on a fake run |
| P0-14 | **Runtime invariant: `messages == derive_messages()`** on every request | P0-13 | A2, I3 | fires on a deliberately bypassed request |
| P0-15 | JSONL persistence provider + `session/flush` (parallel) + buffered async writer keeping `append` sync and I/O-free | P0-08 | A1 | flush drains; `append` never awaits I/O |
| P0-16 | Path roots: `$PH_HOME` / `$PH_CACHE` / `$PH_RUNTIME` resolution (XDG → per-user `$TMPDIR` → `/tmp/ph-$UID` with dir/uid/0700/not-symlink check); Windows mapping; `ph doctor` prints all three | P0-01 | I-1, F9 | wrong-owner tier-3 dir refuses to start; doctor output on each OS |
| P0-17 | `ph -p "…"` print mode; `--dump-config`; `dev-notes/phase-0.md` | P0-13, P0-15, P0-16 | — | a one-shot Q&A against the fake adapter writes an inspectable JSONL |

### Phase 1 — Core parity *(3–4 weeks)*

| ID | Work item | Depends | Delivers | Gate |
|---|---|---|---|---|
| P1-01 | `ph.tools`: `ToolDefinition` (mandatory `output` + `render`), `define_tool`, `register` global/scoped with shadowing, `restrict(filter)` with allow/deny intersection, `schemas(scope)`, `tools/change` | P0-05 | B7 | restricted-away global absent to that scope, present to others |
| P1-02 | Pipeline: `tools/pre-execute` → guards → `ask` → `tools/execute` (around, signal-only replacement) → body → `tools/post-execute` (`accept \| block`) → normalize → `finalize_content` → `tools/result` → `tool/result`; `tool/call` appended at entry | P1-01 | B1, B2, B4, B5 | dsh ordering invariants; guard denial survives later listeners; crashing body leaves `tool/call` and yields `is_error` |
| P1-03 | Execution modes: `is_concurrency_safe` classification, bounded rolling pool, exclusive barriers, model-order commit | P1-02 | B6 | exclusive never overlaps; commit order fixed |
| P1-04 | Presentation: `tools.mode: native \| code \| both`, `ctx.tools.present_as()` per agent, reserved `run_code`, `tools:sdk` section renderer (Python + TypeScript), `tools:code-only` rule, `UNKNOWN_TOOL` before policy | P1-02 | C6 | native `edit` under `code` refused before any listener; SDK block renders per language |
| P1-05 | Dispatch bridge (Code Mode `run_code` body): lossless-JSON sub-call snapshot, per-run pool honouring B6, `parent` token, full pipeline re-entry, `tool/code-dispatch-start`/`tool/code-dispatch`, `tools/code-dispatch-log` waterfall, run-scoped abort, drain-before-return; **denied sub-call settles the run as `CodeRunFailure {kind: "denied"}`**, failed sub-call raises `ToolCallError` | P1-03, P1-04 | C1, C2, C3, C5 | (tested end-to-end in P3-21; here: against `code-runtime-worker-thread` semantics with a stub runtime) |
| P1-06 | `ctx.code_runtime` seam **definition only**: `CodeRuntime` Protocol with `language`, `isolation`, `persistence`; `CodeRunRequest {program, bindings, namespace, signal}`; portable name rules; **registration assertion that `persistence: "namespace"` emits `kernel/snapshot`** | P0-02 | D6 | registering a namespace provider without the hook fails at `provide()` |
| P1-07 | System prompt: `section` / `context()` / `tools` / `variable`, ordering, `request/header` epoch fold and change detection | P0-13 | A12 | header appended only on change; static sections precede `context()` |
| P1-08 | `ctx.approval`: `request()` → `allowed-once \| rejected \| cancelled \| unavailable`; `approval/asked`/`decided`; `approval/request` waterfall to answerers; `ApprovalPolicy` from last `approval/policy`; fail-closed; re-ask on resume | P1-02 | B3 | `unavailable`/`cancelled` deny; asked-without-decided re-asks on resume |
| P1-09 | `ctx.user_questions`; `ctx.commands` (human slash commands, no model turn) | P1-08 | — | a command dispatches without a `turn/*` |
| P1-10 | `agent/request-error` waterfall + retry plugin (backoff, canonical context-overflow detection) | P0-13 | — | overflow classified; retry bounded |
| P1-11 | `checkpoint-policy`: flush before each model request, before top-level tool dispatch, at step end | P0-15, P1-02 | A4 | crash after each barrier; resume shows everything before it |
| P1-12 | Crash repair: `interruptedTurnClosers` on load | P0-11 | A5 | open-turn fixtures; synthetic vocabulary matches dsh |
| P1-13 | `ctx.token_meter`: provider usage authoritative, `tiktoken`/`len/4` estimation for pressure, `measure()` per node | P0-13 | — | baseline switches from estimate to usage after first response |
| P1-14 | `ctx.session_telemetry`: `SessionTelemetryRecord`, `session-telemetry/record` redaction waterfall, JSONL sink; first `assistant/chunk` only per step | P0-08 | — | redaction listener runs before any sink |
| P1-15 | `llm-tau-ai` adapter: OpenAI-compatible (DeepSeek incl. `reasoning_content`), Anthropic; `ctx.llm.register_adapter` | P0-12 | — | real-API smoke (skipped without key) |
| P1-16 | `ctx.credentials`: `CredentialRef` only; resolution at the adapter edge | P1-15 | I-3 | no value in any event, frame or child env on a planted-secret run |
| P1-17 | `ctx.fs` seam + `fs-local`: read/write/edit/glob/grep; `fs/write-intent`, `fs/edit-intent` waterfalls; `fs/observed`; read-before-edit policy row | P1-02 | — | write-intent fires before the write; veto prevents it |
| P1-18 | `ctx.subprocess`: explicit `SubprocessSpawnSpec {argv, cwd, stdio, grace_ms, env?}`, parent-env scrub, offset-based readers with spill, `await proc.wait()` in `finally`, spawned via `ctx.effect()` | P0-02 | F1, F4, I-4 | planted `FOO_API_KEY` absent in child; disposal terminates and reaps |
| P1-19 | `ctx.shell` (bash over subprocess); `ctx.sandbox` seam + `sandbox_policy` (policy-only provider; `confine()` throws `SANDBOX_UNAVAILABLE` rather than pass-through); `permission_presets` | P1-18 | — | requesting confinement with no backend throws, never runs unconfined |
| P1-20 | **§4.9 resource ownership**: `AsyncExitStack` behind `ctx.effect()`; `atexit`; `SIGTERM`/`SIGINT` → orderly dispose with 10 s grace then self-`SIGKILL`; `TemporaryDirectory` helper wrapped in an effect; lint forbidding `Popen`/`mkdtemp` outside the seam | P0-02, P1-18 | F1, F2, F8 | `SIGTERM` unwinds within grace; lint catches a raw `Popen` |
| P1-21 | `ctx.spill_store`: `save_text()` → `SpillRef {locator, bytes, retrieval_hint}` | P0-15 | — | round-trip |
| P1-22 | Tools: `bash`, `read`, `write`, `edit`, `glob`, `grep` with `present_call`/`present_result` cards; `agent-instructions` (AGENTS.md discovery) | P1-17, P1-19 | — | each tool's card renders from durable result alone |
| P1-23 | `ctx.jobs`, `ctx.settings`, `ctx.skills` seam (capability-layer packages) | P0-02 | — | job cancel/done hooks |
| P1-24 | `llm-replay` adapter; **prefix-stability test** over a recorded session | P1-15 | A12 | replay reproduces `derive_messages()`; prefix assertion passes |
| P1-25 | `json` / `transcript` / `rpc` modes (camelCase output), `headless` profile; `dev-notes/phase-1.md` | P1-22 | I-7 | RPC round-trip with the dsh Python SDK client shape |

### Phase 2 — TUI *(3 weeks)*

| ID | Work item | Depends | Delivers | Gate |
|---|---|---|---|---|
| P2-01 | `PHTuiApp` + adapter boundary + state; streaming transcript on `session/event` + `agent/*` | P1-25 | — | resume rebuilds transcript from `session.events` |
| P2-02 | `PromptInput`, sidebar, autocomplete, slash commands via `ctx.commands` | P2-01 | — | pilot keypress tests |
| P2-03 | Pickers: model / session / tree / theme / login | P2-01 | — | pilot tests through each |
| P2-04 | **Approval modal** (`allowed-once \| rejected` + reason), **ask-user modal**, permission-preset switcher, plan-mode review | P1-08, P2-01 | B3, G6 | approval round-trips through `approval/asked`/`decided`; `push_screen(callback)` never awaited in a handler |
| P2-05 | Themes/keybindings from `$PH_HOME/tui.json`; terminal title; notifications; project trust | P2-01 | — | snapshot tests |
| P2-06 | Textual discipline: `Content.from_markup("$var")`, never f-string markup, `notify(markup=False)`, off-pump continuations | P2-01 | — | bracketed user text renders literally |
| P2-07 | `tui` profile; `dev-notes/phase-2.md` | P2-05 | — | snapshot tests for streaming / tool card / error / compaction marker |

Phase 2 delivers dsh's **conversation** view (`ui-conversation`). dsh's
**trajectory** view (`ui-trajectory`) is the second projection of the same fold
and is scheduled at P3-24/P3-25, not here: its record set needs Code Mode
sub-dispatches (P3-19) and subagent attribution (P3-11), and its fork action is
coupled to P3-15. Deferring costs nothing, because the log already records every
field the view needs (I4, I6) — see port plan §5.5 for the two shapes considered
and why the harness-free reader won.

### Phase 3 — RLM bundle *(4–5 weeks; parallel with Phase 4)*

| ID | Work item | Depends | Delivers | Gate |
|---|---|---|---|---|
| P3-01 | fd-3 protocol vocabulary, both sides: `boot`/`boot-ack`/`run`/`call`/`reply`/`log`/`display`/`snapshot`/`restore`/`cancel`/`done`/`shutdown` as `TypedDict`s (guest) and `WireModel`s (host), camelCase; **version field**; `PROTOCOL_FD = 3`; shared truncation-marker function | P1-06 | D4, D7 | mirror test: constants and each frame's required/optional field set equal across sides |
| P3-02 | Host frame codec: `validateChildFrame` shape-validate and **rebuild**; drop extra fields; never echo a non-numeric id; junk → `None`; `hasUnsafeIntegerToken` on raw text | P3-01 | C10 | fuzzing: no raise, no forged reply |
| P3-03 | Guest runner (`ph_runtime.runner`): read `boot`, apply `RLIMIT_CPU`/`RLIMIT_AS` (Job Object on Windows), `boot-ack`; loop on `run`: compile with `PyCF_ALLOW_TOP_LEVEL_AWAIT`, exec as async body against persistent `globals()`, stream `log` with caps, `done {value?, error?}`; `display`; handle `cancel` (`SIGINT` → `KeyboardInterrupt` in the cell); `shutdown` | P3-01 | D2, D3, D4, D5 | top-level `await`/`return`; variable persists across runs; `%%bash` → `SyntaxError`; cpu/memory bombs → `CodeRunFailure`; `cancel` then next `run` succeeds |
| P3-04 | Guest die-with-parent: `PR_SET_PDEATHSIG` (Linux), `getppid()` poll (macOS), Job Object `KILL_ON_JOB_CLOSE` (Windows) | P3-03 | F3 | host `SIGKILL` on each OS → no surviving child |
| P3-05 | Host provider `code-runtime-python`: `language="python"`, `isolation="process"`, `persistence="namespace"`; spawn with `stdio=[pipe]*4` **through `ctx.effect()`**; `boot` with caps + namespaces + namespace id; `run` dispatch; `call` → **P1-05 bridge** → `reply`; `cancel` on abort; `shutdown` + 5 s kill; `await wait()` in `finally`; `<runtime_reset>` prefix after a kill | P3-02, P3-03, P1-05, P1-20 | D1, C1, F1, F4 | `os._exit(1)` in a cell → reset notice, host unaffected; disposal reaps |
| P3-06 | Orphan journal: `$PH_RUNTIME/processes.jsonl` `fsync`ed append (pid, start time, argv digest, session); startup sweep with start-time check | P3-05, P0-16 | F5 | journalled stray killed at next start; reused pid with other start time spared |
| P3-07 | Runtime venv builder at `$PH_CACHE/runtime-venv`: `uv venv --seed`, `ph-runtime-guest`, `dill`, `--editable <skill>` per Python skill; `PH_RUNTIME_PYTHON` override; staleness marker | P3-03 | D8 | deleting `$PH_CACHE` costs only a rebuild |
| P3-08 | `ph_runtime` guest package: `tools.*`, `rlm(...)`, `rlm.find_models/list_subagents/delete_subagent/replies`, `agent_message.*`, `agent_observe.*` proxies each marshalling one `call`; `wrap_skill_module`; bootstrap (`NO_COLOR`, namespaces into globals, skill import with "unavailable" stubs) | P3-05 | C1 | each proxy produces exactly one `call` frame with the declared shape |
| P3-09 | `rlm-presentation`: `ipython` as the presented name for the reserved `run_code` transport (`ctx.tools.present_transport`, a P3-09 addition to `ph-core` — an *alias* cannot work, because the reservation and the C6 refusal both compare against a name); result text `logs\nresult\ntraceback` (see the note below on the stream split); `IpythonToolDetails`; **`present_as("code")` stays the bundle's `tools.mode`, not a second claim by this row**; streaming deferred to P3-19 with its consumer | P1-04, P3-05 | C1, C6 | model sees one callable; SDK block lists every contributed namespace |
| P3-10 | `rlm-bindings`: the `tools` / `rlm` / `agent_message` / `agent_observe` namespaces as `CodeBindingNamespace`s; **budgets** `max_dispatches_per_run=256`, `max_subagent_spawns_per_run=32`. The extension point landed with P3-09 (reshaped by its cleanup pass): `ctx.tools.register_code_namespace(name, factory)` is a keyed, scoped claim — a name conflict fails at mount, not per cell — and the factory is asked one `CodeBindingsRequest` by both the run *and* the SDK prompt section, so the block cannot describe a namespace the program could not reach. A contributed binding's program-facing name is not the tool it dispatches to (`rlm.run` → `spawn_child`), and it goes through the same `DispatchBridge`, so C2's records and C4's budgets apply unchanged | P3-08, P3-09 | C4, C9 | runaway loop fails at the budget; `access="write"` denied under a policy row |
| P3-11 | ~~**The `ctx.subagents` seam itself is unscheduled**~~ — **landed**: the seam definition (`ph/seams/subagents.py`, row `subagents` in `ph-base`) plus the `rlm-child` provider. `SubagentRun` is the admission handle and never carries the answer; `result()` awaits quiescence for the generic `task` contract. Two things reshaped by writing it: `ctx.llm` has **no model catalogue**, so the no-fallback preflight is a *route* check (`adapter_for`) and `rlm.find_models` must bring the catalogue with it; and `ctx.jobs` runs a job **inline** when no task group is bound, which would have made admission block — so `Context.detach()` is new (the pool `drain()` already awaited, made reachable). `access` is validated, recorded, and downgraded to `read` with the reason on the event until `ctx.workspace` lands (Phase 4, D21). Original scope: — §5 of the port plan lists it among the capability seams, but no Phase 0/1 row delivers it and no code provides it, so the Definition half (`register_provider({name, capabilities, start(request) → SubagentRun{id, result, dispose}, prepare_continuable?})`, `start_continuable`, `followup`, `interrupt`, `report_from`) lands here with its first provider. Note also that `access` → `ctx.workspace.acquire` reaches **into Phase 4**: `ctx.workspace` does not exist yet, so P3-11 validates and *logs* `access` and wires it to the seam when D21 lands — the kwarg and its `read` default are the part that must not slip, because a prompt documenting it (P3-14) is a promise. Then: `rlm-subagent-provider` (`ctx.subagents` provider `rlm-child`): kwarg validation, depth gate (`RLM_MAX_DEPTH=2`), model preflight with no fallback, `subagent/admitted` **logged first** (the seam owns neutral event names — ph-app reads them and does not depend on ph-rlm), handle returned immediately, detached `anyio` task, `[task from parent]`, `subagent/status`, usage attribution `subagent/usage-attributed`, terminal notices, `subagent/deleted` tombstone; **the child is an effect of the parent's scope (I2)**, so disposal unwinds it and releases its kernel; `access` kwarg (default `read`) passed to `ctx.workspace.acquire` | P3-10 | E4 | spawn returns before child completes; depth error text; 8-child fan-out reconciles usage; omitted `access` → read |
| P3-12 | `rlm-messaging`: family roster + **monotonic guard** on `agent_message.send`; steer delivery with `deliveryStatus`; received-message rendering verbatim; the `agent_message` and `agent_observe` namespaces (completing P3-10's four). **The rate limit is a body-raised `ToolCallError`, not a `pre-execute` Deny** — under C3 a denial ends the whole cell, and four messages in one second must not cost the model its program; backpressure is the program's to handle and the deployment tunes it by row config. `reachable_family` in the seam is the enumeration half of `family_reach`, derived from it, so the guard and the roster cannot disagree. **Addressing a settled child fails** with the `agent_observe` route named: passivation/rehydration is P3-13's | P3-10 | C7, C8 | non-family send refused and not re-permittable; the pending cap refuses rather than growing a queue |
| P3-13 | `rlm-registry`: roster folded from `subagent/*` events; tombstones; passivation/rehydration (in-process variant). **The roster and the fold shipped with P3-11/P3-12** (`subagent_roster` in the seam, so ph-app's P3-19 panel can read it without depending on ph-rlm); what landed here is **rehydration on address**: settlement releases the child's *agent* (which is what holds an inbox) while its session, log and roster row survive, so `agent_message.send` to a finished child wakes it, through the one seam call `ctx.subagents.ensure_addressable(session_id)` and the declared `RehydratableProvider` protocol. The record is `{status: "running", cause: "rehydrated"}` — a cause rather than a status member, so a woken child that is working still reads as running. A **revoked** child is not revived: the tombstone is the parent's record and quietly reviving it would make that record false. Cross-restart passivation, where the session itself must come off disk, stays the daemon's (Phase 5) | P3-11 | — | roster survives restart and compaction by construction; addressing a settled child wakes it; a revoked one stays revoked |
| P3-14 | `rlm-prompt`: doctrine (ported, minus the raw-call-form line — `tools:sdk` *is* the generated listing that paragraph existed to substitute for), child doctrine at depth > 0, delegation section **conditional on the `rlm` namespace actually being in the resolved view** so the prompt cannot advertise a denied call, and the volatile facts (depth, cwd, log, family, roster) as a `context()` snapshot rather than a cached section (A12). Documents `rlm(..., access=)` and the `read` default. **The depth limit is read from the provider's config, not the module constant** — a prompt quoting the constant tells a model it has a level left after a deployment removed it. The **workspace section ships partial and honest**: `ctx.workspace` is Phase 4, so it says no tier is mounted and a `write` request is recorded but not granted, which is the same warning the full section exists to give | P3-09 | E4 | prompt reflects the kind the child actually got |
| P3-15 | `rlm-kernel-snapshot` (D17): per-variable `dill` + digest, 16 MiB/256 MiB caps, 1.5 s debounce; `snap`/`patch` (`bsdiff4`)/`clear`; **`recipe`** for harness-loaded over-cap variables; HMAC bound to session id; blobs > 64 KiB to spill; **write-ahead ordering**; re-anchor at `max_chain=32`; `restore` with `{restored, rebuilt, unavailable, failed}`; blob GC at session open; `bytes appended per cell` benchmark | P3-05, P1-21 | A7, A8, A10, F7 | fork at boundary restores that namespace; tampered blob refused; `SIGKILL` mid-snapshot leaves no orphan blob; recipe rehydrates with correct notice; benchmark decides patch vs snap-only |
| P3-16 | **Landed in full.** *Increment 1* — the fold at both scopes, validation (H1/H2/H3/H5), apply, rollback (H6), the prompt `context()`, the global log under `filelock`, the projection, and `/refine [--global] [--show] [--rollback <id>]`. *Increment 2* — the planner and auto-refine (H7): two `purpose: "refine"` calls (a cheap review gate, then the planner at `min(model max, 32_000)`), the pass as a `ctx.jobs` job owned by the agent's scope and released with `jobs.forget` when it finishes, and `harness/before-refine` as the veto waterfall. `purpose` gained `"refine"` in `ph-core` — a closed set on purpose, because opting out of the model-visible-means-logged invariant has to be a declaration the union can enumerate. Four decisions the plan left open: **the trigger state is a fold** (`due(session)` reads turns, elapsed and compaction off the log and uses the *triggering event's own timestamp* as the clock, so it answers the same on a resume, on a fork and in a test — no wall clock, no counters beside the session); **`harness/refine-considered` is what advances the cooldown**, so a declined review, an empty proposal, a planner that returned junk and a veto all cost one cheap call rather than one per turn — it is ignorable, because skipping it costs an extra review and not a wrong harness; **an automatic pass is local-only**, since an automatic `--global` would put an approval prompt in front of a user who asked for nothing about a change reaching every future project; and **a user pass skips the review gate**, because asking a model whether to do what the human just asked for spends a call to second-guess them. `/refine --show` replaced increment 1's placeholder status line and prints the harness *unbounded*, which is the one view the prompt section's bound hides. Four divergences the code carries with its reasons: **(i) one event type, not two** — a rollback is an inverse *applied*, and the record already carries `rollbackOf`, so `harness/rolled-back` would be a second fold case that has to agree with the first about one operation; **(ii) H3's gate is `ctx.approval.request(tool_name="refine")`, not a `tools/pre-execute` `ask`** — there is no `refine` *tool*, precisely because `/refine` is a command, so no `pre-execute` waterfall ever fires for it; the approval seam asks the same question through the same answerers and fails closed (B3), while registering a model-callable `refine` tool to satisfy the letter would hand the model the one operation the design says is the human's; **(iii) a rollback is the inverse *record*** (each `AppliedEdit` reversed with its snapshots swapped) rather than an inverse proposal re-run through `apply`, because re-proposing constructs fresh entries and bumps their versions — the restored entry would not equal the one it restores, and an inverse that does not invert is not one; the churn is in the refinement history, where an append-only log keeps it anyway. It is also deliberately **not revalidated**: that state was validated when written, and refusing to undo a bad refinement because the world has since moved would trap a user in the state they are leaving. **(iv) kinds are `note|procedure|skill`**, not prime-agent's `prompt|memory|skill|subagent`: a `note` rendered in the prompt section *is* a prompt patch with the doctrine off-limits (H5), and a `subagent` entry would be a preset — capability, which I7 puts outside the knowledge layer. Found by writing the gates: `HarnessService._local` was a `Field` object rather than a dataclass field (no annotation, so `slots=True` kept it as a class attribute); `_approved` compared the outcome against `"allowed"`, which is not an `ApprovalOutcome` at all, so **every** global refinement was refused however the human answered; and the TUI adapter's vocabulary test caught `harness/refined` unrendered — it is a row, not a record, because the planner refines at turn end with no command to show for it. Original scope: `rlm-harness` (note: `ctx.jobs.start` now takes `scope=` and a job is an effect of its owner (I2) — a `/refine` pass should be owned by the session that asked, so it is cancelled and dropped when that session goes; `jobs.forget` releases a finished one without reporting it cancelled): **fold** over `harness/*` with incremental cache per `(scope, last_seq)` — use `ph.session.SessionFoldCache`, which is that shape and carries the one requirement (the cached function must be a pure fold of the prefix); folds are deliberately *not* attached to `Session`, because `fold_namespace` and P3-24 both need to fold a prefix that is not the live log; global `$PH_HOME/harness/events.jsonl` under `filelock`; `harness_state.json` written after apply, never read; `/refine` as `ctx.jobs` background job at turn end; planner prompt; **validation + H1 (resolve via `ctx.code_runtime`), H2 (render `call_pattern` as binding), H3 (`global` → `ask`), H5**; apply with before/after snapshots; `harness/refined`; `/refine --rollback <id>`; auto-refine (25 turns / post-compaction, review gate, 20-min cooldown, `session_before_refine` veto) | P3-05, P1-08, P1-23 | A9, H1, H2, H3, H5, H6, H7 | delete-and-re-derive byte-identical; concurrent global writes safe; missing import rejected with failure on event; `edit` entry renders binding form; global prompts; rollback restores fold |
| P3-17 | **Landed.** `rlm-context-loader`, `disabled: true` in the bundle: `path`/`text` sources → corpus; `context_search`/`context_chunks`/`context_head` registered as **tools**, which is what makes them `await tools.context_search(...)` — Code Mode builds the `tools` namespace from the registry, so no custom namespace is needed and each query is its own governed dispatch; metadata-only prompt **section** (the corpus is resolved once at mount, so the text is fixed for the session and belongs in the cached prefix, unlike the harness's `context()`); `min_chars` defaults to 0 and `rlm-stable` sets 200 000. **The recipe is `context/loaded`, not `kernel/snapshot {kind: "recipe"}`**: the plan files it under the snapshot chain, which is right for a *kernel variable* the harness built, but this corpus never enters the kernel — precisely because access is a binding — so recording it as a namespace variable would describe a namespace that does not contain it. Same `{loader, sources, digest}` semantics, honest event: a matching digest is silent, a changed one tells the model the corpus was rebuilt, an unreadable source says so. A `Document` keeps line *offsets* rather than a tuple of lines, because a corpus worth this row would otherwise hold every line as its own string for the process's life | P3-10, P3-15 | A8, C5 | queries produce dispatch records and are individually offloadable; corpus rehydrates after restart |
| P3-18 | **Landed.** `rlm-skills-python`: `<dir>/<name>/SKILL.md` frontmatter validated to the format `skills-progressive` will read (name 1-64 lowercase `[a-z0-9-]` **and equal to its directory**, description ≤ 1024, file ≤ 10 MiB, later sources shadow earlier); a directory with a `pyproject.toml` is installed **editable** into the runtime venv and its import name handed to every kernel at boot; catalog section, bodies left on disk for G9. Three things the row needed that did not exist: the `boot` frame gained `skills` (**both protocol twins**, and the mirror test is what caught the second one missing), the guest's `install_skills` — written in P3-08 with **no producer** — is now called from the bootstrap so skill names are `_protected` like the rest of the harness surface, and the venv builder decides editability in one place (the staleness marker digests the *specs*, not their contents, so a non-editable local skill would keep serving its build-time bytes after every edit). **Two lists, deliberately**: `skills` is what to install, `skill_modules` is what to import — `acme-websearch` imports as `acme_websearch`, so one list could not answer both | P3-07 | — | a skill's `run()` is callable in a cell |
| P3-19 | **Landed.** `CodeCellWidget` — a *subclass* of `ToolCardWidget` selected by the card kind `terminal`, so a tool declaring that kind gets the rendering without the view knowing which tool it was: the program above the output, then the `IpythonToolDetails` facts line (`3 governed calls · 1 attachment · output truncated`), then the per-dispatch rows the base card already drew. Two records that had no consumer now have one: `IpythonToolDetails` (written at P3-09) reaches the card because `_present_result` threads `view.meta` into a new `ToolCard.details`, and the **program** reaches it through a new `ToolCallView.body` — the call view's full input, symmetric with `ToolResultView.body` — rather than the widget parsing `arguments`, which is the raw JSON the model emitted and may not parse at all. **Subagent panel**: `subagent/status` and `subagent/usage-attributed` moved out of the adapter's `RECORDLESS` set — which named *this* row as the reason they were classified rather than rendered — and fold into `TuiState.subagents`, drawn as a `children` section of the sidebar that hides when the family is empty. They still produce **no transcript rows**: a fan-out of eight ticking through `queued → running → done` would push the conversation off screen. A test pins the panel's fold equal to `subagent_roster`'s, because two projections of one fold that disagree is what A11 forbids. Fixed in passing: the dispatch collapsible said "1 governed calls" | P2-01, P3-09 | — | renders from durable events alone |
| P3-20 | **Landed.** `ph --profile rlm` = `tui` + the bundle (the interactive posture, because a person is present for the approvals Code Mode's dispatches raise). The wiring needed a mechanism: **ph-app must not depend on ph-rlm** — the rule that lets the app read `subagent/*` without importing the row that emits them — so a bundle is now *discovered* the way a plugin is, through a new `ph.bundles` entry-point group (`ph.bundles.resolve_bundle` / `installed_bundles`). `available_profiles()` lists only what this install can compose, so a profile whose distribution is missing is never offered and then failed; naming one that is missing says which package to install. The smoke run mounts `resolve_profile("rlm")` itself — not the bundle path — so what boots in the test is what a person's `--profile rlm` composes, `tui` layer included | P3-09 … P3-18 | — | profile boots; smoke run |
| P3-21 | **Governance gate (a)–(e)** as a named test module, mounting `ph-base` + the shipped `rlm` bundle through the loader rather than hand-picked rows. **(b) and (c) test the per-dispatch boundary, not the Phase 4 consumers** (`ToolCallLimit`, the spill policy) — stand-in listeners assert the boundary those rows will attach to. Found and fixed: `CodeRunFailure` did not declare `denies`, so a policy-refused cell reported `kind: "failed"`; and `cancel_grace` was the one grace a row could not tune. Also fixed (the cleanup pass): `FsDenied` is now a `HarnessError` too, so an `fs/write-intent` veto reports `denied` — `SandboxError` already did, and `FsDenied` was caught nowhere; and `denies` became `failure_kind: FailureKind`, because a boolean collapsed `CodeRunFailure`'s three kinds into two and reported an abort as a failure. (c) moved from the log-only `tools/code-dispatch-log` to `tools/post-execute`, which is where the Phase 4 spill row attaches and the only seam that changes what the *program* receives | P3-10, P3-12 | C1–C4, C7 | all five pass — if not, the fold did not land |
| P3-22 | **Landed** as `test_conformance.py`, and the shape is the point: the behaviour was already covered by `test_kernel`/`test_codec`/`test_snapshot`/`test_journal`/`test_lifecycle`, so what this row owes is **completeness** — a claim those cannot make. It reads the protocol's own `FRAME_FIELDS` and the *mounted* profile's namespaces and fails when one has no coverage, so a frame or namespace added without a test breaks at the only moment anyone is looking. Same enumeration for `SNAPSHOT_KINDS`. Two vocabulary gaps are now recorded rather than invisible: `display` is decoded and rendered but has **no producer** (the guest has no `display()`), and `patch`/`recipe` are reserved — `patch` benchmark-gated by D17, `recipe` waiting on a harness-owned load of a *kernel* variable, which P3-17's corpus deliberately is not. New behaviour: the `fault` frame's producer (a protocol mismatch refused at boot, D7) and all three magic prefixes (`%%`, `%`, `!`), not just `%%bash`. **And it found the bug a cross-layer suite exists to find**: the RLM doctrine promised the model "a cell whose first line is `%%bash` is a shell cell" — ported from prime-agent — while pH's own guest raises a `SyntaxError` for it. D19's argument is that owning the runtime is what *closes* that hole; the doctrine was advertising it. Fixed, and the suite now asserts the two layers describe one runtime. Windows stays unimplemented, not merely untested (`pass_fds` is POSIX-only; the Job Object work has no code) | P3-05, P3-06, P3-15 | D1–D5, F3–F5, F7 | full suite green on Linux; macOS untried; Windows unimplemented |
| P3-23 | **Landed** — report at `docs/dev-notes/prime-agent-replay.md`, re-derived by `test_fixture_replay.py` wherever the vendored checkout exists (it skips without it, since `sources/` is not part of this repo). Not a re-run — no model, no key, and a recorded trajectory is not deterministic anyway — but a *structural* comparison: reduce each session to what the port could change, and account for every difference. **The framing finding: both fixtures are prime-agent's *coding* agent** (454 and 391 native `bash`/`edit`/`read`/`write` calls, zero `ipython`, zero spawns), so the replay tests surface translation across exactly the surface C1–C3 replaced. All four tools exist in the shipped profile, asserted against the mounted registry. Four diffs, held not as a list of names — the first draft compared literals against literals in one function and could not fail — but as a **coverage table over the fixtures' own record types and roles**, so an unmapped one fails: `one-cell-many-dispatches` (454 top-level calls become one cell + N dispatch pairs — finer records, not lost ones), `sdk-block`, `access-default` (**expected but not observable** — neither fixture delegates, asserted so a future one that does fails rather than going stale), and `no-bash-execution-record`. That last is a real capability gap rather than a porting error: prime-agent logs a *user's* interactive shell into the session and pH has no counterpart, so a person running `ls` today does it outside the session and the log does not know | P3-20 | — | report checked in; unexpected diffs triaged |
| P3-24 | **Landed** (`ph_app/tui/trajectory.py`). Original scope: **Trajectory projection** (`tui/trajectory.py`): dsh's closed record set `system \| user \| context \| compacted \| message \| tool \| subtool`; 1-based `#N` + `sourceSeq` identity; **source attribution** from `PluginSource(plugin=, form=, sections=)`; records for the six types the conversation view does not render (`request/header`, `step/start`, `step/end`, `approval/policy`, `fs/observed`, `session/end-seed`); prompt-and-tool-catalog snapshot **plus the snapshot it replaced**; call-time tool schema; TTFT and decode throughput derived from `step/*` + `usage` | P2-01, P3-11, P3-19 | A11 | every type in `KNOWN_SESSION_EVENT_TYPES` either produces a record or is explicitly classified record-less — no silent omissions; the projection equals its fold (live and replayed agree, as P2-01). **Both gates are enumerated from the vocabulary**: `set(HANDLERS) \| RECORDLESS` against `KNOWN_SESSION_EVENT_TYPES`, the shape `adapter.py` already used. The first version drove each type through the projection and asked whether a record appeared, which a catch-all `else` made a tautology — a new type could never fail it. There is no `else` now, and a test asserts the gate rejects an invented type. Two additions the port needed: an **`event` kind** beyond dsh's seven, because dsh's vocabulary describes a *conversation* and pH's log also carries harness facts (a policy change, a file read, the seed boundary) — folding those into `context` would claim they were shown to the model; and a **differently-shaped `RECORDLESS`** — not smaller, as a first draft claimed: both hold seven, and four of this one's are types the transcript renders. The difference is the point: `request/header`, `approval/policy`, `fs/observed` and `session/end-seed` are record-less for the *reader* and are exactly what the auditor wants, while chunks and the dispatch's opening half are conversation rows and audit duplicates. A test pins the difference in both directions. Timings are derived from `step/*` and `assistant/chunk` event **times** — never a clock read at render, or a replayed log would not reproduce them (A11) — and report `None` rather than guessing. The fold gate found a real asymmetry worth keeping: a seeded session appends `session/end-seed`, so a resumed log has one event the live one never had, and the equality is over the shared prefix |
| P3-25 | **Landed** (`ph_app/tui/trajectory_app.py`, `tui/widgets/trajectory.py`), with one shape decision worth stating: the view is **its own `App`, not an entry in `App.MODES`**. The plan's own argument for shape (b) is that it must run with *nothing mounted*; sharing `PHTuiApp` would mean sharing its mount, its trust prompt and its agent, so the co-equal-views split lands at the process boundary instead — which is what makes the harness-free entry point trivial rather than conditional. `ph --mode trajectory --session <id|path>` reads a log and mounts nothing. Fork is offered only on marked rows and *reports* rather than failing silently in both refusal cases (off a boundary, and no store because the view is reading a file — advertising a key that cannot work is the failure the plan names). The table holds focus with `/` for the filter, because a filter input focused by default swallowed the single-key actions. Found and fixed while driving the real CLI: a missing path was reinterpreted as a session id and reported as `/nope.jsonl.jsonl` — a file nobody named. Original scope: **`TrajectoryView` as a second app view** (§5.4/§5.5 shape (b)): `App.MODES` chat/trajectory, each with its own screen stack; virtualised table + timeline + toolbar; details panel; **incremental search index** over record text *and* sources; cross-navigation by `sourceSeq`; **fork at a record** via `ctx.sessions.fork`; harness-free entry point `ph --mode trajectory --session <id\|path>` (reads `read_session()`, mounts nothing, no agent, no provider, no answerers) | P3-24, P3-15 | A6 | opens a stored log with nothing mounted; search finds a tool call by name and by argument; fork offered only at a closed-turn boundary record (A6) and refused elsewhere; the fork's prefix is byte-identical to the source; pilot tests drive the table, the details panel and the fork |

### Phase 4 — Stabilization bundle *(4–5 weeks; parallel with Phase 3)*

| ID | Work item | Depends | Delivers | Gate |
|---|---|---|---|---|
| P4-01 | **Landed** (`ph_stabilize/todo.py`, `ph_stabilize/bundle.yaml`). First row of the `stabilize` bundle, which becomes a real bundle here: `ph.bundles` entry point, `bundle.yaml`, its own tests directory. **The prompt text and the error are verbatim, fetched and checked rather than reconstructed** — `WRITE_TODOS_SYSTEM_PROMPT`, `WRITE_TODOS_TOOL_DESCRIPTION` and the parallel-call sentence come from `langchain.agents.middleware.todo` at 1.3.18, which satisfies the pinned `>=1.3.17` the plan asks be re-verified. Copied, not imported: pH does not depend on langchain, and prompt text is the thing most likely to be silently reworded by an upgrade. Three things the pinned release settled that the plan had from a secondary source: upstream's `Todo` is `{content, status}` with **no `activeForm`** (deepagents' own test carries that field; the middleware's schema does not, and pH renders a glyph beside the content in both the sidebar and the prompt, so a second phrasing would be a field with no reader); the tool's result text is still `f"Updated todo list to {todos}"` (the evals docstring describes a *proposed* neutralization to "Todo list updated." that the pinned release does not carry); and the error is assembled from upstream's own three fragments so a diff against `todo.py` stays readable. **The parallel rule is read from the log, not from the batch**, and that is the one real adaptation: upstream's rule is about an assistant *message* while pH's `tools/pre-execute` waterfall is per call. The assistant message is committed before any of its tool calls executes, so counting `write_todos` in it gives every sibling the same answer before the first body runs — which is what makes "every one of them fails" true rather than "all but the first", and why upstream's own test asserts that **no todos are written**. A rule that let the first through would leave the session holding a list written by a call the model was told had failed. **`todo/write` stops being a forward reference**: the type was declared in `FORWARD_REFERENCES` in Phase 2 because the sidebar was ready before a producer existed, and two gates subtracted it rather than ignoring the difference; this row lands the type and its producer together, and the set is deleted with its last member — an empty subtraction made the two vocabulary gates part-vacuous, and the next type rendered ahead of the vocabulary should fail the strict equality loudly and be argued for, which is the non-silence the mechanism existed to buy. Required rather than ignorable, because the list reaches the model through a prompt *context*, so a reader that skipped the event would assemble a different prompt than the session had. **The list rides `context()`, never `section()`** (A12): the advice is static and caches, the list changes on the turn a long task makes most often, and a test asserts the rendered prefix is byte-identical across a `write_todos` call while the context is not. dsh's own `tool-todo` is named `todo_write` and carries an `allowParallelInProgress` knob with uniqueness and single-active validation; pH follows Deep Agents' `write_todos` naming, which is what this row's gate names, and does not port the knob — a config nothing sets is a policy nobody chose. Post-review, three corrections: the row ships **`disabled: true` in its own bundle** (the `rlm-context-loader` idiom; `rlm-stable` flips it at P4-15), because "opt-in" written in a comment while the bundle mounted the row unconditionally was a policy/YAML mismatch — layering `stabilize` for offload or compaction must not hand the model a tool, and a test now mounts the profile both ways; the log reads ride **`Session.latest` + `derive_event_message` + `thaw_json`** instead of hand-rolled reverse scans and payload unwrapping (the incremental `latest()` fold exists for exactly this per-step shape — the hand scan cost ~113 µs per prompt assembly at 10k events, measured); and the parallel rule is **scoped in writing to the native transport**: a Code Mode cell orders its own calls (the tool declares no concurrency safety, so dispatches serialize), so the no-defined-precedence ambiguity the rule guards against cannot arise there — not firing is correct, not a gap, and the docstring now says so rather than claiming "per model invocation". Also found by the review: `test_every_row_in_the_profile_activates` (here and in the ph-rlm original it was copied from) called `Loader.inactive()` **without mounting**, and `inactive()` reads the forks `mount()` creates — a gate answering over an empty table; this bundle's copy now mounts for real, and the rlm original is noted for its own fix. Original scope: `tool-todo` + `todo/write` + prompt section (opt-in row) | P1-22 | G1 | parallel-call error; reminder text |
| P4-02 | **Landed in full** (`ph_stabilize/offload.py`, `ph_stabilize/input_offload.py`). `TOO_LARGE_TOOL_MSG`, the preview shape and `TOOLS_EXCLUDED_FROM_EVICTION` are verbatim from `deepagents/middleware/_message_eviction.py` and `filesystem.py`, which *are* vendored under `sources/` (unlike P4-01's langchain constants). The threshold comparison is upstream's own `>`, which is exactly the gate: 80 000 characters stays inline, 80 001 does not — asserted as a **pair**, because a threshold tested only from the far side passes for any limit at or below the value tested. Both thresholds are derived in the test from the two constants rather than written as a literal, so the gate moves with the policy. Ported deliberately narrowed: upstream's line-numberer chunks lines past 5 000 characters with `5.1`-style continuation markers, and that branch is **unreachable** here because every caller clips to 1 000 first — unreachable machinery is a claim nobody can check, so it is not ported and the docstring says why. `excluded_tools` carries upstream's six names *and* pH's own (`read`/`edit`/`write` beside `read_file`/`edit_file`/`write_file`), so a deployment running either name gets the same treatment and a reader diffing against `filesystem.py` can see nothing was dropped. `Config` merges the two prior arts the plan names — Deep Agents' estimated-token limit and dsh's `spill-policy` byte limit — with the byte knob off by default. `offload/spilled` is **ignorable** (the `tool/result` event already carries what the model saw, so a reader that skips this loses the forwarding address, not the conversation) and both projections classify it, the transcript as a notice naming the path. Found by mutation testing: **fail-open had no gate** — turning the `except` into a `raise` passed every other test in the file, so a test now patches the spill store to refuse and asserts the model keeps the original; and C5's "individually, per result" was untested until a two-call batch asserted the oversized sibling is replaced while the small one is not. Also found: a test asserting against `str(event.data)` cannot match a multi-line original, because the payload's repr escapes newlines — the tests dig the model-visible text out of the block structure instead. **`input-offload` is a surface `replace`, which is the pH-shaped answer and was reached by rejecting two others.** Upstream keeps the oversized human message in `state` and substitutes in the *projection*; pH has no state/projection split — it has **log and surface**, and the surface already does exactly this for compaction. So the original `user/message` is logged as the person sent it, and a second `user/message` carrying the preview is appended with `surface_op: replace` citing it: `derive_messages()` yields the preview, `transcript()` yields the original in full. Verified empirically before building, because the two rejected shapes were rejected on the strength of it — **(a)** rewriting the message before it is logged, which keeps a 2 MB paste out of the JSONL every resume re-reads but makes the log attribute harness-authored text to the human (a false statement in an append-only record, not merely a lossy one), and **(b)** rewriting the outgoing request only, which breaks I3. The objection that killed the chosen shape's first assessment — "the substitution has to be stated twice" — turned out to be wrong: `_build_request` calls `derive_messages()` *after* the `agent/request` waterfall, so a listener appends the replacement and returns the config untouched, and the loop's own re-derivation applies it. One statement, no copy to keep in step. The replacement is attributed `PluginSource(plugin="input-offload", form="notice")` rather than `user`, because marking it `user` would be the same false record one layer down. `offload/input-spilled` is ignorable for the same reason as its sibling. Deviations: the spill is named by the seq it replaces rather than upstream's uuid (the store already dedupes by content digest, so a random name buys nothing a traceable one does not), and the notice summary is clipped to `CONTEXT_SUMMARY_MAX_CHARS`, which a full spill path exceeds. **Three of five mutations survived the first test pass**, all of them test defects: fail-open could not tell success from the listener taking the whole request down, because `agent.prompt` *reports* a failed turn rather than raising — the test now asserts an `assistant/message` exists; and idempotence was untestable end-to-end, because after an offload the next append is the *next* human message, so `latest("user/message")` is that one and the check is never consulted (it is consulted on a second **step** of one turn), so the predicate is now put in that state directly, at a lowered threshold where the preview is itself oversized — the only arrangement in which the check does any work. Post-review, five changes worth naming. **A tool now declares that it self-limits** (`ToolDefinition.self_limits`, beside `timeout_ms`) instead of this row matching a list of upstream's tool names: five of the ten names matched nothing pH registers, ph-rlm's paging `context_*` tools were not on it, and the exclusion test registered a stub called `read` so it proved name-matching worked without touching whether the list matched reality. The row already injected `tools` and never used it. `excluded_tools` survives as the escape hatch, empty by default. **The guard rail measures the projection that will be sent**, not the body's own output — abstaining whenever another `tools/post-execute` row had rewritten the content would have let any future redactor or formatter switch offloading off by touching the result at all. **Fail-open moved into the seam** as `SpillStore.try_save_text`, because three production spill writers were each remembering the rule and had already drifted on which exception counts (`ph_rlm/snapshot.py` still catches `OSError` — noted, not changed). **The replacement is built with `create_user_message` + `PluginSource`**, so the 120-char `summary` cap is enforced by the validator at append rather than by a hand-slice whose failure would surface inside `derive_messages()`. And the ordering `input-offload` leans on — `derive_messages()` running *after* the `agent/request` waterfall — **is now pinned by a ph-core test**; nothing in that seam's contract said so. Found while reviewing, in code the row only reached: the TUI adapter keyed the compaction row on `is_replacement_surface_event` alone, so an offloaded paste rendered as "(history compacted)" — a surface `replace` is a mechanism, not a cause, and the log's own attribution is the discriminator. Original scope: `tool-result-offload` + `input-offload`: thresholds, previews, excluded tools, `conversation_history/` | P1-21 | G2, G3, C5 | 80 001 vs 80 000; excluded untouched |
| P4-03 | **Landed in full** (`ph/seams/compaction.py`, `ph_stabilize/compaction.py`, `ph_stabilize/compact_command.py`). **The seam is in `ph-core`, and it had to be** — the row's own third deliverable settled it. G10's variable listing is contributed by `ph-rlm`, which depends on `ph-core` and must never depend on `ph-stabilize`, so the type a contributor imports (`CompactionNote`) and the service it registers on cannot live in the bundle that ships the engine. That is dsh's own split — `dsh-compaction` defines `CompactionEngine`, `compaction-basic` implements it — and `ph-base`'s existing shape for `code_runtime` and `subagents`: a seam with no provider, so a profile that layers nothing simply never compacts. **The numbers are upstream's, fetched not remembered**: `0.85`/`0.10` and `170 000`/`6 messages` are `compute_summarization_defaults` verbatim (both branches, because pH has the same split — `request/context` carries a `context_window` only when an adapter resolved one, and the window-unknown branch is therefore the *opening state of every session*, not a rare fallback: `request/context` is appended inside `_build_request`, which runs after `agent/pre-step`). `DEFAULT_SUMMARY_PROMPT` is copied from `langchain` 1.3.18 — the release P4-01 pinned — and its `<messages>` marker is documented upstream as part of that constant's public contract, so **G10's notes are spliced in at exactly the seam deepagents uses for its media-reference block** rather than at a new one. Both thresholds are asserted as **pairs**, for P4-02's reason. **The cut moves backward, and that is a correction to the upstream comment**: `_find_safe_cutoff`'s docstring still says it advances past tool messages ('summarizing more') while the code searches *back* for the `AIMessage` that owns them — advancing would summarize a call and hand the model an orphaned `tool-result`, which several providers reject outright. Balance is folded over the surface in **current order** (dsh's `tool-pairing`), never over step markers, because a landed replacement has already moved what 'position' means; no balanced cut means **no compaction**, which dsh states as a contract — a single oversized retained unit cannot be repaired by replacing a surface. **The plan is indexed by surface position, not by index into `derive_messages()`**: an `assistant/message` logged only to host a max-tokens step's usage is a surface node that projects to no message, and counting messages would cite a range one short of what it shadows — an append `Session.append` refuses outright. The summarizer reads a **bounded tail** (langchain's 4 000 tokens, `strategy="last"`), because a summarize call that itself overflowed is a remedy that fails exactly when it is needed; one deviation, argued in the module: when the tail is trimmed the rendered text says so, since a model asked for SESSION INTENT from the middle of a conversation and not told that is what it is reading will state the middle as the intent. Two events, both ignorable and **adjacent by construction** (no `await` between the two appends), which is dsh's shadow-price protocol: `compaction/summarized` carries the accounting and the provenance while the required `user/message` beside it carries what the model reads. Fail-open keeps upstream's *without-path* wording, and that half is load-bearing rather than cosmetic — a spill that failed must not leave the model reading a path that holds nothing. `compact_now` **raises** where `compact_if_needed` **declines**: 'your conversation is short' and 'your summarizer is broken' are different answers, and a decline is recorded and then holds until the surface moves, without which the overflow path re-attempts inside the same `while True` step loop at one model call per attempt. **Argument truncation is the row's model-free half** (§7.4 item 2, upstream's `truncate_args_settings`): over-long string arguments to the file-writing tools are elided from *retained* history as an `assistant/message` surface `replace`, so the log keeps the model's exact bytes and only the derivation shortens. It runs before the summary on every pressure check, because eliding a 40 KB file body the model itself wrote costs no model call and often takes enough off that the summary is not needed this step. Two pH-specific findings. Upstream matches `{write_file, edit_file}`, which **matches nothing pH registers** — its fs tools are `read`/`write`/`edit`/`glob`/`grep` — so the shipped list carries both sets, the `excluded_tools` idiom from P4-02; the restriction itself is kept, and is doing real work, because a long `run_code` cell in old history is exactly the argument a model re-reads. And **the replacement must drop `usage`**: `TokenMeter.last_usage` scans *backward* for the newest `assistant/message` carrying one and a replacement is appended at the end of the log, so copying it would make an old turn's number the meter's baseline and tell the trigger the session had shrunk — the TUI footer reads the same field and would have shown the same stale number. `ToolCallBlock.arguments` is the model's raw JSON string, so this is the one place the port knowingly re-serializes; admissible only because it is a *replacement* and the exact bytes are one seq away. **The overflow clip is ported** (§7.4 item 7, `_clip_overflow_tail`): before summarizing on overflow, an over-budget trailing `tool/result` batch is spilled and replaced through a `tool/result` surface `replace` — which the surface validator already constrains to changing content alone, the invariant that makes rewriting history the model has read safe at all. It earns its place by covering the case summarization *cannot*: a trailing batch bigger than the retention budget leaves no balanced cut, so `safe_cutoff` correctly declines and nothing else would ever shrink it; the row therefore retries when **either** pass moved the surface, which is dsh's own rule. One deviation: upstream has two paths (head-slice a `read_file` result and point at its `file_path`, offload everything else) and pH has one, because the spill store is content-addressed — re-spilling the same text costs one file, not a copy per occurrence — and one shape of pointer is one thing for the model to learn. A member of the batch whose pointer would be *longer* than its content is left alone: the batch is what must shrink. **`/compact [instructions]` takes what dsh and deepagents both refuse.** dsh's `command-compact` rejects arguments and `compact_conversation` takes none, but the moment someone compacts on purpose is usually the moment they are changing subject, and what they are about to work on is the one thing the summarizer cannot read off the conversation. It is spliced in at the same `<messages>` seam as the state notes, last and labelled as the person's so it is read as an instruction rather than as a message to compress, and recorded on `compaction/summarized` — a summary weighted towards one thing should say who asked. **Three things still not ported, and one correction to why.** dsh's durable `compaction/start`/`end` lock: the first draft of this row said it excludes *other processes*, which is wrong — dsh's own `busy` text reads "this process has an active compaction". What the pair actually buys is a consequence of *ordering*: dsh appends `start` **before** summarization, so a crash mid-summary leaves a marker needing an `end` and a reconciliation. pH writes nothing until the summary is in hand, so there is no in-flight state to reconcile — and, because the metering event and its replacement are appended with no `await` between them while the JSONL provider only drains on an awaited `session/flush`, no flush can interleave: the pair is atomic *with respect to durability*, not merely ordered. The other benefit dsh names, "failed attempts remain visible in the log", is `compaction/declined` — but only after a later correction: as first landed, the manual path recorded **nothing** while telling the person "the attempt is recorded in the session log", and an unexpected exception escaped `compact_if_needed` (whose docstring said "never raises") into `agent/pre-step`, where the driver contains it with one debug line — a stabilization row able to end a turn by having a bug. Both entry points now record through one site, the guard is `Exception` with cancellation re-raised (`Cancelled` and `AgentCancelled` both derive from it), and `busy` stays unrecorded because nothing was attempted. One durability gap is left open and named: the history blob is written *before* the first append, so a crash in between leaks a file, and `SpillStore.sweep` is only ever called for `kernel/<namespace>` owners — F7 never grew to cover the compaction and offload owners (P6-15). What pH gives up is dsh's second use, delimiting context injected *while* the summary ran; nothing reads that today. `compaction-tool-result-pruner` is **unscheduled, not rejected**: no row anywhere in this plan covers it while the port plan says "keep both", and structurally it would be a second engine on a seam that holds one. Upstream's media offloading is inapplicable by construction rather than by policy: it exists to relocate inline base64 *data URLs*, and pH's `ImageBlock.attachment` is already a durable reference — and the absence is repo-wide rather than this row's: nothing constructs an `ImageBlock`, and neither adapter serializes one — `_to_anthropic` has branches for text, reasoning, tool calls and tool results and silently omits anything else — so an image reaches no provider from any path. Compaction's own renderer would need a branch when a pipeline lands: an `ImageBlock` falls to `_block_text`'s catch-all and becomes the string `[image]`, so the *history file* — which the replacement tells the model holds the full conversation — would not carry the `attachmentId` that is the only handle on the bytes. The log keeps the block either way. **The summarize request replays the conversation's own envelope** (§7.4 item 5): same `system`, same `tools`, the shadowed messages as themselves, and the extraction prompt appended as the last *user* message — placement being the whole mechanism, since anything moved into `system` changes the prefix and forfeits the cache. Because the shadowed range is a prefix of the surface, the request is a strict prefix of the one the loop just made, and a test asserts that structurally rather than hoping for it (a cache miss looks exactly like a hit, only on the invoice — `test_prefix_stability`'s own argument). Choosing that envelope **forces a second decision**, and the two are not separable: keeping the prefix means sending the tool schemas, and a model holding the session's tools under the session's system prompt may answer a summarize request with a tool call and no prose — which was an empty summary and a decline. So a reply with no text is retried once in the self-contained shape, without tools and with the **whole** range rather than a tail: cheap first, correct second, and nothing hidden by falling back. Two departures from byte-identical, both outside the token prefix so both free: `max_tokens` is the summary cap, and `reasoning_effort` is dropped so a reasoning model does not spend a thinking budget on an extraction. `SUMMARY_INSTRUCTION` is *derived* from the verbatim constant by splitting on the `<messages>` marker rather than retyped, so the text stays byte-identical to the pinned release and moves with it. **Overflow never replays** — the envelope in question is the request the provider just refused for being too large — and keeps the trimmed, inlined form, which is also why the 4 000-token trim survives at all; it is the one path where a *suffix* of a balanced range can begin with an orphaned tool result, which rendering to text sidesteps and sending as messages would not. The event records which `shape` paid for each summary, beside the usage that says whether the cache was hit. **A finding that bounds the payoff**: pH sets no `cache_control` anywhere — `anthropic.py`'s `_body` builds `system`, `tools` and `messages` with no markers — so on that adapter prefix caching is off entirely and this shape buys nothing today; DeepSeek and OpenAI-compatible providers auto-cache on prefix match and get the benefit immediately. P6-13 closes that. Still open from §7.4: the history is one content-addressed file per compaction rather than one appended file per session (the spill store writes whole files), the model-facing `compact_conversation` tool, and the `compaction/before` veto waterfall; agentic summarization is P6-14. **The row found two live mis-discriminations, both of the same shape.** `ContextForm` gains `compaction` because P4-02's TUI fix keyed the compaction row on `kind != "plugin"` — but a compaction summary *is* plugin-authored (dsh's own checkpoint source is `{kind: 'plugin', plugin: 'compact'}`), so the first real summary would have rendered as an ordinary context row; the discriminator is now the log's own `form`, which is a claim about the surface rather than a colour. And `ph_rlm/harness/auto.py` triggered an auto-refinement on *any* surface replacement and labelled it `compaction`, so an offloaded paste (P4-02) already fired one — now keyed on `compaction/summarized`. Found in the tests as well: `test_compaction_marks_what_it_replaced_and_keeps_it` built its replacement with `user_payload`, i.e. a person's own words shadowing a range, which no producer writes; `ph.testing.plugin_payload` now spells the second shape a `user/message` takes so the next fixture cannot get it wrong. One test-design note: the manual-path tests need a conversation long enough that the *shipped* 10% retention actually cuts — a four-line session is correctly a no-op, and the first draft asserted against one. Original scope: `compaction-summarize` as `CompactionEngine` + `command-compact`; RLM-aware variable listing | P1-13, P1-10 | A3, G4, G10 | 0.85 / 170k triggers; never splits a pair; log intact; variables survive |
| P4-04 | **Landed** (`ph_stabilize/limits.py`) except the child caps, which need `ctx.subagents` and are not in this row's gate. `ModelCallLimitMiddleware` and `ToolCallLimitMiddleware` ported onto `agent/pre-step` and `tools/pre-execute`, with the denial text verbatim from langchain 1.3.18. **The counts are a fold over the log, not a counter**: a limit in a field is one a resume forgets, and the log already answers it — `step/start` per model call, `tool/call` per tool call, `turn/start` as the reset — through `SessionFoldCache`, which folds only the slice appended since the last read because this runs on every step and every call. A test reopens the log in a second `Session` and asserts the same counts. **Two vocabularies mapped once**: upstream's *thread* is pH's **session** and its *run* is pH's **turn**, renamed here rather than in each message so a diff against `model_call_limit.py` stays readable. **`>` where upstream has `>=`**, and the reason is pH's own ordering: `tool/call` is appended *before* the pipeline runs (B4), so the fold already counts the call being judged — the two comparisons are the same statement about the same call. **`end` closes the turn at a step boundary rather than reaching into the batch**: upstream jumps to the graph's end and synthesizes results for the calls it skipped, but pH's batch is already dispatched, so the breaching call is denied, its siblings get upstream's own "Execution stopped before this tool call could run…" wording (they did nothing wrong and must not read the refusal as being about them), and the next `agent/pre-step` rejects — scoped to the breaching turn by comparing seqs, without which one `end` would reject every step for the rest of the session and the agent would never speak again. One deliberate deviation: upstream injects an artificial `AIMessage` so a reader sees the agent announce its own limit; pH records `limits/exceeded` and renders a notice, because inventing model speech is what this codebase refuses everywhere else. **Every ceiling is unset by default** — a limit nobody chose is one that fires on someone's longest legitimate turn — while the breaker ships on at 5, because five identical failures in a row is not a long task but a stuck one. **The budget is a live footer reading, not only a notice.** `ctx.tui_status` (a new `ph-core` seam, `tui_screens`' sibling) lets a row contribute one short reading beside the model name and the context gauge; `limits` registers the *tightest* active budget and turns it amber at the gauge's own 0.85, so two readings on one line mean the same thing by the time a person has learned one. This is the answer to what upstream's injected `AIMessage` was reaching for: it announces a limit on the step it stops you, which is the one moment the information can no longer change anything — the notice stays as the *record of why a turn ended*, and the reading is what lets someone plan around it. The seam carries a semantic `level`, never a colour (the `ContextForm`/`CardKind` rule), which is what lets ph-core own it without knowing what a terminal is, and a field that raises is dropped rather than taking the footer down — it is read on every spinner frame. **`exit: end` ends the turn through the loop's own flag**, not a latch: `Deny` gains `concludes_turn`, the same field a *successful* result carries (`ToolRunContext.conclude_turn`), reached from the denial side — `commit_ready` already reads it on every committed result, a denial from `prepare()` included. The first shape appended `limits/exceeded` and re-read it at the next `agent/pre-step`, scoping it to a turn by comparing seqs; that made an **ignorable** event into control state, so a build that skipped it — as the vocabulary explicitly permits — would not have ended the turn. The sibling/breacher distinction is now arithmetic (`used == limit + 1` is the call that crossed) rather than a second read of the same latch, and the whole class of "when does the latch expire" question disappears. Two findings while testing: `SessionFoldCache` hands `extend` the **session**, not its events (caught by an unrelated todo test going red, which is the cross-row coverage working), and a non-zero shell exit is **not** a tool error — `is_error` is about the tool raising, so the breaker's fixture reads a missing path instead. Original scope: `limits`: `ModelCallLimit`, `ToolCallLimit`, breaker, child caps | P1-02 | G5 | end/continue/error; breaker trips |
| P4-05 | **Landed** (`ph_stabilize/hitl.py`, plus `edit`/`respond` on the approval seam and in the modal). The row is a `tools/pre-execute` listener that returns `Ask`; everything after that already existed and is unchanged — `approval/asked` then `approval/decided`, fail-closed on a missing answerer, re-ask on resume because an `asked` with no `decided` *is* the pending state. **Two of the four answers carry data**, so `ApprovalOutcome` stays the four bare strings (the fail-closed reading is unchanged and every existing answerer keeps working) and `ApprovalAnswer` widens it with `Edited{arguments}` and `Responded{message}`. The two are translated into the *pipeline's* own vocabulary on the way back out of the approval seam — `Edited` into `Allow(arguments=…)`, `Responded` into a new `Respond` member of `PreToolDecision` — so `prepare()`'s switch stays closed over its own union and neither capability is reachable only through approval: a cache row, a replay row and a "disabled here, do this instead" row all want to short-circuit a call with an answer, and the same reasoning `Deny.concludes_turn` is written down with. The substitution is applied by `prepare()` rather than by assigning to `execution.arguments` from inside the seam call, which was the only write to that field in the repo and reached around every `tools/pre-execute` listener that had already been handed the old value. `respond` returns a **successful** result carrying the human's text and never dispatches the body — a denial the model would have to interpret is the wrong shape for "you don't need to run that, here is the answer". **Both versions of an edited call are in the log, and that is the design**: `tool/call` is appended before the pipeline runs (B4) and already records what the *model* asked for, so the substitution lands on `approval/decided` — rewriting the call's own record would have attributed the human's arguments to the model. The classifier is a pattern set matched against the call's **arguments**, not the tool name, because a cell is not a tool: one `ipython` call may delete a tree or force-push, and gating the transport would prompt on every cell or on none. Its verdict rides the ask's `reason`, so a person is told what the harness is worried about and an auditor can tune it. `manual | auto | yolo` is folded from the log rather than held in a field, so a resume keeps the posture — from **`approval/mode`, the row's own event**, after a first draft rode an optional `mode` on the seam's `approval/policy`. Two things were wrong with that and only one was a layering complaint: a ph-core seam whose parameter docstring named its ph-stabilize provider, *and* a last-write-wins fold with two writers, since `permission-presets` also calls `set_policy` and knows nothing about a posture — so switching preset silently reverted it. `known_event_types.py` already hosts types produced by other packages (`todo/write`, `offload/spilled`, `kernel/snapshot`) with a per-type comment recording the deal, which is the established route. **`interrupt_on` is empty by default** — a harness that asks about everything on first run teaches its user to approve without reading, which is worse than not asking; `rlm-stable` names the gated set at P4-15. Two findings: `ApprovalRequest.allowed_decisions` had to be a `list`, not the `tuple` the config uses, because `freeze_json_value` refuses tuples outright (A1) and a wire model that cannot be appended fails in someone's session rather than in review; and the request carries `arguments` for the modal to prefill but **excludes them from `approval/asked`**, since `tool/call` already holds them and two copies of one fact can disagree — built field by field rather than by subtracting one key from `to_wire()`, so the next field added for the answerer's benefit does not land in the log by default. **A cleanup pass then moved three things and left one named.** `allowed_decisions` was policy enforced only by the modal that hides a button, which made it a *rendering* rule: the seam that owns the fail-closed reading now refuses an answer the asking row withheld, while a refusal is never withheld because that is how this seam says no. The vocabulary became `ApprovalDecisionName`, a `Literal` beside `ApprovalOutcome`, so a typo in a profile fails at config load instead of rendering a modal with no buttons and nothing to say why — the two are the same decision from either side of the prompt (`approve` is the button, `allowed-once` is the verdict). And the classifier read the *frozen* argument tree with `json.dumps(…, default=str)`, which cannot walk a `MappingProxyType`: it was scanning a Python repr that happened to contain the same substrings, so it would have gone on matching while being wrong — `ph.session.json.dumps` exists for exactly that shape. The ask now quotes the *matched text* rather than the regex that matched it, because showing someone the classifier's own source presents an implementation where `rm -rf /tmp/x` presents their call back to them. **Left named, not fixed: `interrupt_on` is keyed by tool name** and that name is a mandatory first filter, which is the pattern this bundle rejects in writing one row over — see P6-16. Original scope: `hitl`: `approve \| edit \| reject \| respond`, `MANUAL \| AUTO \| YOLO`, destructive classifier for `run_code` and mutating bindings | P1-08, P2-04 | G6 | pilot flows; destructive cell prompts in MANUAL |
| P4-06 | **Landed** (`ph_stabilize/permissions_fs.py`, plus a read gate and a listing filter on `ctx.fs`). **The rules hang off the seam, not off the tools**, and that is the row: `fs/write-intent` and `fs/edit-intent` already existed, so the port added the missing third — `fs/read-intent` — and a deny rule now covers `read`, a Code Mode binding, a second editing tool and an MCP server's file writer without this module naming one of them. Keyed on a tool name it would have covered none of them silently, which is the failure `compaction`'s truncation pass and `offload` argue against in their own docstrings and which P6-16 files against `hitl`. That the read gate was *missing* is the row's first finding: the seam's docstring said it bounds tool-mediated access, but a deny rule on a path could be walked around with `read`, so a policy row could only ever have been half a policy. **Enumeration is filtered, not gated.** `glob`/`grep` go through a new `FsService.hide` — a synchronous predicate, deliberately not a waterfall, because a walk visits thousands of candidates and nobody approves nine hundred prompts. It runs *during* the walk rather than over the results, which is the second finding: `grep` reads every file it visits, so post-filtering its matches would have returned no rows while having read every byte of the protected file — the test asserts the contents never appear, not merely that the path is absent. For enumeration `deny` and `interrupt` both conceal, since asking is not available and revealing a path the rules said to check with a human is the leak the rule was written to prevent. A predicate that raises conceals, the approval seam's fail-closed reading. **First-match-wins with a default of `allow`**, and the test pins the *direction* rather than one pair's outcome: the same two rules reversed give the opposite answer, which is what makes order the configuration language instead of an implementation detail — evaluated by specificity or by strictest-wins, a narrow `allow` above a broad `deny` would silently stop meaning \"except this one\". Patterns go through `ph.seams.fs.matches_glob`, which this row **promoted from private**, so `**/*.env` cannot mean two things in one harness; each candidate is offered both spellings (absolute, and relative to the workspace root when it has one) so an operator need not know which form the seam resolved to. `write` covers create, overwrite and edit as one operation, because a rule set that split them is one where \"do not write here\" left `edit` open. `interrupt` routes through `ctx.approval` — the same channel `hitl` uses, so a deployment configures its answerer once — and denies when none is mounted. **Recursive delete fails closed, and pH has no producer for it yet.** The rules describe paths that do not exist, and the tree is enumerated by the OS at delete time, so \"is anything in here protected?\" cannot be answered by matching the directory's own path and checking each child would still race the ones created after the check; `deletion_reason` refuses whenever a non-allow rule *could* match a descendant, judged from the pattern's literal head, with containment on separators so `/w/xyz` is not inside `/w/x` and a leading wildcard refusing everything. pH registers no delete tool and this row does not add one — that is a capability expansion, not a permission — so the rule is written, tested directly, and inherited the day `fs.delete` lands. A third finding, caught by a failing test: the descendant check compared a relative pattern head against an absolute directory, so `build/keep/**` did not protect `/w/build`; it now uses `decide`'s own both-spellings rule. **E9's sentence is live, not computed at mount**: `reach` reads `ctx.sandbox.available` through the held seam, so it toggles when a backend is registered *after* this row — a mount-time boolean would have been the wrong sentence for exactly the profile that layers its backend later. It is warned once at mount when rules exist and no backend does, and carried on `ctx.fs_permissions` because `ph-app` cannot import this package and P4-12's doctor has to be able to ask. **Empty rules attach nothing at all** — not a predicate that returns `False`, since `hide` is consulted per file a walk visits — while the service is still published, because \"no rules\" is an answer doctor wants to give. **A claim this row first made and the cleanup pass disproved.** The first draft of this entry said a read rule now applies to the harness's own readers — `memory-agents-md`, the subagent seam, `ph-rlm`'s harness service. It does not. `tool-fs` is the *only* caller of `FsService.read`/`write`/`edit` in the tree: `agent-instructions` reads with `Path.read_text` at mount time and asks `ctx.fs` only for `.root`, `ph-rlm`'s harness service reads with `Path.read_text`, and `seams/subagents.py` touches no files at all. An MCP server is a remote client and can never reach `ctx.fs`. Code Mode genuinely is covered, because a binding is the namespaced face of a governed tool. So the placement is right for the reason it was chosen — one gate rather than one per tool, and the next writer inherits it for free — but the coverage is mostly *future*, and the readers that bypass the seam are the migration §7.6/§7.7 already asks for ("memory read once per turn via `ctx.fs`"). **The cleanup pass changed five things.** `matches_glob` was `fnmatch`, whose `*` crosses `/` — so `docs/*.md` also matched `docs/private/keys.md`. For a search box that is a quirk; for an ACL evaluated first-match-wins it is a hole with a direction, because the idiom the rules are written in is a narrow `allow` above a broad `deny`, and an `allow` silently wider than written permits what the `deny` under it exists to stop. It is now a separator-aware regex, `lru_cache`d per pattern, which is also what made precompilation free. `delete` was folded into `write` (the port plan's own two-operation vocabulary): as a third operation it was both unreachable *and* a trap, since `{operations: ["write"], paths: ["secrets/**"], deny}` left deleting `secrets/` open, and `deletion_reason` turned an `interrupt` into a flat denial where the live gate would have asked. `_spellings` called `Path.relative_to`, which allocates a `Path` per root segment: **28.3 µs per concealment check at a five-segment root, 92% of it, nearly doubling the wall time of a repository-wide `grep`** — a prefix strip against a root computed once in `__post_init__` gives the same answer, and `conceals` measured 28.3 → 1.34 µs at one rule and 43.1 → 10.2 µs at ten. The `interrupt` path was the third hand-written copy of the fail-closed approval dance and the first to disagree with the other two: it asked with `agent=None`, and since `ApprovalService.request` derives the session from the agent, that would have put a question in front of a human with **nothing written to the log** and walked past the `approval_policy: "never"` short-circuit a deployment set on purpose — it now denies, as `ToolRuntime._service_ask` and the RLM harness gate both do, threads `cancel`, and reports *which* refusal it was through a new `ph.seams.approval.DENIAL_REASONS`, hoisted out of the tools registry so the three sentences have one home (registry's own wording is unchanged, verified by its tests). And the sandbox seam is now looked up through the held `ctx` at question time rather than captured at mount, which was the same staleness bug the `reach` property was written to avoid one field over. Original scope: `permissions-fs`: first-match rows, recursive-delete fail-closed, `ls/glob/grep` post-filter; **reach self-report** when no confining provider is mounted | P1-17 | G7, E9 | first-match tests; the reach message toggles with a sandbox |
| P4-07 | **Landed** (`ph/seams/workspace.py`). Seam + the shared provider only; the worktree backend and the `ctx.fs`/`ctx.subprocess` wiring are P4-08. **`ctx.workspace` is the seam, not the workspace** — the row's one real design finding. `ph-rlm`'s prompt line already asked `ctx.get("workspace")` and duck-typed the *provision* as a `Workspace` (`root`/`kind`/`repo_writable`), with a docstring saying it would "start telling the truth without being edited" when D21 landed. It would not have: `ctx.<name>` is a service everywhere in this codebase, and a workspace is per-agent state a service hands out, so the line would have started describing the seam object the day it was mounted. The seam therefore keeps a live map and answers `of(agent_id)`, the prompt asks it for the agent it is rendering, and `WORKSPACE_LINE` changed from "no tier is mounted" to "none acquired" — the first became false the moment `workspace-shared` went into `ph-base`, while the practical fact for the model (nothing is bounding it) is the same either way. **`acquire` never fails and never returns `None`.** An agent needs a working directory, so "no workspace" is not a state the lifecycle can be in: a provider that cannot serve a request declines (returns `None`) and the seam falls back to `shared`, which is why the shared provider lives beside the seam rather than in a row a profile might not layer — a fallback that has to be composed in is not a fallback. A provider that *raises* falls back too, and the log then says `shared`, because a tier that broke is a tier that is not in force and an operator needs to see that rather than a traceback. **`repo_writable` is the guarantee obtained, never the one requested.** `access="read"` at the `advisory` tier yields `shared` with `repo_writable=True` — §4.8's own table — because nothing here enforces anything and a `False` would be a claim a caller would act on; `worktree-ephemeral` is likewise `True`, since its writes happen and simply reach nobody. Only `readonly-scratch` is `False`, and only a sandbox backend can produce it. **`scratch` is created, not merely named**, per session *and* per agent, and lives at `$PH_HOME/scratch/<session>/<agent>` rather than inside the workspace — the spec says `<agent artifacts>/`, and pH's existing convention for per-owner side files is `$PH_HOME/<kind>/<owner>/` (spill, attachments), so this follows the convention the repo has rather than the one the prose names. Outside `root` is the load-bearing part: a child whose worktree is discarded still leaves behind what it produced. **The events are a pair, and the seam owns both halves** — an `acquired` with no `disposed` is how a leaked workspace is detected at session open (§4.9), and a pair only reconciles if one place writes both; a provider that forgot the second would leave every workspace looking leaked. Disposal wraps the provider's teardown so the live map empties even when that teardown raises, since one failed `git worktree remove` would otherwise have the seam claiming an agent holds a workspace for the life of the process, with the prompt and `ph doctor` both repeating it. `tier` reports the **effective** tier (`advisory` with no provider registered), not the configured one, which is the distinction P4-12 has to print: a `worktree` row over a directory that is not a repository declines on every acquire, and a doctor reading config would report containment nobody has. Mounted in `ph-base` beside `fs`, and the gate holds — the seam is present, no tier is in force, nothing is acquired until P4-08 wires the lifecycle. **A cleanup pass then found the row had built half a lifecycle.** The durable pair is the *crash* half, and `Context.effect`'s own docstring names the other one — "every external artifact an agent takes — a child process, a **worktree**, a temp path, a lock" (I2) — so a workspace lost to an ordinary error path or a disposed agent scope released nothing and would have been reported as leaked by P4-14's reconciler, which exists for `SIGKILL`, the case that runs nothing on any platform. `acquire` now takes a `scope` and registers its teardown through `ctx.effect`, exactly as `jobs.start` and `subagents.admit` do, which also made disposal idempotent for free (the disposer deregisters itself) where the hand-rolled version would have run `git worktree remove` twice and written two `disposed` events. That let `Workspace` become a pure value: `session`, `agent_id` and the `dispose` method came off it and the seam holds them beside the disposer, since a value handed to a caller should not carry the bookkeeping of the service that made it — and the closure's `finally: self._held.pop(agent_id)` was the seventh hand-written copy of the `_registry` rule and had drifted the same way it warns about, evicting a *successor* when a re-acquired agent disposed its first handle. **`kept` was a disposal outcome frozen at acquire time**, so it could never have carried this row's own stated policy ("keep dirty, remove clean, discard ephemeral even if dirty") — a decision made while releasing, and not derivable from `kind`, since a `worktree` is either. The provider's teardown returns it instead. Three smaller things: the `WorkspaceProvider` Protocol was declared, exported, and then used as a type nowhere — `provider: Any` under `mypy --strict` means a provider whose keyword drifted raises inside the seam's own `except`, is logged as a fallback and reports `shared`, which is the containment vanishing silently, so the slot is typed and `tier` is a Protocol member rather than a `getattr` probe defaulting to `"worktree"` (a default that would have called a mis-declared sandbox provider a worktree, in the one property whose job is to not overstate the tier), with `ContainmentTier` named once here because P4-11 selects it and P4-12 prints it; the decline notice fired for a provider that *raised* as well as one that declined, telling an operator the tier said no when it had crashed; and **the seam now owns the scratch layout** rather than each provider re-deriving `scratch_root / session / agent` and its `mkdir` — the module docstring states scratch as a *seam* guarantee, and a per-tier copy is one a tier can get wrong with nothing checking, so the path is created before dispatch and handed to the provider, which may substitute it when its tier needs the directory somewhere reachable. Finally the two `events.declare` calls came out: these are session-log types, not bus events, and nothing dispatches them — every other declared name in the tree is dispatched, `fs/observed` and `approval/asked` are the standing precedent for *not* declaring, and `ph events` renders the registry as the producer/consumer matrix, so a declaration nobody produces is the drift that matrix is generated to catch. Original scope: `ctx.workspace` seam + `workspace-shared` (default; returns cwd; `scratch` at `<session artifacts>/scratch/`) | P0-02 | E5 | mounting the seam changes nothing until a profile opts in |
| P4-08 | **Landed** (`ph/seams/workspace_git.py`, the `workspace-lifecycle` row, and the per-agent root on `ctx.fs`). **The tier is two rows, not one, and the split is the row's first finding.** `workspace-git-worktree` answers "what happens when someone acquires"; `workspace-lifecycle` answers "who acquires, and when" — and only the second is in `ph-base`, because the provider costs a checkout per agent and which profiles pay for it is P4-11's decision. Written as one row, a deployment driving the lifecycle itself (a test, an embedder) could not have had the seam without it. **Acquisition is lazy, at the first `agent/pre-step`**, for two reasons that are not convenience: `agent/created` is an `emit`, so a listener there could not hold the agent up while `git worktree add` ran and the first tool call would race the checkout; and a child's base and access are its *parent's* decision, so the spawn path acquires first and the row has to find that one rather than overwrite it — which `of()` already answers. **The provider lives in `ph-core` and not in `ph-rlm`'s bundle**, where the port plan's §6.1 row listing puts it: both `rlm-stable` and the child profile need the tier, and P4-03 already settled that neither sibling package may depend on the other. Which profile *layers* the row is untouched. **`ctx.fs` had to resolve per agent, and that is what makes E2 true rather than decorative.** A process-wide root means `edit("x.py")` is the same file for every child, so the tier would rename the fan-out hazard instead of fixing it. `FsService` gained `rebase(resolver)` — a *slot* via `claim_slot`, not a list like `hide`, because two answers to "where does this agent write" is a contradiction and the first-match-wins reading a list would need is one nobody could configure — and `resolve`/`glob`/`grep` take the agent that `read`/`write`/`edit` were already threading for their intents. The seam is not consulted from `ctx.fs` directly: the row that knows about both wires them, so a profile with no workspace row keeps exactly today's behaviour. **The kernel's cwd is the load-bearing half.** A cell's `open("notes.txt", "w")` reaches no waterfall by construction, but it does resolve against the child process's directory — so `PythonCodeRuntime` resolves cwd *per kernel* (the namespace already **is** the agent id) rather than holding one runtime-wide `cwd`, which a parent and its children would otherwise share. A test runs a real cell that writes a relative path and finds it in the agent's own tree. `bash` follows the same root, since a shell command is the shortest path from a model to a relative write. **The redirection env ships at the `worktree` tier, not only at `readonly-scratch`, and that is what makes E12 a phase-4 gate instead of a phase-6 one.** `pytest` writes `.pytest_cache/` and `__pycache__/` into the tree it runs against, so without redirection *every* worktree an agent tested in would be dirty, every one would be kept, and "remove a clean worktree, keep a dirty one" would decay into "keep everything" — the policy silently stops meaning anything. The test runs a real `pytest` in a real worktree and asserts `git status` is empty afterwards. **Three findings from driving real git.** `_toplevel` asks `git rev-parse` rather than looking for a `.git` entry: a worktree's `.git` is a *file*, a bare checkout has none, and a subdirectory is a perfectly good `base` — three cases a directory probe gets wrong in the direction of a false positive. `add` tolerates the two states a resume finds — an existing worktree at this agent's path is *reused* (recreating it would discard the work the keep-dirty policy exists to preserve) and an existing branch is attached rather than reset, since `-B` would silently drop it — and prunes first, because a crash between `add` and the next run leaves a registration git refuses to write over, which would otherwise make the provider decline for the life of the repository while the seam reported `shared` and nobody could see why. Removal is `--force` for both kinds: ephemeral is discarded *even if dirty*, which is the kind's whole promise, and a tree measured clean a moment ago can still hold an untracked file. **`granted` is access to the *project*, not to a directory**, which is the RLM child path's finding. `worktree-ephemeral` writes freely and merges nothing, so it grants `read`; `shared` and `worktree` grant `write`. The consequence is that at the shipped `advisory` tier a `read` request comes back `write` — and that is the honest answer, because §4.8's own table calls that row **not read-only**: nothing there can make a repository unwritable, and claiming `read` would be the promise-nothing-enforces the field was built to refuse. No downgrade code was added for it, because it is a *widening* and `downgrade_reason` means narrowing; the requested/granted pair carries the fact and the child's own workspace prompt line states it in words. `workspace-not-mounted` stays reachable and now means what it says — a profile with no seam at all. Two test-visible consequences worth naming: `workspace/acquired` now appears in the loop's own event order before the first `step/start`, which `test_agent_loop` pins; and `json` mode counted its events *inside* the context manager, so the first event ever appended during teardown — a released workspace — made its own "same count as the log" assertion false. It counts after unwinding now, which is what the claim always meant. **A cleanup pass then moved four things and found two defects.** The question "what workspace does this agent hold" had been written five times — the prompt line, `bash`, the kernel, the spawn path and the fs resolver — and the copies had already disagreed about whether an agent with no `id` means `None` or a lookup with an empty key, and about whether a raising seam is fatal: this is the drift `_registry`'s own docstring exists to complain about, so `workspace_of(ctx, agent)` now holds the optional-seam and fail-soft readings once. The kind→access rule moved with it, as `project_access(kind)` beside `WorkspaceKind` and **exhaustive over the `Literal`** rather than a membership test in `ph-rlm`: P6-05 adds `readonly-scratch` and P4-12's doctor prints the same mapping, and a kind added in `ph-core` that `ph-rlm` did not know about would have silently classified as `read` with nothing failing — `mypy` refuses the match instead. `redirection_env` moved out of the git tier for the same reason: the table is a property of *scratch*, not of worktrees, and §4.8 gives it to `readonly-scratch` too, so leaving it here meant P6-05 importing from the git provider or keeping a second copy. And **the derivation moved out of `tool-bash` into `ctx.shell`**: the tool was reading the fs root *and* the workspace env to build one spawn, which made "a command run for an agent runs in that agent's workspace" a property of one builtin rather than of the seam — and `ShellService.run` already carries a `policy` parameter P4-10 must fill from the same workspace, which would have been the third read. `run(command, agent=…)` now resolves both, which also removed a `env` parameter whose additive contract silently contradicted `SubprocessSpawnSpec.env`'s wholesale one, one call apart. **The two defects.** `TMPDIR` pointed at `<scratch>/tmp`, which nothing creates — `tempfile` raises on a missing `TMPDIR`, so the one variable that exists to make a read-only repo usable would have broken the first temp file; it is `scratch` itself now, the one directory the seam guarantees. And **`branch -D` was force-deleting the branch of a worktree whose agent had committed its work**: `_dirty` reads `git status`, a commit empties it, so an agent that finished *properly* presented as clean and had its tree removed and its branch dropped — discarding exactly what "keep dirty" exists to protect. The non-ephemeral path uses `-d`, git refuses an unmerged branch, and `kept` reports what actually survived. `worktree prune` moved onto the failure path it exists for (a stale registration is *why* `add` refuses) and `_toplevel` is memoized per `base`, which together drop 14 of the 16 duplicated git spawns an eight-child fan-out was paying for — measured at 24% of acquire wall time for the prune alone, on the critical path before each child's first step. Three test fakes of one tier, in two packages, became `ph.testing.StubWorkspaceProvider` beside the existing `StubCodeRuntime`; each had absorbed the provider protocol behind a `**_`, which is what would have hidden a `WorkspaceProvider` change from all three at once. `test_workspace_git.py` now mounts the row instead of hand-assembling the seam — ten of its eleven tests never exercised the entry point, and every `git` call was accumulating an effect disposer on a `Context` nobody disposed. **That rewrite immediately caught a mistake in its own fixtures**: the repositories were being built under `ctx.fs.root`, which is the *process's* directory — this checkout — so the tests were initialising git repositories inside pH's own tree and sharing one branch namespace across the file, which made the E12 assertion pass for the wrong reason once. The test now asserts `kind == "worktree"` before measuring anything, so a declined tier cannot make it vacuous again. **Two findings left for the rows that own them.** `permissions-fs` captures `fs.root` at mount, so under this tier an agent's paths sit outside that prefix and an anchored rule (`secrets/*`, a bare `.env`) silently stops applying inside a worktree — that is P4-10's row, which pairs `permissions-fs` with the worktree tier, and it is unreachable until P4-11 layers a tier into a profile. And disposal is N sequential three-spawn releases because `Context.dispose` unwinds one `await` at a time; on a real repository that is seconds of exit latency, and it belongs to cordis rather than to this provider. Original scope: `workspace-git-worktree`: `acquire(access)`; `worktree` (branch `ph/<session>/<agent>`) / `worktree-ephemeral`; `repo_writable`; `ctx.fs` root + `ctx.subprocess` cwd → `workspace.root`; redirection env; non-repo → decline + fallback + notice; dispose policy (keep dirty, remove clean, **discard ephemeral even if dirty**); `workspace/acquired`/`disposed` events | P4-07, P1-17, P1-18 | E2, E3, E5, E12 | two children merge without collision; ephemeral discarded; `pytest` green in a read child writing only to scratch; non-repo → `advisory` in doctor |
| P4-08b | **Landed** (`ph/seams/workspace_provision.py`, `ph/seams/workspace_commands.py`, plus provisioning and named declines on the seam). **Three findings, and the first invalidated a sentence this row had already written.** The plan said provisioned paths would be appended to "the worktree's own `info/exclude` (`.git/worktrees/<name>/info/exclude`, per-worktree by construction)". **There is no such file.** Git resolves `info/exclude` against the *common* directory even from inside a linked worktree — `git rev-parse --git-path info/exclude` run in a worktree returns the repository's own file, and writing the per-worktree path has no effect at all, both verified before changing anything. The only exclude that works is the shared one, which would hide these paths in every other worktree *and in the user's own tree*, which is the rudeness the per-worktree claim existed to avoid. So the mechanism moved out of git entirely: the provisioned list rides to disposal and becomes `:(exclude)` pathspecs on the `git status` that decides dirty-or-clean. That is strictly better than the original plan — it mutates no git state, and it scopes the rule to the *one decision* it is about rather than to what the person sees, so a kept worktree still shows its materials. It cost a signature: `Workspace.release` now takes the provisioned paths, because "is there work in this tree" and "is there anything in this tree" are different questions and only the first should decide whether it is kept. **The second finding is `.git` itself.** The guards refuse it in both directions, and the `dest` half is not tidiness: a `to: .git/hooks/pre-commit` is a planted hook — *code that runs on the agent's next commit* — which is exactly the execution this row refused when it dropped the installer, arriving through the door left open by the copy list. `wtp` has no equivalent refusal. **The third is that `resolve()` has to come before the containment check, not after**: a check on the joined-but-unresolved path accepts `link/passwd` for any `link` pointing outside, so a project containing one symlink would have reopened the escape decision (1) closed. All three are tested one refusal at a time. **Provisioning lives on the seam and nothing reaches a `shared` workspace**, whose root *is* the base — copying `.env` onto itself is the one way this could destroy the file it exists to provide. Modes are `copy` (default; tries the Linux `FICLONE` ioctl first, so copy-on-write arrives free where the filesystem has it and degrades silently where it does not — the development machine's ZFS pool has block cloning off, which is why it is a try-and-fall-back rather than a capability probe), `hardlink` and `symlink`, each opt-in with its trade named. A directory walk recreates inner symlinks rather than following them, because a dependency tree is full of them and copying *through* one turns a small tree into whatever it points at. **The project half is data and only data**: `.ph-workspace.yml`, discovered nearest-first walking up (`agent-instructions`' rule), and `WireModel`'s `extra="forbid"` is what makes the claim enforceable rather than aspirational — a `command:` key does not get silently dropped, it *rejects the file*, so a repository cannot smuggle one past a reader who assumed the model was data-only. Every parse failure is a shrug, since refusing to start would make an optional file load-bearing. **`/workspaces` is three refusals and two verbs.** A tree a live agent holds is refused by asking the *seam*, not the filesystem — a worktree clean this instant belongs to an agent that may write to it next; `--force-branch` without `--with-branch` is refused rather than silently ignored; and a branch is deleted with `-d`, so git declines an unmerged one and the answer names the flag that would override it instead of making a person guess. Worktrees pH did not make are never listed, because `git worktree list` reports the user's own and a management command offering to delete those is a different and much worse tool. **Declines now say which one they were** — `WorkspaceDeclined(reason)` raised rather than a bare `None`, read off git's own words for `branch-in-use` and `path-exists` — and the field is *absent* when no tier was configured, because "advisory because nobody asked" and "advisory because the tier could not serve" are different facts and a doctor that ran them together would be useless. Two smaller things: the LIFO ordering that stops a worktree being removed out from under a process still running in it was true by construction and asserted nowhere, and now is; and `from`/`to` could not be the wire names despite matching `wtp` and reading better in YAML, because `test_wire.py` pins every alias to `wire_alias(field_name)` (Q2, "declare aliases, never derive them") and an explicit `alias="from"` would have been the one field in the tree the convention does not generate — `source`/`dest`, with the reason written down where the next reader will ask. **A cleanup pass then found a defect, two wrong altitudes and a measured 50%.** The defect: `_materialize_tree` hand-rolled `os.walk(followlinks=False)`, which puts a symlinked *directory* in `dirs` and never descends — so a loop recreating only `files` **dropped symlinked directories entirely**, which is most of a pnpm `node_modules` (every `<pkg> -> ../.pnpm/...`) and every `.venv/lib64 -> lib`, i.e. precisely the trees the row exists to carry. `shutil.copytree(symlinks=True, copy_function=…)` is the same dispatch the walk was written for, fixes it, and is **2.2× faster** because `scandir` answers "is this a symlink" from the directory entry instead of a stat per file. Pinned by a regression test. Beside it, `_reflink` fired a guaranteed-failing `ioctl` per file on a filesystem that cannot clone — two opens, the failing call, an unlink and two closes, **+50% wall time over a 20 000-file tree, paid per child in a fan-out** — so the answer is now latched per device pair and a cross-device copy never attempts it at all. **Wrong altitude, one: "which paths are the agent's work" was a parameter on the teardown callback**, so it existed for one caller; `/workspaces` asked the same question separately and called the same tree *dirty* that the disposal policy called clean, on exactly the case E14 introduced, and P4-09's `/revert` would have been the third with no access at all. It is now `Workspace.agent_work_pathspec()` and `release` takes the whole value, so the next fact a teardown policy needs costs no protocol change. **Wrong altitude, two: `/workspaces` was in `ph/seams/`**, the only module there that publishes no service and registers no provider — it injects four seams and is a pure consumer, and its shape-siblings (`compact_command`, `/refine`) all live outside. It moved to `ph/commands/`, which is where P4-09's `/revert` now has a home rather than facing the same fork alone. Two smaller altitude fixes: the decline vocabulary said `git-failed` for *any* provider and the seam **fabricated** that reason for a provider that declined without giving one — the generic code is `provider-failed` and an unexplained decline now records nothing, since inventing a reason reintroduces one level down the confusion E15 exists to remove; and `_add` classified declines by matching English in git's stderr, which is gettext-translated and which `ctx.subprocess` passes `LANG` through to, so on a non-English host every decline collapsed to the generic code *in the row whose entire point is telling an operator why* — it asks git's state instead (`worktree list --porcelain`), and `git` invocations now pin `LC_ALL=C` so nothing else read off git's words can drift by locale. **`ProvisionEntry` now validates its own syntax**, which moved an operator's YAML typo from the *model's prompt line*, once per agent forever, to profile-config load where the person who wrote it is standing — `provision_failures` reaching the agent is right for "this project has no `.env`" and wrong for "your config is malformed", and the tests now split by which enforcement point catches which. Provisioning also moved *after* the `ctx.effect` registration: a 20 000-file walk between `provider.acquire()` and the effect was the widest unowned window in the module that argues I2. Reuse: `safe_yaml_load` instead of bare `yaml.safe_load` — `.ph-workspace.yml` is the least trusted config the harness opens and was the only one parsed by different rules than every profile row; `claim_entry` in `_registry` because `ProvisionEntry` is the first contribution list holding *values*, where `list.remove`'s `==` would have one row's disposer take another's (the five sibling lists hold closures and got identity by accident); `is_under` in `paths.py` for the two containment spellings; and `ph.testing.git_repo` for the repository setup two test modules had copied and a third was about to. Efficiency in the command: `--untracked-files=normal` where the result is a boolean (`all` enumerated 20 000 paths to say what one line says), no `git status` at all for a held tree, none for `merge`/`remove` which never read it, and the rest concurrent — measured 4.5× on eight leftovers, which is the thing the keep-dirty policy is designed to accumulate. And `held` is matched by **root path** rather than by inverting a directory name back into an agent id, which `sanitize_ref` makes lossy — an id that did not sanitize to itself would have read as unheld and lost the refusal protecting a live agent's tree. Skipped and named: routing provisioning writes through `ctx.fs`/`permissions-fs` (the intents are agent-scoped and provisioning precedes the agent; `git worktree add` writes a whole checkout past the same policy, and P4-10 is where that reach is *reported*), and `find_upward` in `paths.py` (two callers with different collect rules; worth it when P4-13 brings the third). Original scope: **What the worktree tier is still missing, read off `sources/wtp`** (Go; `cmd/wtp/{add,remove}.go`, `internal/hooks/executor.go`, `internal/config/config.go`, via codegraph). wtp is `git worktree` plus four ideas — path-from-branch-name, local→remote branch resolution with multi-remote refusal, post-create hooks from a repo-owned `.wtp.yml` (`copy` / `symlink` / `command`, in order, output streamed), and `remove --with-branch` — and the review sorted them into adopt, adapt and skip against P4-07/08. **The gap it exposes is that a worktree is not a usable checkout.** `git worktree add` carries no ignored file, so a child in its own tree has no `.env`, no `node_modules`, no `.venv`, and "run the tests" fails for a reason the model cannot see and reads as its own bug — the exact failure E12's redirection env solves from the *output* side. wtp's `copy` hook is the input side, and every design choice in it is right for pH as written: `from` is relative to the **main** worktree so a gitignored `.env` is reachable (pH's analogue is the *base* — the parent's tree, which in a fan-out is what the child branched from), `to` is relative to the new root, both are traversal-guarded (`ensureWithinBase`), source and destination are SameFile-guarded, mode bits are preserved, and a symlink refuses to clobber an existing path. **Adopt, on the seam rather than the provider**: `copy` and `symlink` lists as `workspace-lifecycle` row config, applied by `WorkspaceSeam.acquire` *after* the provider answers, so `readonly-scratch` (P6-05) and any later fresh-root kind inherit them without re-implementing the guards — the same argument that put `scratch` on the seam in P4-07's cleanup. Logged as `workspace/provisioned {agentId, copied, linked, failed}` (ignorable: a reader that skips it misreads nothing about the rest of the log), and a failure reaches the child's **workspace prompt line** — wtp prints `Warning: Hook execution failed` and keeps the tree, which is the right recovery and the wrong audience: nobody reads a warning, and the one party that must know `.env` is missing is the agent about to run the tests. **One hazard wtp does not have and pH will.** A copied `.env` or a symlinked `node_modules` that is not gitignored is an untracked file, so `_dirty` reports the tree dirty and the keep-dirty policy keeps it forever — P4-08's `.pytest_cache` finding from the other direction. Provisioned paths are appended to the worktree's *own* `info/exclude` (`.git/worktrees/<name>/info/exclude`, per-worktree by construction), so `git status` keeps meaning "the agent's work"; wtp never auto-removes, so it never met this. **Adopt the decline taxonomy, not the prose.** wtp parses git's stderr into five typed errors (`already checked out`, `branch already exists`, `destination path already exists`, `is ambiguous`, invalid path) each with a "Solutions:" list. pH's decline-and-fall-back is the right posture — never turn "not a repository" into "pH will not start" — but the *reason* today reaches only a log line, and P4-12's doctor has to print it: `workspace/acquired` gains `declined: not-a-repository \| branch-in-use \| path-exists \| git-failed` when the kind is `shared` under a registered tier. **Adapt `remove --with-branch` into the human half of the keep-dirty policy.** wtp's README opens with "remove worktree, forget to delete the branch, orphaned branches accumulate" — and pH's disposal policy *creates* that accumulation on purpose, under `$PH_HOME/worktrees/<session>/<agent>` on `ph/<session>/<agent>`, with no human-facing way to see, merge or remove what was kept. `/workspaces` on `ctx.commands` beside P4-09's `/revert`: `list` (agent, session, branch, dirty), `merge <agent>`, `remove <agent> [--with-branch] [--force-branch]` — `-d` by default so an unmerged branch refuses and names the flag, `-D` only when asked, which is exactly the rule P4-08's cleanup pass arrived at for the automatic path. wtp refuses to remove the worktree you are standing in (`isPathWithin(target, cwd)`); pH's translation is `seam.of(agent) is not None` — a tree a live agent holds is refused, and the same property is pinned structurally with a test that the kernel subprocess is unwound before its workspace (LIFO), which is true today by construction and asserted nowhere. **Provisioning is materials-only: no installer runs, ever.** wtp's third hook type is `command` (`npm ci`, `make db:setup`; `sh -c`, cwd the new tree, `GIT_WTP_WORKTREE_PATH` and `GIT_WTP_REPO_ROOT` in the env), and it is **not adopted** — the reasoning is about lifespan, not about danger. A person's branch amortizes a thirty-second install over weeks of work; an agent's worktree may live for one turn, and in a fan-out that install is paid per child, *before* each child's first step. It also converts a local, deterministic, offline operation into a network-dependent one: `git worktree add` cannot fail because npm or GitHub is down, and an install can, which would turn a registry outage into "pH cannot start an agent". The materials a test run actually needs are already sitting in the parent's tree, so they are **copied, not rebuilt** — the same answer `.env` gets, applied to `node_modules`, `.venv`, `vendor/`, `target/`. Measured on a synthetic dependency tree (2 000 packages, 20 000 files, 22 MB) on ZFS: **`cp -a` 0.674 s, `cp -al` (hardlink farm) 0.188 s, `ln -s` 0.003 s** — against tens of seconds plus a network round-trip for the installer they replace. So the row ships three modes and no fourth: `copy` (isolated; the default, and the seam tries `--reflink` first since copy-on-write is isolation at symlink cost where the filesystem has it — btrfs, XFS, APFS, and OpenZFS only with block cloning enabled, which the development machine does *not* have, so it must degrade silently rather than be assumed), `hardlink` (near-free, and safe for the replace-and-rename pattern every package manager uses — pnpm's entire store is a hardlink farm — but a build step that writes *in place*, a native addon or `patch-package`, mutates the parent's copy through the shared inode, so it is opt-in with that named), and `symlink` (instant, zero space, and **shared and mutable**: a child writing through it reaches the parent's tree, which hands back some of the collision isolation the tier exists to buy — also opt-in, also named). **Two things fall out of dropping the installer, and both are the point.** There is no provisioning process, so there is no environment to argue about: F1's scrub of `*KEY*`/`*SECRET*`/`*TOKEN*` stands with no exception, where an `npm ci` against a private registry would have needed one written for `NPM_TOKEN`. And a repo-discovered list becomes *data only* — a `copy`/`symlink` entry cannot execute — so cloning a repository and starting pH runs nothing the repository authored, which is the property wtp gives up when it accepts that its config is "controlled by developer". If a build step is ever genuinely needed it is its own row, argued on its own merits, and it starts from the fact that this one refused it. **Explicitly not adopted**: path-from-branch-name (pH's names are session and agent ids, the plan's own decision); remote-branch tracking and multi-remote disambiguation (an agent branches from its parent's tree and checks out nothing); `cd`, `exec`, shell hooks, completion, `--quiet` (human CLI ergonomics — `wtp exec` is `ctx.shell.run(agent=…)`, already there); inheriting the full `os.Environ()` in hooks (F1); and the `command` hook itself, per above. A small knob that fits and can wait: `base_ref` (`main`) for a child that should start clean rather than from its parent's in-progress branch — today's `HEAD`-of-base is right for the fan-out merge story and is the default. **One difference stated so nobody "fixes" it.** wtp locates the main repository with `git rev-parse --git-common-dir`; pH's `_toplevel` uses `--show-toplevel`, which inside a linked worktree returns *that worktree*. That is the intended answer here — a child branches from the parent's tree — and `worktree add`/`prune`/`branch -d` operate on the shared refs from any linked worktree, so nothing downstream needs the common dir. | P4-08, P1-05 | E14, E15, E12, F6 | a child in a worktree reads the parent's gitignored `.env` and runs `pytest` green against a provisioned dependency directory, with no installer and no network; a provisioned path leaves a clean tree clean (still removed); a repo-supplied list naming `../x` is refused; `/workspaces remove --with-branch` removes both on a merged branch and refuses naming `--force-branch` on an unmerged one; `remove` on a tree a live agent holds is refused; the kernel is unwound before its workspace; `ph doctor` shows the decline reason on a non-repo cwd |
| P4-09 | **Landed** (`ph/seams/workspace_checkpoint.py`, `ph/commands/revert.py`, plus `ToolDefinition.undone_by_workspace_restore`). **The capture has to be invisible, which is what `GIT_INDEX_FILE` buys.** `git add -A && git write-tree` against pH's own index file leaves branch history, the working tree *and the agent's own staging area* untouched — an agent that had staged a file for its own commit finds it still staged and only staged, which is what lets this run before every cell rather than being a thing a person opts into. The index is **kept between checkpoints** rather than made fresh: a fresh index has no stat cache, so git would re-hash every file in the repository on every cell. **Write-ahead in A10's shape, and it buys a nicer address.** The tree goes to the object store, then the event, then the ref that keeps the tree from being collected — so a crash in the window leaves a checkpoint that is *unavailable* and says so, never a ref nobody recorded. And because `seq = len(log)` is exactly what `append` will assign (A1), the ref can name the seq of an event that has not been written yet: `/revert <seq>` therefore names **one** thing instead of making a person correlate an event with a ref. The ref lives outside `refs/heads`, so `git branch` does not list it and a push does not carry it. **Only a workspace pH made**, gated on `fresh_root(kind)` — a restore point in a `shared` workspace is the *person's* own checkout, and offering to overwrite their uncommitted work with whatever state an agent found the tree in is the one thing this must never do. **The restore is three steps and the middle one is where the care is.** Files in the tree are written from a *scratch* index, not the agent's: `git add -A` puts untracked-not-ignored files in the checkpoint, so reading that tree into the real index would bring them back **staged** — a `git status` the agent did not create and cannot explain. Files not in the tree but known to git and not ignored were made by the run, so they go, along with the directories that are then empty; `.gitignore`d paths are never considered in either direction, because a `/revert` that wiped a build cache would turn a recovery into a rebuild. Scratch is outside the worktree entirely and survives by construction, which is what makes it the right place for anything a child wants to keep (E5). Every listing is read `-z`, since a path may contain a newline and the file a line-split restore skipped is exactly the one it promised to remove. **N3 is a `ToolDefinition` field, not a name list.** `/revert` prints what it restored *and* the run's dispatches a tree restore does not cover, read from each tool's own `undone_by_workspace_restore` — the same argument `self_limits` and `arguments_disposable` already carry: these are registered plugins, so a deployment renames `bash` and an MCP server adds a publish tool, and a name list in this module cannot know either. The default is the **safe** direction, so a capability nobody thought about is over-reported rather than silently trusted, and the five fs builtins opt in so the listing stays about calls that actually reached past the tree. Two findings while testing. The dispatch arguments come back from the log frozen as a `MappingProxyType`, which is a `Mapping` and is **not** a `dict` instance — an `isinstance(..., dict)` check rendered every entry as `bash()` with the one detail a person needs to recognise it stripped out, which is the same trap P4-05 hit reading a frozen argument tree. And `git` grew an `env` passthrough for `GIT_INDEX_FILE`, additive over the scrubbed environment, because a git that inherited nothing else would not find its own configuration. `workspace/checkpoint` is **not** ignorable: a build that skipped it would offer `/revert` a session with no restore points while the refs sit in the repository, which is recoverable state misread as unrecoverable. **A cleanup pass then moved the module, rewrote the restore, and found the checkpoint was hashing the wrong set.** Placement first: `workspace_checkpoint.py` was the only module in `ph/seams/` that is neither a seam nor a provider of one — verbatim the criterion `ph/commands/__init__.py` wrote down one row earlier when `/workspaces` was evicted for it — and the placement had already cost something concrete, since no `seams/` module imports `ph.tools.definition` and the hook was typed `execution: Any` where every sibling `tools/execute` wrapper types it `ToolExecution`. It folded into `workspace_git.py`, whose vocabulary (`git`, `sanitize_ref`, refs, index files, `_toplevels`) is already every line of it, and §12 Q9c assigns the capability to that provider in as many words. **The restore was rewritten and is 30× faster for a better reason than speed.** `checkout-index -a -f` rewrites *every* file in the tree — 2.3 s of a 2.4 s restore on an 11 000-file checkout — and, worse than the time, stamps a new mtime on every unchanged file, invalidating every mtime-keyed cache the person has (pytest, mypy, ruff, the editor's index) so they pay for the full-tree rewrite again on their next command. Seeding the scratch index from the refreshed checkpoint index and using `read-tree --reset -u` touches only what differs, ~70 ms, and **also does the removal job**: three `ls-*` spawns, two set builds and a Python unlink loop collapse into one `diff-index` taken before the reset. Verified by hand against a tracked modification, a deleted tracked file, an ignored file, a nested new directory and a file the run had `git add`ed — the untracked file comes back *untracked* in the agent's real index, which is the property the scratch index exists for. **The first cell was re-hashing the repository.** The module's own claim that a kept index avoids that holds from the *second* cell; the first started empty and paid 2.6 s on an 11 000-file checkout, per agent, so a fan-out paid it per child. Seeding `ph-checkpoint-index` from the index `git worktree add` just wrote costs half a millisecond and makes it 32 ms. **And it was capturing the wrong set**: `git add -A` wholesale hashes provisioned materials — a copied `.env`, a hardlinked `node_modules`, untracked-and-not-ignored by construction (E14) — into every cell's tree, and a restore then deleted anything provisioned after the checkpoint. Both sides now take `Workspace.agent_work_pathspec()`, whose docstring already named this row as its third consumer and warned that consumers answering separately disagree. **Four smaller corrections.** `seq = len(session.events)` materialised the whole log to read its length (372 µs at 200k events) *and* re-derived `append`'s private seq assignment — the event is appended first and its own `seq` is the address, so the payload now carries neither `seq` nor `ref` because both are derivable from the event holding them. `rev-parse --absolute-git-dir` spawned a git process per cell for an immutable path, the same finding `_toplevels` records one class up; it is memoised. `/revert`'s `_root_of` was a sixth spelling of `workspace_of` — and *less* safe, since a disposed agent whose directory got reused would have compared equal — so it asks the agent id. And `ctx.tools.get(name)` dropped `scope=`, which ran in the **unsafe** direction: a row shadowing `write` resolved to the global builtin's `True`, so a dispatch that reached past the tree would never have been listed. The field is now `effects_confined_to_workspace`, named for what the *tool* knows rather than for the consumer's git verb, with its one real gap written down — `ctx.fs.resolve` passes absolute paths through, so a `write` outside the tree reports as covered, and the mechanism that stops that is P4-10's permission row rather than a predicate here. Tests: `pytest.mark.skipif(git)` was missing, so on a machine without git this module produced a dozen errors where its two siblings skipped cleanly — the marker, the tier's row set and a `worktree_agent` fixture now live in `ph.testing.git` and all three modules use them, and the two near-identical fixtures in this file became one. The policy row itself was the only untested code in the changeset and now has a mounted test proving a code run checkpoints and a native `read` does not. Original scope: `workspace/checkpoint` + `/revert <seq>`: `git write-tree` under `refs/ph/<session>/<agent>/pre-run/<seq>` before a mutating run (write-ahead: event first); restore tracked + untracked-not-ignored; never ignored paths or scratch; `/revert` lists non-filesystem dispatches | P4-08, P1-05 | E7, N3 | denied run reverts exactly; ignored paths untouched; irreversible dispatches listed |
| P4-10 | **Landed** (`ph_stabilize/workspace_scope.py`, plus a per-agent root on `permissions-fs` and `workspace_policy` on the seam). **It is a row, not the `permissions-fs` rules the scope line called for, and the encoding failed on its own terms.** A rule names paths; the path this policy is about is per *agent* and does not exist until an agent acquires a workspace, so a static `paths:` list cannot say "this agent's own tree". The obvious escape was the two spellings `permissions-fs` already offers — a relative pattern for inside, an absolute one for out — and it does not work: `matches_glob` was checked directly and `**` matches `/tmp/x` as readily as `a/b.txt`, so the two are not separable as globs. What *is* separable is the question, which the workspace seam already answers, so the row asks it and reuses everything else — `ctx.approval` for the prompt, so a deployment still configures one answerer, and the same fail-closed reading, where a missing answerer **and a missing agent** both deny (the seam derives the session from the agent, so an agentless ask would prompt a human with nothing in the log and walk past a deployment's `approval_policy: "never"`). **Precedence is the layering, and the bundle says so.** The row sits *after* `permissions-fs`, and since waterfall listeners run outermost-first in registration order an operator's explicit `deny` refuses before this default is ever consulted — which is what makes it a default rather than an override of the rules someone wrote on purpose. A test pins it, and the bundle comment says reordering the two changes what they mean. **Scratch is inside the scope, always.** It is outside the worktree by design (E5) and is the one place a read-only or ephemeral agent is *told* it may write, so a boundary drawn at `root` alone would have prompted on exactly the writes the design invites. **The carried-over hole is closed.** `permissions-fs` captured `fs.root` at mount, so under this tier an agent's paths sat outside that prefix, `_spellings` offered only the absolute form, and every anchored rule — `secrets/*`, a bare `.env` — silently stopped applying *inside a worktree*, which is the one place a rule is most needed and with nothing failing to say so. It asks `ctx.fs.root_for(agent)` now; the intents already carried the agent, `hide`'s predicate gained it (a concealment answering for the wrong tree is the same bug one function over), and P4-06's 26 µs `relative_to` finding survives as a prefix cached per *root* rather than per service. Two agents with one rule now get two answers, which is the point. **One boundary, two vocabularies, one definition.** `workspace_policy` lives on the seam beside the value it describes rather than in this row, because `ctx.shell` requests it of a backend and this row prompts about what falls outside it — two spellings that drifted would be exactly the defect §4.8's tier table exists to prevent. `ctx.shell` fills the `policy` parameter no caller had ever filled, but **only when a backend is available**: `confine()` refuses rather than passing through, so requesting confinement unconditionally would turn every shell command into a `SANDBOX_UNAVAILABLE` denial until P6-04. Left for P4-11: no profile layers the `worktree` tier yet, so the row is inert in every shipped profile — with no acquired workspace there is no scope to be outside of, which is the honest behaviour rather than a boundary drawn around a directory nobody chose. **A cleanup pass then deleted the row and made it a rule, which is what the scope line asked for in the first place.** The review confirmed two defects by probe. A `permissions-fs` `interrupt` rule matching an outside path produced **two prompts for one write** — the two gates queued rather than composed, which is precisely the ergonomics failure E6 exists to prevent — and the new gate returned `None` on approval instead of `await next_()`, which under `Context.waterfall` **vetoes the rest of the chain**, so an approved outside-write never reached a row layered after it. Both are symptoms of the same thing: two listeners on one waterfall, with a security precedence expressed only by the order two rows happen to appear in a bundle — in a repo whose three other ordered registries all carry an explicit `order: int`, and whose loader can only *append*, so a downstream profile could not have placed its own rule ahead of the default anyway. **And the row's own argument for existing had been falsified by this same changeset.** "A static `paths:` list cannot say *this agent's own tree*" was true of paths and stopped being true of *rules* the moment `decide` was given the agent and a per-agent root — so the fix is a rule field, `scope: anywhere \| outside-workspace`, evaluated at decide time. One engine, one first-match-wins list, one prompt, and precedence that comes from a rule's position rather than from a bundle's layering; the whole module and its entry point are gone, and the default is the last rule in `permissions-fs`'s own config where an operator can read it beside their own. It inherits E9's reach sentence and `ctx.fs_permissions` for free, which the separate row had neither of — and it is the *more* over-believable of the two, being named after the tier. **One writable set, not two.** `writable_roots` sits on the seam beside `workspace_policy`, which now derives from it: the row and the sandbox policy had enumerated "root + scratch" separately in two packages while the docstring claimed they could not drift, and the test asserting they agreed was the tell — a consistency check between hand-maintained copies catches drift instead of preventing it. **`is_under` was reintroducing the cost P4-06 removed**: `Path.relative_to` measured **17.9 µs** and was ~90% of a gate running on every write, against **0.16 µs** for the separator-aware prefix compare the package already used — with `normcase`, since folding case on Windows is the one behaviour worth keeping from `relative_to`. The measured headline worry did *not* hold: the per-agent prefix costs +0.5 µs/file, 1.2% of a repository-wide `glob`, against the 26 µs/file P4-06 fixed. It is still an `lru_cache` rather than a dict field, because the field was unbounded by *roots ever seen* rather than by live agents and had lost its `init=False`, making a private cache a public constructor argument of a frozen value. `FsPermissions` holds `roots: Callable` rather than `fs: Any` — the one member it uses, which restores the value's hashability, types the call, and deletes the stub class the `Any` had required of the tests. Smaller: `concealed`'s `agent` is required rather than defaulted, since a default silently answers for the process root, which is the bug per-agent roots exist to fix; `answer_approvals` returns what it was asked, collapsing three spellings of "what was prompted" including a `lambda: asked.append(...) or "allowed-once"` smuggled through its own callable escape hatch; the workspace-acquiring boilerplate became `scoped_agent`; the `ask_outside` boolean is gone with the module, its "deny instead" case already served better by `approval_policy: "never"`, which records both halves in the log; and `ctx.shell` now says in a debug line when it declines to confine, because `confined_by=None` cannot otherwise distinguish "the tier wanted confinement and had no backend" from "nobody asked" — which is the passthrough the sandbox seam refuses one layer down, and what P4-11's `containment.strict` turns from a notice into a refusal. Original scope: Default write scope: `SandboxExecutionPolicy {workspace-write, <worktree>}` + `permissions-fs` row for the `worktree` tier | P4-08, P4-06 | E6 | inside-worktree cell prompts zero times; outside prompts once |
| P4-11 | **Landed** (`ph/seams/containment.py`, a `SandboxProvider` protocol, and the rlm bundle's choice). **The selector could not be a loader predicate**, which settled the shape: `disabled:` accepts a literal, `${platform:…}` or `${env:VAR}` and nothing else, deliberately (I-8, no code evaluation), so "disable this row unless the tier is worktree" is inexpressible without widening a closed vocabulary. Selection is therefore **per acquire**: the provider registers whenever its row is layered, and what the tier decides is whether a given caller *asks* for it. That keeps one provider slot — two answers to "what makes a workspace" is a contradiction — while letting a parent and its children sit on different rungs, which turns out to be the whole point. **The scope line's `rlm: advisory, child: worktree` is not two profiles, it is two roles in one.** A person running `rlm` works in the directory they opened and would be surprised to find their agent editing a copy elsewhere, while their *children* are exactly the fan-out §4.8 opens with — so `tier` and `child_tier` are separate knobs and one would have had to be wrong for one of them. `child_tier` follows `tier` when unset, so a deployment that means one rung says it once. **`None` is not `advisory`, and the distinction is load-bearing.** The row ships in `ph-base`, so a default of `advisory` would have opted every profile out of a provider it deliberately layered — the first draft did exactly that and broke forty-eight tests, which is the mechanism telling the truth. `None` means *nobody chose*: layering a provider was the choice, and this row adds selection rather than replacing it. Only a profile that names `advisory` is saying "this agent stays where the person is". **`strict` needed a readable `enforcement`**, so the sandbox seam gained the `SandboxProvider` protocol P4-07's cleanup gave the workspace seam, for the same reason: a backend whose method drifted would fail inside a caller's `except` and be reported as *unconfined*, the one direction this seam must never fail silently in. Enforcement is a descriptor rather than something learned by confining, because a refusal-to-start cannot be "run a command and see". `partial` refuses, as E8 says in as many words, and so does `None` — "no backend" and "a backend that bounds some of it" are both "not confined" to a deployment that asked to be sure. **The check runs after the profile is mounted**, in `ph_app.runtime.mounted`, not inside the row's `apply`: a backend may be layered *after* the row that asked for it, and a verdict computed at mount would be wrong for exactly the profile that orders things that way — E9's live-sentence reasoning — and it is the one place "refuse to *start*" can still mean it, since a call later is a process that already started. The refusal names the missing backend **and** the setting, because an operator told only the problem is left guessing whether to install something or change their mind, and one of those may be theirs to decide. **One finding, and it was the test suite writing into the developer's repository.** Turning the rlm bundle's `child_tier: worktree` on made two shipped-profile tests branch a real `ph/*` worktree off `main` in this checkout, plus a stray `authored.txt` at the root — because `fs.root` defaults to the process cwd and the child branched from it. That is correct in production (the project *is* the cwd) and catastrophic in tests, and it was the **third** time this class landed: a test repo inside the checkout, a stray `parent-tree`, and now a worktree. The first two were fixed locally; this one is fixed once, in the `mount` fixture, which now points `fs.root` at `tmp_path` for the same reason it already pointed `PH_HOME` there — and `fs.root` is the stronger of the two, since it is what a tier branches from. The strays were removed and the repository verified clean. **A cleanup pass then moved the decision into the seam and the refusal into the loader, and found two statements that had begun to contradict each other.** The tier was a *caller-supplied argument*, and `ShellService.run` had already made the opposite call in writing one file over — "the seam resolves it itself, rather than making each shell-shaped tool remember to". The cost was not hypothetical: there was a **third** caller, `ph.testing.git.worktree_agent`, which did not pass one and silently got "use the provider" — the *unsafe* direction for a root agent, since it escalates the person's own agent into a worktree, the exact surprise the rlm bundle says a deployment should have to ask for. And the role was derivable all along: a child's session header carries `origin: "subagent"`, which the spawn already stamps, so `acquire` derives the rung from the session it is handed and `tier=` survives only as the explicit override `shell.run`'s `cwd=` is. That deleted the parameter threading at both call sites, the `tier_for` module function, the nested `_tier_for` wrapper *and* the deferred import — the cycle existed only because a policy row reached back into the selector by import, where every other cross-seam consult in the package is a runtime `ctx.get`. **`WorkspaceSeam.tier` was reporting a rung the root agent never gets.** Its docstring names `ph doctor` as its consumer and it returned `provider.tier`, so under the shipped `rlm` profile — git provider layered, `advisory` chosen — it answered `"worktree"` while the root agent deliberately gets `shared`. That is E10 inverted, and the same defect §4.8 closes on, from the other side; it now consults the choice as well as the provider. **Two operator-facing sentences had started to disagree.** `permissions-fs.confined` read "a backend exists" while `containment` read "a backend that says `full`" — so with a `partial` backend, E9's reach sentence would have told a person a sandbox bounds their code cells while E8's refusal told them a partial boundary is not confinement at all. One `enforcement_of(ctx)` on the sandbox seam now answers it, and `confined` requires `full`, because promising E9's sentence for a partial backend is the tier-name overstatement E1 exists to prevent. In the same family, `ContainmentService.enforced` never read `self.tier`, so `advisory` plus a full backend reported `True` against its own docstring — deleted rather than fixed, since its only callers were two test assertions and P4-12 wants the reading, not this spelling of it. **The refusal moved to a `profile/mounted` hook on the loader.** It had been one hard-coded seam name inside `ph_app.runtime.mounted`, which covered the app's five modes and *not* the three test modules or the suite's own fixture that mount through `Loader` directly — visible in the fact that a ph-core test had to import `ph_app` to exercise a ph-core row's central claim, the only such import in that package's tests. `Loader.mount` now dispatches `profile/mounted` (`serial`, so a listener that raises stops the process where a contained `emit` would swallow exactly the refusal that matters), the row registers a listener, and a second row wanting to refuse a deployment registers one too instead of adding another `if` to whoever starts the process. Smaller: `refusal()`+`verify()`+three `.format` sites became one clause producer and one format at the raise; a `StubSandboxProvider` joined `ph.testing`, replacing a local class *and* a bare `object()` registered as a provider one package over, which had stopped satisfying `SandboxProvider` the moment it grew `enforcement` and kept passing only because the reader it met happened not to ask; the checkpoint row's `kind` gate hoisted above the `rev-parse` it makes moot, a subprocess in the person's own checkout newly reachable once the rlm bundle layered that row beside an `advisory` root; a dead logger removed and `__all__` completed; and `base.yaml`'s new comment, which had displaced the `workspace` row's own and asserted "`advisory` for everyone by default" — the precise reading this row's docstring calls wrong and whose first draft broke forty-eight tests. Original scope: Containment tier selector (`containment.tier`, per-profile defaults `rlm: advisory`, child: `worktree`, `rlm-stable: worktree`) + **`containment.strict`** (refuse unless `sandbox` + `enforcement: full`) | P4-08, P1-19 | E1, E8 | strict on a backend-less host refuses, naming the backend |
| P4-12 | **Landed** (`ph/seams/diagnostics.py`, a `TIERS` table, and a `doctor` that mounts). **`ctx.diagnostics` is `ctx.tui_status` minus the `Session`** — same `register(..., scope=)`, same drop-a-raising-contributor rule, same order-then-id sort — and two seams rather than one parameterised seam because what they are read *for* differs: a footer field answers "where am I now" on every spinner frame and must stay cheap, while a diagnostic answers "what is this deployment" once, at a person's request, and may stat a tree. A field that quietly became expensive would take the footer down with it. A reading is `(label, value)` **rows**, which is what `PathRoots.describe()` already returns and what doctor already printed, so a contributor with several things to say says them as rows instead of inventing a delimiter the printer has to learn — and there is **no severity level**, deliberately, where `tui_status` has one, because §12 Q10 asks for the three columns *rather than* a colour: a colour is an invitation to skip the sentence, and the sentence is the whole of E1. **§4.8's table now has one home.** It was prose in the plan and about to become a second copy in doctor, which is the drift that is invisible because each copy looks right alone; it is `TIERS` in `containment.py`, doctor prints it verbatim, the test asserts it verbatim, and P6-06's docs row inherits one thing to check rather than two to reconcile. **Printing one rung would have been a lie about the shipped profile.** `rlm` puts the person's agent on `advisory` and its children on `worktree`, so a report showing only the root's table says "bounds: nothing" about a process where most of the writing is bounded — the report prints a column set per rung in force, which is what generalised `WorkspaceSeam.tier` into `effective_tier(child=)` (the property stays as the root-agent spelling its callers already had). Both halves of that reconciliation can only *lower* the answer — a chosen `advisory` declines a registered provider, an absent provider cannot deliver what was chosen — and `acquire` makes the same reconciliation by doing it, which is why nothing else is allowed a third spelling. Reported **effective, not configured**, with `tier (configured): worktree — not in force here` when they differ (E10). **The permissions-fs reading registers before the row's no-rules early return**, which is not a detail: a deployment that wrote nothing has a *wider* reach than one that wrote a deny list, so the empty case is the one most worth printing and was exactly the one an early return would have dropped — it also retires P4-06's own note that `ctx.fs_permissions` was "a bespoke name until P4-12 exists". **The worker model reading must not resolve the interpreter**: `environment()` shells out to `uv` to build the managed venv, and a diagnostic that costs thirty seconds and a network the first time somebody runs it is a diagnostic people stop running — an unresolved interpreter is reported as unresolved, which is also the more useful fact. **Doctor never composed a `Context` before this row**, so it could say where the log would go and nothing about what the process would be; it now mounts `--profile` and prints what the rows contribute, and a profile that *refuses* to mount is reported as a sentence with exit 1 rather than a traceback — `containment.strict` on a host with no backend is precisely the case someone runs doctor for (E8), which is the one place in the CLI a broad `except` is the right instrument. **A cleanup pass then deleted the ordering constraint this row had written into `base.yaml`.** Contributors registered with a `ctx.get("diagnostics")` at their own `apply`, copying `tui_status`' only existing consumer — which is safe there by accident rather than by design, since `insert:` can only append and every `tui_status` contributor is therefore downstream of a base row. Two of the four new contributors are *in* base, so the row had to be placed ahead of them and the file's opening promise that "row order carries no load semantics" annotated with an exception. `ctx.inject` is the mechanism that was already there for exactly this — optional where a plugin's own `inject=` list is mandatory, order-free because it settles on the reconcile fixpoint, and it hands back a child scope that disposes when the key goes — and `ph_rlm`'s kernel-snapshot row had already made the identical argument in writing: "the compaction seam is a nice to have here, and a deployment that removed the compaction row must not thereby lose kernel snapshots". One `diagnostics.contribute(ctx, ...)` now replaces the four-line get-then-register epilogue at four sites in three packages, the `scope=` nobody can forget any more, and the `base.yaml` carve-out; a test mounts the contributor *above* the seam and fails against the old shape. **The copies had already drifted**, which is the argument for the helper rather than a hypothetical: `permissions-fs` was the one site that omitted `order`, so it sorted at 0 and printed *ahead* of the containment section the design treats as the headline — found by running the command rather than by reading it. **`WorkspaceSeam.tier` is gone**: this row moved its body into `effective_tier(child=)` and left the property as a second public spelling with no production callers and a docstring still claiming to be what `ph doctor` prints, directly above a new docstring arguing that a third spelling would be a report disagreeing with the tree on disk. Four test assertions say `effective_tier(child=False)` instead. Two test helpers moved to `ph.testing` on the road `git_repo` and `StubWorkspaceProvider` took before them — `acquire_for_role`, whose "a child is a session stamped `origin: subagent`" encoding was the exact fact under test in two hand-maintained copies, and `report_section`, hand-unwrapped in three packages where two of them raised a bare `KeyError` on a renamed section. Using the first of them surfaced a gap: **no test covered a child agent's row**, the thing per-agent reporting exists for, so the workspaces test now acquires both roles and asserts the shipped posture end to end. Smaller: doctor's `--profile` option and its unknown-profile refusal were verbatim copies of the callback's, now one `ProfileOption` and one `_documents()` — and folding them in **introduced a bug the fold then exposed**, since `typer.Exit` subclasses `RuntimeError` and doctor's deliberately broad mount-failure catch swallowed the exit-2 refusal, printed the right sentence and followed it with the wrong one; resolution moved outside the catch, `typer.Exit` re-raises, and a test pins the code. The containment reading spelled "are there two rungs" three ways with an unwritten invariant that they agree, and `ID_MAX`'s comment argued for a shared constant that `_names` deliberately does not have. **One finding deferred:** `ContainmentUnavailableError` is a bare `RuntimeError`, so E8's refusal reads as a sentence in `ph doctor` and as a traceback in `ph -p --profile <strict>` — the person owed it most is the one whose *run* refused to start. That wants a named cordis error caught in the four mode paths, which is P4-11's surface rather than this row's. The efficiency review measured the seam at **+7 µs per mount** with all four readings closure-free bound methods, and confirmed against an empty cache that reading the code-runtime section does not build the managed venv. Original scope: `ph doctor`: effective tier, per-agent `kind` + `repo_writable`, permission reach, worker model | P4-11 | E9, E10 | outputs match the three-column tier table; a row contributes a reading without ph-app importing its package |
| P4-13 | **Landed** (`ph/system_prompt/memory.py`, the reader and row in `ph/seams/skills.py`, `ph/tools/builtin/subagent_task.py`). Three rows, and each one turned out to be about *where* something goes rather than what it says. **`memory-agents-md` is a `context()`, and that buys two properties at once.** Phase 1's `agent-instructions` registered `AGENTS.md` as a static prompt section and said in its own docstring that placement “lands properly in Phase 4”: G8's gate is that the prefix test survives a memory edit, and a section *is* the cached prefix, so every edit to the one file a user is most likely to touch mid-session would re-bill every token before it. The same move makes memory **live** — the old row read the files once at mount and baked them into a string, so editing memory did nothing until a restart, while a provider is asked per assembly. Cheap and live are the same change; the cache is why it is safe, not why it is worth doing. Per-agent too, through `fs.root_for(agent)`, so a child in a worktree reads the tree it is actually in (D21), and re-reading is a `stat` per candidate directory per turn that re-renders only when mtime or size moved. The gate is asserted in `test_prefix_stability.py`, where the prefix machinery already lives, and it **fails against a section** — verified by putting it back. **`skills-progressive` took the SKILL.md reader out of `ph-rlm`**, which P3-18 had already written down as the plan: its frontmatter was “validated to the format `skills-progressive` will read”. The seam now owns the format, G9's three limits, the catalog and a `skill` tool, and `rlm-skills-python` keeps only what is true in Code Mode — that a skill directory which is *also* an installable package gets built into the kernel venv. **The catalog is a provider over the registry rather than a fixed string**, which is what let the rlm row delete its own second catalog: its per-skill “callable in a cell” clause became a `hint` on the registered skill, so one prompt lists every skill once with every way to use it, and neither row has to mount before the other. The 10 MiB limit is enforced twice deliberately — the scanner refuses a file that large and `body()` refuses to *return* one, because the window between discovery and a read is exactly where a skill directory that is a checkout gets `git pull`ed. **`subagent-task` is the blocking half of delegation**, for deployments with no inbox: `rlm_run` admits a child and collects it later, which needs a roster, a message protocol, and a model that knows to check back. Both shapes are real — a fan-out wants the handle, one sub-answer wants the wait — so the rlm bundle **disables this row** rather than putting two ways to do one thing in one prompt. A child that ends as anything but `done` is a *failure* rather than a value with an empty `answer`: a parent reading `answer: ""` as “it found nothing” is a wrong conclusion drawn from a delegation that never ran. **This row surfaced a rule the other two then adopted.** A tool advertised in every prompt and refused on every call spends the context window teaching a capability the deployment does not have — so `task` registers only where a subagent provider is mounted and `skill` only where a skill is installed, both at `profile/mounted` so a provider or a skill contributed by a row *below* still counts, and `headless` consequently ships neither. Writing that check found two defects in it: a configured `provider` name was trusted without checking it was mounted — the phantom arriving by the one route that looks deliberate — and a second `profile/mounted` dispatch double-registered. Both fixed and pinned. `StubSubagentProvider` joined `ph.testing`, since the seam had no core-side fake at all and nothing outside `ph-rlm` could test a caller of it, along with `write_skill` now that two packages test one on-disk format. **A cleanup pass then found the memory row doing the opposite of what its own docstring promised.** It said “a `stat` per candidate directory per turn… ‘memory is live’ must not mean ‘every turn re-reads 64 KiB from disk’”, and the code called `discover()` — which opens and reads every `AGENTS.md` — *before* computing the signature it compared. So the cache saved a `str.join` and paid an extra `stat` per file for the privilege; measured, it was 19 stats + 3 opens + 18 KiB read per model **step**, and 97% of the entire cost of prompt assembly. Split into `locate` (paths and `stat` facts, no reads) and a read pass that runs only on a miss; the read is now `handle.read(MAX_BYTES)` rather than `read_text()[:MAX_BYTES]`, which was reading a 5 MiB file to keep 64 KiB of it — 2.87 ms per step in the measured case — and `$PH_HOME` is resolved once at mount instead of per assembly. The cache is bounded now too: its key is a *workspace root*, so a daemon fanning out children would have retained a rendered snapshot for every worktree it ever saw. **The test that guarded all this asserted the wrong number**: it monkeypatched `read_text`, asserted `reads == 1` — that the file *was* re-read — under a message written for `reads == 0`, and passed identically with the cache deleted. It now asserts zero opens, which is the assertion that distinguishes the two designs. **The two rows had already grown two copies of the same gate, and diverged on the first copy**: `subagent-task` carried a `registered` nonlocal, `skills-progressive` carried nothing, so under a second dispatch one would crash and the other would not. Both now call `tools.register_when_composed(ctx, build)`, which owns the subscription, the `scope=ctx`, and a guard that is *derivable* (`ctx.tools.get(name) is None`) rather than a flag — the move `diagnostics.contribute` made one seam over, for the same reason. That widened `profile/mounted` from “a row may refuse the deployment” to “…or collect what it turned out to contain”, so the declaration says both now, including the rule the shared `serial` dispatch imposes on the new use: **collect by side effect and return `None`**, because a collector returning a value would silently skip the refusals registered after it. **The catalog was naming a tool that need not exist**: it told the model to read instructions “with the `skill` tool” while the tool registers only where a skill is installed, so a skill registered after mount produced a catalog pointing at nothing. `render_catalog` takes `tool=` and the provider passes what is actually registered. **“Which provider when none is named” moved to the seam** as `SubagentSeam.resolve`, since it is a property of that provider table and not of a tool module; the same review found `ph_rlm/bindings.py` still trusting its own `provider:` knob unchecked — the phantom arriving by the one route that looks deliberate — which is left open as the only skipped finding, because changing a shipped row's behaviour is outside this diff. Smaller: the two new tools had raised two different error types (Code Mode's `ToolCallError` and a bare `ValueError`) and now raise one; `hint` was the only `Skill` field `register` did not bound; the rlm row registered into a shared registry without `scope=ctx` and injected `system_prompt` it no longer uses — a real activation precondition, so in a profile without that seam the row would silently never mount; `PythonSkill.directory` became derived; `_discover` became a `partial`; and four modules still named the deleted `agent-instructions` row. In the tests, two byte-identical `_text` helpers became `text_of`, three hand-rolled frontmatter strings became the `write_skill` this very change had hoisted to avoid them, `_write` became `write_text_under`, and the `subagent-task` helper stopped taking a dead parameter and started serving the two tests that had been repeating its body. Original scope: `memory-agents-md` (after caching), `skills-progressive`, `subagent-task` | P1-07, P1-23 | G8, G9 | prefix unchanged by a memory edit; skill body absent until requested |
| P4-13b | **Landed** (`SkillRestriction` and per-scope reads in `ph/seams/skills.py`; `check_grant`, `apply_grant`, `Preset` and the `subagent-presets` row in `ph/seams/subagents.py`; selectors on `task` and `rlm_run`). **The ruling shaped the mechanism, not just the policy.** The plan text had said to make skills visible per owner the way `SystemPromptService` does — but owner-scoped *registration* is a way to give one agent something another cannot see, which is precisely the widening the ceiling forbids. So the seam grew **narrowing only**: `SkillRestriction(allow, deny)` intersecting along the scope chain, `ToolRestriction`'s shape for the same question asked of a different registry. There is no mechanism to hand a child a skill its parent lacks, so the widening case is not guarded against — it is absent, which is the strongest form the rule can take. Installation stays global and `register`'s `scope=` stays a *lifetime*, said out loud in the service docstring because the two readings look identical at a call site. **The seam refuses and the provider narrows**, and the split is deliberate: `check_grant` runs in `SubagentService.start`, the one path every delegation takes, so a ceiling no provider can skip; `apply_grant` runs in the provider, because the child's scope is the provider's to create. A provider that forgot to call it yields a child holding *the parent's* set — wider than the spawn asked for, never wider than the parent — so the failure mode of the split is a lost preference, not a lost boundary. **Writing it found a hole the ruling would otherwise have had at runtime.** `AgentRegistry.create` scopes every agent under the *registry*, so a child agent's scope is its parent's **sibling**, not its descendant, and a parent's restriction does not reach it. The first draft applied nothing when a spawn named nothing — which would have handed the child of a *narrowed* parent the deployment-wide set. Inheritance is therefore materialized: `None` means "everything the parent holds" and is written out as that explicit allow-list. It has its own test, which fails against the earlier version. **A named skill is direction.** Its body goes into a `subagent:brief` section on the child's scope — G9 inverted for the one case where the question G9 defers is already answered, since a child spawned *for* `code-review` will need that one, certainly, and making it spend a turn fetching what it was created to do is a wasted call and a real chance it never fetches. Everything else it may still reach stays catalog-only, and the catalog itself is now per-agent, so a narrowed child is not advertised skills it would be refused. **Presets have no prompt field**, which was the one design call worth making twice: a preset with standing instructions and a directing skill are two channels saying what a child is for, competing where they disagree and duplicated where they do not. A preset binds a name to capability — `reviewer` is `skills: [code-review]` — and what a reviewer *does* is written once, in the skill, where a human edits it. Presets fill only what a caller left unsaid, an unknown name is refused rather than ignored (`_resolve_model`'s argument: a spawn that asked for a `reviewer` and silently got a generic child is a result the parent cannot interpret), and the resolved request goes through the same ceiling — so a preset naming what the parent lacks is refused like any other spawn. Left for a later row: the `skill` tool's gate is still process-wide, and the RLM's importable half still reaches every kernel through the boot frame. **A cleanup pass then found the ceiling had a hole in shipping code.** `RlmChildProvider.rehydrate` builds a **fresh scope** for a settled child from its retained options — and narrowed nothing. The filters bounding the old scope were effects of it and went with it, so a child that outlived its own restriction came back holding the whole deployment: the one thing the ruling forbids, reachable from a public method, and invisible to every test because they all drive `start`. It also falsified this row's own docstring, which had claimed a forgetful provider would lose "a preference, not a boundary". **So enforcement moved into the seam.** `SubagentRun.scope` is handed back by the provider, the seam applies the grant on both paths, and `Grant` — the materialized `(skills, tools, brief)` — rides on the run so a replay needs neither the parent nor the scope that is gone; `_Child` already kept the child's *options* for exactly that reason, and the capability half needed the same treatment. A provider that hands back no scope is **refused, but only when the spawn actually narrows** — a deployment where nothing is restricted keeps working with any provider, and the moment narrowing matters, a provider that cannot deliver it is refused rather than silently ignored. The rehydration test fails against the previous version. **The restriction table was O(skills × every restriction in the deployment).** A flat list scanned per read meant a fan-out of sixteen children put sixteen filters in front of the *parent's* own catalog, none of which could change what it sees: measured, the parent's prompt assembly ran 3.9 times slower per model step, and the whole delta was that scan. Re-keyed by `owner.isolation` and memoized on a generation counter — `ToolRuntime.view`'s arrangement, for its reason — a read walks `isolation_chain()` and nothing else: **262 µs → 2.3 µs** for the parent, 271 µs → 0.7 µs for the child. **The brief was re-read from disk on every model step**, because it was a provider closure calling `body()` inside a `PromptSection` — the *cached prefix*, which a filesystem hit makes neither static nor free, and a skill body may be 10 MiB. Rendered once at admission into a literal string, with a test asserting zero opens across three assemblies. `SkillRestriction` and `ToolRestriction` collapsed into one `NameFilter` in `ph/seams/_restriction.py` — `_names.py`'s argument about a regex, applied to a rule — while the two *registries* stay separate, since one resolves layers with shadowing and a reserved transport and the other is a flat table. `restrict`'s disposer released by `==` rather than identity, so two children narrowed to the same set would have one release the other's filter. Smaller: `Preset.access` could never be distinguished from its default (both tools default `access` to `"read"`) and had no test, so it is gone; `Preset`/`PresetService` became `SubagentPreset`/`SubagentPresetService`, matching `PermissionPreset` next door, and the service says out loud why it has no registration path; `ORDER_BRIEF` claimed "after the skills catalog" while being an independent offset from a third constant, and is now `ORDER_SKILLS + 10`; "what the parent holds" was computed in four spellings and is now `held_by` once per spawn; and the catalog's claim that a `skill` tool exists is asked per agent, so a child narrowed away from that tool is no longer told in prose to call it. In the tests, `_skill`/`_parent` became `ph.testing`'s `skill()` and a sync `_agent`, the child scope is now built the way `agents.create` really builds one — a **sibling**, not a descendant, which is the whole premise of the ceiling — and `test_naming_nothing_inherits_the_parents_set` was deleted: stubbing `restrict` to a no-op left it passing, so it could not fail for the reason it named. Original scope: **Delegation carries capability, not just instruction — and a child never holds more than its parent.** Today a spawn carries `prompt`, `model`, `access` and nothing else, so every child of a deployment is equipped identically and steered only by prose. Three gaps, deliberately not the same size. **(i) Tools are already scoped and simply unwired.** `ToolRestriction(allow, deny)` with `restrict(scope=)` exists and restrictions *intersect*; what is missing is a selector on `SubagentRequest` and a provider that applies it to the child's scope. **(ii) Skills are not scoped at all.** `SkillService._skills` is one flat dict; `claim_key` records an owner and uses it **only for disposal**, so `list()` answers the same for every agent, and the catalog section is registered at the row's scope so `_visible`'s `owner.reaches(target)` passes for all of them. The fix is the shape `SystemPromptService` already uses one module over — hold `(owner, skill)` and give `list`/`get`/`body` the scope being asked about, which the catalog provider has on `AssembleContext` and the `skill` tool has as `run.agent`. **(iii) A directing skill wants its body in the prompt, which inverts G9 for exactly one case.** G9 keeps bodies out because a catalog of twenty is twenty bodies the model probably will not need — but a child spawned *for* `code-review` will need that one, certainly, and making it spend a turn fetching what it was created to do is a wasted call and a real chance it never fetches at all. So a skill **named at spawn** is materialized into the child's first prompt while the rest stay catalog-only: G9's own rule applied where "will this be needed" is already answered. **The ceiling is absolute, and it is the whole security content of the row. A child gets a subset of its parent, always — there is no elevation, no preset that grants beyond the selector, no flag.** To give children a skill or a tool, grant it to the *parent* first; that is the workflow, stated rather than worked around. Applied transitively it means the root agent's grant bounds the entire tree and capability is monotonically narrowing down it, which is the property that makes a fan-out auditable from one place. A **preset** — the vocabulary P3-16 already reached for when it rejected a `subagent` knowledge entry as "a preset — capability, which I7 puts outside the knowledge layer", with `permission-presets` (P1-19) as the working precedent — is therefore a deployment-authored *menu*, never a grant: naming one whose entries the parent does not hold is **refused, not silently intersected**, on `_resolve_model`'s existing argument that a child which answered on something other than what was asked for "is a result the parent cannot interpret" — a `reviewer` child missing its review skill does the job wrong and reports success. **Enforcement belongs at the spawn, because the registry deliberately provides a widening path**: `_build_view` filters global names only, so "an agent's own registration cannot be masked out from under it" — a tool registered on the child's scope is unmaskable and invisible to the parent's restrictions. So a spawn may **only narrow**: it selects from the parent's own view through `ToolRestriction`, and never registers on the child's scope. **Two things to settle rather than assume.** The `skill` tool's gate is process-wide today (`register_when_composed` asks "is any skill installed at all"), and per-agent skills make that answer wrong per agent, so the gate becomes scoped or P4-13's phantom-tool rule quietly breaks for the child that has none. And **the RLM half cannot follow**: `runtime.skills`/`skill_modules` are baked into every kernel's boot frame, so "callable in a cell" is process-wide by construction — this row scopes instructions and tool *visibility*, and scoping the importable package is a boot-frame change to price separately. One consequence to watch, not a reason to weaken the rule: the root ends up holding the union of everything any descendant may need, which widens its own prompt and surface. If that becomes the binding cost, the move that keeps the invariant intact is a capability the parent *holds but the deployment marks non-invocable for it* — the parent still holds it, so nothing is elevated — and that is a separate row, not a flag on this one. Not chosen: per-agent discovery of `<workspace root>/.ph/skills`, which composes with the worktree tier for free but makes a child's capabilities a property of a directory it can write — the wrong side of I7. | P4-13, P4-08, P1-19 | G9, I7, B7 | a child spawned with `skills`/`tools` sees only those, and a named skill's body is in its first prompt; a spawn naming anything the parent does not hold is refused, preset or ad hoc; a grandchild cannot exceed the root; a spawn that names none is byte-identical to today |
| P4-14 | **Landed** (`workspace_leaks` and the `workspace-reconcile` row in `ph/seams/workspace.py`, `GitWorktreeProvider.reclaim`). **The pair earns its keep by answering a question the filesystem cannot.** A crashed process leaves a worktree on disk, and so does the disposal policy when it keeps a dirty tree for review — `git worktree list` reports them identically, so `/workspaces` cannot tell them apart and neither can a directory walk. The log can: a `disposed` with `kept: true` is the policy deciding, and no `disposed` at all is a process that died holding the tree. That asymmetry is the whole of F6, and it is why the seam writes both halves rather than each provider writing one. **Folded, not tracked** — `subagent_roster`'s reason one seam over: the answer must be computable from a log being read off disk by a process that was not running when it was written. Keyed by agent so a re-acquire after a release reports only the live tree, and filtered by `fresh_root(kind)` so an unclosed `shared` pair records a crash and no stray — its root *is* the base, and the person's own checkout is the last thing this row should touch. **On `session/created`, which is also the resume path**: `sessions.adopt` publishes through it, so a session coming off disk meets the same listener as a fresh one and a fresh one has no events to fold — one mechanism rather than a resume-only hook a second way of opening a session would quietly miss. Detached, because `emit` schedules an async listener and does not wait: reconciliation runs `git` per leaked tree and a person reopening a session should not sit behind it, with `ctx.drain()` as what a test or a shutdown uses to know it settled. **Reclaiming is a second Protocol**, `ReclaimingProvider`, on `RehydratableProvider`'s argument in the subagents seam: not every tier can release a tree from a log record, and a `getattr` probe would report a provider whose method drifted as "cannot reclaim" — the failure mode that *hides* a leak rather than closing it. **A crash is not a reason to throw away work**, so reclaiming runs the same disposal policy an orderly release runs: clean is removed, dirty is kept. That needed `_dirty` and `_release` to accept "no `Workspace` value", which is the honest reconciliation case — nothing is known to have been provisioned, so every file counts as the agent's work and the error goes toward keeping, where the cost is disk rather than the work. **A leak this profile cannot reclaim is reported and left alone**: the tree belongs to a tier that is not mounted here, and removing a directory on the strength of a record written by a configuration we are not running is the one way this row could destroy what it exists to protect. The pair closes either way, including when the tree was already gone, because a leak reported and left open is one reported at every future open — noise that hides the record that matters. Four of the nine tests fail with the row disabled, including the gate. **A cleanup pass then found the row would have deleted a live worktree.** `SessionStore._publish` serves three shapes, not two: `create`, `adopt` — and **`fork`**, which seeds the child with the parent's transcript. So a fold over the whole log reported the parent's still-held `workspace/acquired` as the *child's* leak, and reconciliation would have run `worktree remove --force` against a tree an agent was actively working in. Confirmed by driving a real fork before changing anything. Two guards, each with its own precedent: the fold now starts at `session.header.seed_length`, which is `Inbox`'s line verbatim and its argument too — what a session inherited is not what it acquired — and the seam skips any record it is **still holding**, which is the question `live()` exists so `/workspaces` can ask *before it offers to delete a directory*. Both are pinned, and both tests fail against the previous version. That defect was downstream of an altitude error, so the fix moved with it: **reconciliation is now `WorkspaceSeam.reconcile`, not policy in the row.** Both facts it needs are on the seam — `_held`, and the payload shape — and the row had been hand-building a `workspace/disposed` two hundred lines below the `_payload` whose docstring says the pair is "spelled once", giving the durable pair a second writer in the half nobody watches. `_payload` is now the module function `pair_payload`, called by both. The row is three lines and a catch-up pass over sessions that already exist, matching `session-persistence-jsonl`'s reason for having one. **`WorkspaceRecord` now locates its own repository.** `reclaim` took the base from the reconciling process's `ctx.fs.root` — a guess, which made the record's claim to be "the log's own fields" untrue of the one field the reclaim actually needed, and would resolve the wrong toplevel for a pair recorded against a different checkout. A linked worktree knows its own `--git-common-dir`, so the tree answers the question about itself and the row stops injecting `fs` at all. **Two measured efficiency fixes.** Reclamation ran sequentially where the sibling `/workspaces` deliberately fans out and says why — 8 leaked trees took **2364 ms sequential, 1165 ms concurrent**, and a crash mid-fan-out leaks exactly one tree per child. And `reclaim` ran the most expensive `git status` form there is for a boolean: `--untracked-files=all` with no pathspec to refine, **20 000 lines and 589 KB of stdout decoded to compute one bit** on a provisioned tree. Both fixed; together the common case goes 331 ms → ~16 ms. The `Workspace \| None` threaded through `_dirty`/`_release` became an explicit `pathspec: Sequence[str]`, so "everything counts as the agent's work" is a value the caller states rather than a sentinel the callee decodes — and it is what selects the cheaper `-u` mode. Smaller: the fold tests the event type before reading `agentId`, as `fold_subagent_event` does and for its reason; the two event names are constants now that five sites spell them; the `isinstance` check left the per-record loop; and the tests use `ph.testing.worktree_agent` instead of reassembling it three times, plus one `_reopen` helper for the three facts a reconciliation test must get right. One test was replaced rather than kept: it claimed to cover "nothing on disk to remove" and never removed anything, so the already-gone branch had no coverage at all — it now deletes the tree first and asserts the pair closes with `kept: false`. Original scope: Paired-event reconciliation at session open (`workspace/acquired` without `disposed`) | P4-08 | F6 | crash between acquire and dispose reconciled on next open |
| P4-15 | **Landed** (`ph_app/profiles/rlm-stable.yaml`, `docs/dev-notes/phase-4.md`). **Two of the three deliverables were already built.** The context-usage footer and the todo sidebar landed in Phase 2's TUI and have been folding `todo/write` and reading `TokenBaseline.pressure` since — the sidebar even carried a docstring saying "and (from Phase 4) the todo list" while already rendering it. Checked rather than assumed, and left alone; a row that re-implements what exists is worse than a row that reports it. **What was missing is the profile.** `rlm-stable` is the first that composes **both** capability bundles at once, so it is where their rows meet for the first time — Code Mode's transport beside the tool gates, one `permissions-fs` over both, one containment tier under both — which is why "boots" is a real gate rather than a formality, and why the test mounts through the actual resolution rather than a hand-assembled row set. It turns on the rows the two bundles deliberately ship **disabled**: `tool-todo` and `rlm-context-loader` each name this profile in the comment that says so, and each would otherwise make "I want offload" mean "and also a tool, and also a corpus". `minChars: 200000` is Q4's own threshold, so a small corpus behaves conventionally. `containment` is `advisory` for the person and `worktree` for children — the shipped posture P4-11 argued for, where the fan-out is the hazard and the person would be surprised to find their own agent editing a copy elsewhere. **The human gate names two calls, not everything**, because a harness that asks about everything teaches its user to approve without reading: `bash` and `run_code`, matched on the *arguments* that reach past what a worktree can undo — recursive delete, push, publish, sudo, curl-into-shell, `subprocess`, `rmtree`. `run_code` withholds `edit`, since a program is not something to hand-patch in a modal and a half-edited one is worse than either answer; everything else is left to the workspace boundary `permissions-fs` already enforces. The notes are written as five things worth knowing rather than a row-by-row recap — the phase's one recurring rule (a tier's name must not promise more than it delivers), the child-never-exceeds-parent ruling and the two consequences that fell out of implementing it, placement as a design decision, the pair that distinguishes a leak from a feature, and the six defects the *measuring* reviews found that the reading ones did not. **A cleanup pass then found the human gate was inert.** A profile writes `run_code` because that is the *reserved* transport name — unregisterable, unshadowable, unrestrictable — but `ToolRegistry._build_view` renames the transport **in place** to whatever a presentation row calls it, so under `rlm` the name reaching `tools/pre-execute` is `ipython`. `hitl` looked the rule up literally, so the entire Code Mode half of this profile's approval gate never fired: the config parsed, and the test passed, because it asserted the YAML had round-tripped rather than that anything asks. `hitl` now resolves the reserved key against the live `transport_name` — a profile naming `ipython` instead would turn its own gate off the day a deployment renamed its presentation — and the test drives a real `ipython` call and asserts a human was asked, failing against the previous version. **The pattern list was a divergent retype of a shipped constant.** `ph_stabilize.hitl.DESTRUCTIVE_PATTERNS` is curated, exported and tested, and was reachable only from Python — so the first profile that wanted it hand-copied a subset that had *already* disagreed with it on its first day: it widened `git push` from force-only to every push, against `test_hitl.py`'s own pinned case that an ordinary push is ordinary, and dropped the `wget`, `dd`, `mkfs`, `chmod -R` and SQL entries. `Rule` gained a `preset:` field resolved against a `PATTERN_SETS` table, so a profile *names* the set and adds only what is specific to it — a typed field on a row's config rather than widening the loader's deliberately closed interpolation vocabulary (I-8). **The `containment` row was a no-op duplicate**: the `rlm` bundle already sets `advisory`/`worktree` with the same reasoning, so the profile was a second writer of one posture that would silently pin the old rung the day the bundle moved — deleted, along with a `mode: auto` that restated the shipped default. **The test moved to the repo-level `tests/`**, because it composes `ph-app`'s profile over two bundles and no one of the three depends on the other two; in `ph-rlm`'s suite a stabilize regression would report as an rlm failure, and that suite would break in any environment holding only one of them. Three change-detector assertions went with the move — reading back YAML values written six lines away exercises nothing — and the relocation dropped the `$PH_HOME` isolation fixture, caught by the same pass and restored. `RLM_LAYERS` joins `TUI_LAYERS` so `rlm-stable` derives from `rlm` rather than re-listing it under a comment claiming it is that. The efficiency review measured the rest and found nothing worth changing: 11.6 ms per composition (all YAML parse, 0.1% of the suite), +4.2 µs on `available_profiles()`, and the pattern set linear rather than backtracking — 478 µs over 100 KB containing 200 non-piped `curl`s, with the obvious alternation optimization measuring *slower* above 1 KB. Original scope: `rlm-stable` profile; context-usage footer + todo sidebar; `dev-notes/phase-4.md` | P4-01 … P4-14 | — | profile boots |
| P4-17 | **Landed** (`ph/seams/tui_screens.py`, `ph_app/tui/screens.py`, `ph_app/tui/trajectory_screen.py`). `ctx.tui_screens` is the seam, `PHTuiApp` stays the shell, and `TrajectoryScreen` is its first registrant — composed by the standalone `--mode trajectory` app *and* by the chat, so §5.5's "(a) is a strict subset of (b)" holds by construction again. Nine departures from the written design, each with its reason in `docs/dev-notes/screen-registry-design.md`; four are worth naming here. **The seam is in `ph-core`, so `build` is `Callable[[Any], Any]`** — a plugin depends on ph-core alone (ph-rlm does not depend on ph-app) and `test_layering.py` forbids Textual there; core owns the presentation vocabulary and ph-app owns the pixels, exactly as it already does for `ToolCallView`/`CardKind`. **The seam grew a *presenter*, because the gate was otherwise untrue**: `claim_key` makes the *screen* an effect of its row, but the slash command and the key are made by the front end and the app's own disposer list would have outlived that row — so `present_with` attaches a front end and every presentation is owned by the **registration's** scope, `add_disposer`'s idempotent release letting one teardown belong to two lifetimes (row unload, front-end detach). The registrant passes `scope=ctx` for this to hold, which is a rule rather than a mechanism — **P6-12** is the general form: it is the only one of 38 registration call sites in the tree that passes it, so every other seam has the same gap. **`key` is a default key string, not a `TuiKeybindings` field name**, because a plugin cannot add a field to that dataclass and a field name would have made plugin screens the one class of key nobody could rebind; `TuiKeybindings.extra` carries unrecognized binding ids from `tui.json` straight into `set_keymap`, and a test drives the rebinding through the file. **Cross-navigation reads the join in both directions**: `RevealSeq` (screen → shell) and the `Revealing` protocol (shell → screen) over `source_seq`/`seq`, falling back to the nearest preceding row because a `request/header` is a record with no transcript row by design; transcript → record uses the topmost *visible* row rather than a focused one, since the transcript is widgets in a scroll and inventing row focus is more than this row. The registry row is in `ph-base` as designed; the *registrant* could not be, because `base.yaml` may not name a ph-app plugin without breaking a ph-core-only install — it is inserted by `profiles/tui.yaml`. Found by building it, both in code this row only *reached*: `build_trajectory` asked `is_fork_boundary` per record, which rescans the prefix — 467 ms to fold an 8 000-event session, previously paid once at `--mode trajectory` startup and now on a keypress, so `ph.session.fork_boundaries` answers the rule for the whole log in one pass (A6 stays in `store.py`, and a test pins the two statements to each other); and `TranscriptView.sync` re-armed the bottom anchor on **every** dirty frame, so Textual's release-on-scroll was undone by the next event — a jump to a record would have lasted until the next chunk arrived, and scrolling up in a live conversation never held. Original scope: **`ctx.tui.screens` — the front-end's registration seam, and `TrajectoryScreen` as its first registrant.** Ported from dsh's slot service (`packages/client/ui-slots`), whose ~35 `ui-*` packages contribute into named slots — `conversation.view`, `conversation.composer`, `sidebar.workspaces`, `settings.section`, `tool.call.toolview`. The shape to copy is narrow and exact: `ctx.slots.register({name, id, order, label, inject})`, and *"the registration rides the slot service's effect wrapper, so plugin unload removes the tab"* — I2 applied to the front end. **Direction matters**: dsh's `ui-trajectory` registers into `conversation.view`; the conversation is the host and the trajectory is one tab at `order: 10`. pH keeps that relationship — `PHTuiApp` stays the shell. Deliverables: `ScreenDefinition {id, label, order, build, key?}`; `TuiScreenRegistry` over `claim_key` (which already gives the identity-checked disposer this needs); one generated `TuiVerb` + remappable binding + palette entry per registered screen, so a plugin gets `/trajectory` and a rebindable key without touching `TUI_VERBS`; `TrajectoryScreen` extracted from `TrajectoryApp` so **both** compose it — the standalone `--mode trajectory` entry keeps its nothing-mounted property, which is the whole reason shape (b) was chosen; and `sourceSeq` cross-navigation in both directions, which §5.5 lists as a shape-(b) capability and P3-25 did not build (the join is stored on both sides — `TrajectoryRecord.source_seq` and `ChatItem.seq` — and read by neither). Restores §5.5's "(a) is a strict subset of (b)", which P3-25's separate-`App` deviation broke | P2-01, P3-25 | I1, I2 | a plugin row registers a screen and gets a verb, a key and a palette entry; unloading the row removes all three; the trajectory opens over the chat and returns to it; a record jumps to its transcript row and back; `--mode trajectory` still mounts nothing |
| P4-16 | **Landed** (`ph-core/tests/test_containment_ladder.py`). **The module tests the claims, not the mechanisms.** Every other workspace module pins behaviour — `test_workspace_git.py` drives real `git worktree`, `test_workspace_lifecycle.py` pins who acquires and when, `test_containment.py` pins which rung a role gets. This one pins the *sentences*: what `ph doctor` prints and what §4.8's table says, because those are what a person reads before deciding what they are protected from, and a claim is the one part of a design that rots without any code changing. **The load-bearing test asserts an escape.** A raw absolute-path write from inside a worktree lands outside it, and that is asserted on purpose: it is the difference between collision isolation and confinement, and the day someone "fixes" it at this rung without moving to `sandbox`, the tier table becomes a lie. The failure message says so — *if this now contains, the table and `ph doctor` are wrong and E13 must be revised* — because the wrong repair is to delete the test. Paired with an assertion that `TIERS["worktree"].does_not_bound` still contains the clause the escape proves, so the prose and the behaviour cannot drift apart; mutating that string fails the module. **The writes go through `ctx.shell`**, a real subprocess doing a real `open()`, never through `ctx.fs`: a seam-mediated write proves the seam, and what is under test is precisely what happens *beside* it. The other four claims are stated as outcomes rather than path arithmetic — two agents writing `shared.txt` get two files, an ephemeral tree is discarded *with work in it* while its scratch survives, `$TMPDIR` as the subprocess actually sees it points into scratch, and doctor reports `worktree` against a real tier rather than a stub. **Two things the writing found.** `agent.id` *is* the session id, so two agents created on one session are one identity to everything keyed by it — the workspace registry included, which is why a real spawn creates a child *session*; the first draft's fan-out test had both siblings acquiring the same directory. And a workspace is only unwound by disposing an agent if `acquire` was given `scope=agent.ctx`, which is what `workspace-lifecycle` passes and what a hand-rolled acquire in a test has to remember — the ephemeral-discard claim is false without it, which is I2 showing its teeth. **A cleanup pass then found the load-bearing test was measuring the wrong thing.** It made its raw write through `ctx.shell.run(agent=...)` — and `ShellService.run` applies `workspace_policy(workspace)` whenever a sandbox backend is available, so the escape would have passed or failed on *what the profile mounted* rather than on the rung. Worse for the pair this row sets up: P6-06's "same write, refused" would then have passed because of that branch instead of because of `sandbox`, and the two halves together would have measured the shell seam rather than the ladder. The writes are now bare `ctx.subprocess` spawns — no `agent=`, no policy — which is what the tier sentence is actually about and is the shape `test_workspace_git.py` already uses. **Half the module restated claims its owning module already pins**, which made the module's own stated rule untrue of itself: the scratch, redirection-env and fan-out tests were cut (each duplicated `test_workspace.py` or `test_workspace_git.py`, one of them asserting `workspace.env == redirection_env(workspace.scratch)` — a function equal to itself — and another an `or` whose left branch can never be true, since `Path(x).parents` never contains `x`). What survives is one test per column of §4.8's table plus the two that only this module can make. The ephemeral test **stayed** against two reviewers' advice, on the third's concrete point: the sibling disposes through `ctx.workspace.dispose(agent_id)` while this one goes through `ctx.agents.dispose`, which is the I2 unwinding a live deployment uses and the only route that shows a workspace is an effect of the *agent's* scope. The doctor test dropped its repository, commit and worktree — `effective_tier` reads the registered provider and never observes an acquire, so "a tier that actually served" was scenery costing 33 ms and a `@needs_git` skip; it is now a mount and one assertion, the only one `test_diagnostics.py` cannot make since that registers a stub. Smaller: the diagnostics report was hand-unwrapped for the **fourth** time when `report_section` exists precisely to stop that (and had been hoisted two rows earlier for the same reason); `"confinement" not in worktree.buys` was a negative substring check with E13's polarity backwards — it would trip on a rewording that made the sentence *more* honest — and is now three positive claims; and several assertions that could not fail independently of the line after them were removed. **One finding deferred as a plan row rather than a comment:** a workspace acquired without `scope=agent.ctx` is an effect of the *seam* and silently outlives its agent, which is a real leak anywhere but `workspace-lifecycle` — `acquire` takes `agent_id` already and could derive the scope or refuse without one. That belongs in P6's sweep beside `claim_key`'s owner default (P6-12), not in a test-only row. Original scope: **Containment test module**: `worktree` bounds relative writes; **absolute-path `open()` escapes `worktree`** (asserted, so E1 cannot regress — the `sandbox` half lands in P6-06); ephemeral discard; scratch; redirection env; doctor effective tier | P4-08 … P4-12 | E1, E13 | suite green |

### Phase 5 — Long-running *(3–4 weeks)*

| ID | Work item | Depends | Delivers | Gate |
|---|---|---|---|---|
| P5-01 | **Landed** (`ph_app/daemon/{framing,supervisor,server,client}.py`, `PathRoots.daemon_socket`, `ph daemon`). **The row's whole content is one inversion.** Every mode before it ties an agent's life to a connection — `ph -p` exits with the turn, the TUI's root dies with the terminal, `--mode rpc` lives as long as stdin — and Phase 5 is about a run that takes an hour and must not stop because a laptop lid closed. So a root owns a mounted profile, a session, an agent and a queue, and **its own `anyio` task drains that queue with no reference to a client at all**: nothing in `Supervisor._run` mentions a connection, which is what makes "TUI close leaves the root running" a property rather than a promise. Attaching subscribes a connection to the root's events and detaching unsubscribes it; neither starts nor stops work. **Two roots are two deployments**, each with its own `Context` — sharing one would make a row's `ctx.provide` visible to a root that never asked for it, and separate sessions, workspaces and kernels are what "one task per root" has to mean to be worth anything. Watchers are held **on the root, by token** rather than on the connection by identity: the root is what is still there between attachments, a client may attach twice and detach once, and a subscriber whose socket died is *dropped* rather than raised through — a watcher must never become the thing the work waits on, which is the same inversion one layer down. Writes go through a single per-connection pump task so a notification arriving mid-reply cannot interleave two frames, and a client that cannot keep up is dropped from the root rather than allowed to slow it. **The socket is state, and stale state is a lie**: a path left by a crashed daemon makes every client hang on a connect nobody will answer, so binding removes an unresponsive one — while a *responsive* one is another daemon, refused rather than stolen, because two supervisors both believing they own this user's roots is I-5's question and P5-03's lease to arbitrate. `0o600` on the socket for `$PH_RUNTIME`'s own reason: it carries every command this user's agents will take. Framing is JSONL, `ph.session.dumps`, bounded at 8 MiB — a stream socket hands you bytes rather than messages, and the daemon is the one process in pH that outlives whatever talked to it, so an unterminated peer must become a refused connection rather than a dead process. The method names extend `--mode rpc`'s shape rather than opening a second vocabulary, so P5-02 adds capabilities and cursors to *this* surface. Six tests over a real socket with real frames — no in-process shortcut, since the transport is what the row delivers and a fake one would agree with whatever the code did — and the gate test disconnects the client entirely before a second one finds the root still running with the turn it was queued. Left to their rows: capabilities and `{generation, sequence}` cursors (P5-02), the lease (P5-03), the retry ladder where a failed turn currently just logs and keeps the queue (P5-04), passivation (P5-05). **A cleanup pass then found the suite writing into the developer's real `$PH_HOME`** — five session logs under fixed ids that later runs append to — because `PH_HOME` was pinned by *opt-in* fixtures (`mount`, `make_tui_app`) and the daemon tests mounted through neither. That is the **fourth** appearance of this class here (a test repo in the checkout, a stray `parent-tree`, a real `ph/*` worktree, now sessions), and each earlier one was fixed where it happened; the strays were removed and the rule is now an **autouse** fixture in the root `conftest.py`, the one place no test can route around. Something else had been leaking there for days, which is how long the opt-in version had been quietly insufficient. **Three things the code claimed and did not do.** `shutdown` set a flag nothing read, so `serve`'s "runs until shutdown" and the CLI's "blocks until a client sends shutdown" were both false and every test cancelled the scope by hand; it is now an `anyio.Event`, and — because a client awaiting a reply would wait on a frame the daemon is concurrently losing the ability to write — it is sent **without an id**, which is also the honest shape for "stop". The falling-behind watcher was announced as dropped and never dropped: `notify` caught `WouldBlock`, logged, and returned, so the subscriber stayed and re-paid the whole fan-out for every later event; the policy now lives with the subscriber list, on the root. And `MAX_LINE` bounded the accumulated buffer rather than one frame. **The prompt queue was a second, non-durable `Inbox`.** A prompt accepted by `root/prompt` and still in a private memory stream when the daemon died was lost with no record — in the one pH process built to outlive its clients, and against P5-04/P5-05, which restore a root from disk. It now calls `agent.followup()`, which logs `agent/inbox/spliced` before the projection changes, and the stream carries only a wake; `ReactLoopAgent.run`'s own docstring — *"a daemon or the CLI calls it after queueing input"* — had anticipated the shape. `Root.status` went the same way: the agent already has a `status` property and emits `agent/status`, and a copy kept beside it was true only at this class's own call boundary, which is why the failure path published `"failed"` while storing `"idle"` and `roots/list` could never report a failed root. **Measured waste, all on the per-event path.** `relay` rendered the full wire payload before discovering there were no watchers — 6.6 µs per streamed chunk, ~13 ms of a 2 000-chunk turn, discarded; guarded, and `to_wire(thaw=False)` since the only destination is `dumps`. `read_frames` was quadratic twice over (re-scanning the buffer per chunk, recopying the tail per frame: 57 ms for 4 096 frames in one read) and is now `BufferedByteReceiveStream.receive_until`, which anyio ships with the cap built in. Replay sliced a materialized snapshot of the whole log (1.3 ms at 200 000 events) and now uses `events_from`; `describe()` counted the log the same way and now reads `seq`. And the profile YAML was re-parsed per root — 74% of the 6.5 ms cost of starting one — so `runtime.compose()` parses once and `mounted(parsed=…)` reuses it, which also replaced the hand-rolled `Context()`+`Loader.mount` pair whose missing `finally` leaked a mounted context if a root failed to start, and added the flush-before-unwind the TUI's `HarnessSession` documents. Smaller: `session.observe` instead of the store-wide bus plus a filter, so a root stops paying for every subagent event in its deployment to discard it; the subscriber token table became a set of bound methods; three write-only fields, a dead `connect()`, a nullable task group with a guard, and a doubled socket-path resolution that could print one path and bind another. **Deferred to P5-02, which owns the protocol:** the daemon's dispatch loop, `-32000` envelope and `PROTOCOL_VERSION` are copied from `modes/rpc_mode.py` and have already diverged — daemon frames omit the `"jsonrpc": "2.0"` the RPC mode sends, while this row's docstring claims the shipped dsh client stays usable. That row is specified against the dsh SDK shape and has to reconcile the two vocabularies anyway. **Then the sharper question, and it found a data-corrupting bug.** Reviewing the cleanup, the observation was that two of its findings looked like one root cause — *not treating the log as the source of truth*. That is right about `Root.status` (a private copy of a projection that `agent/status` already publishes, which is A11 in one line) and right, for a worse reason than the test pollution, about `$PH_HOME`: **the daemon never read an existing log.** `Supervisor.start` called `sessions.create`, which mints a fresh session, and the JSONL store appends — so restarting a daemon with the same root id concatenated a second session onto the first. Measured on a real restart: one file, one header, 33 records, and `seq` restarting at zero at index 16, which breaks **A1** and makes every fold over that file double-count. It also meant P4-14's reconciliation never fired for a daemon root — the process most likely to die holding a worktree was the one that never checked for leaked ones coming back up. (The third finding, the watcher "announced as dropped", is *not* this pattern: that was `log.warning`, the operator's diagnostic channel, and the defect was a claim/action mismatch. Conflating the two would blur a distinction worth keeping — the session log is the trace, the Python logger is a message nobody reads back.) **The fix resumes, and records the resume.** `resume_session` now appends `session/resumed` with `events`, `interrupted` and `closed`, where `interrupted` is `interrupted_turn_closers` being non-empty — the honest signal for "this crashed" as against "this was reopened", since a clean stop synthesizes no closers. Recorded rather than returned, because provenance is exactly what a log is for and is not derivable afterwards: a session that was reopened and one that ran straight through look identical. That is also what serves an unattended run — a cron-started agent leaves the fact in its trace whether or not anyone reads stderr. Three surfaces, one record: the daemon warns on stderr for whoever started it, the TUI renders a notice row (*"Resumed after an interrupted run — 16 earlier events, and the turn that was open when it stopped has been closed"*), and the trajectory carries it as a record rather than classing it `RECORDLESS`, because an auditor needs to know where somebody else's work ends and this run's begins. The vocabulary tests are what forced all three: adding an event type fails `set(HANDLERS) \| RECORDLESS == KNOWN_SESSION_EVENT_TYPES` in two packages until each front end has decided what to do with it. Two existing assertions changed deliberately — a resumed transcript now differs from the pre-crash one by exactly that notice, and `test_repair` asserts the closers *then* the seam record. Original scope: Daemon supervisor: `$PH_RUNTIME/daemon.sock` (named pipe on Windows), JSONL framing, **one `anyio` task per root**, worker addressed by id | P1-25, P0-16 | I-2 | attach/detach; TUI close leaves the root running |
| P5-02 | **Landed** (`ph_app/protocol.py`, the daemon's dsh vocabulary, cursors, a logged command journal, paged snapshots). **One protocol, two transports.** P5-01 shipped them as two — the daemon grew `root/*` methods and dropped the `"jsonrpc": "2.0"` field the RPC mode sends — and the divergence was invisible because each transport only ever tested itself, while the docstring claimed the shipped dsh client stayed usable. The envelope, the version and the capability block now live in one module and the daemon speaks dsh's names: `initialize`/`daemon/hello`, `session/new`, `session/prompt`, `session/attach`, `session.event`, `session.status`. What stayed per transport is the *method table*, because one process serving one session over a pipe and a supervisor serving many over a socket genuinely differ — and a transport says which it is in the capability block rather than a client inferring it from the socket it opened. **A cursor is `{generation, sequence}` because a sequence alone is only meaningful against the log that counted it.** `generation` is the session header's `createdAt` — stable across a resume, which by P5-01's fix continues the same log, and different for anything that is not that log. A cursor from another incarnation is neither honoured nor refused: honouring it would skip events the client never saw, refusing would strand a client that did nothing wrong, so it reads as "you have seen nothing of *this* log" and replays from the start. That replaced P5-01's `replay=N`, which made the client guess at a count instead of stating what it could prove. **Idempotence is a fold, not a table.** `clientId+commandId` is written to the session log as `client/command` *before* the prompt is spliced — A10's write-ahead ordering, since a duplicated turn is visible in the transcript and a dropped one is not — and the duplicate check reads it back. A dict on the supervisor would forget across a restart, which is precisely the moment a client is most likely to retry: it reconnects, cannot know whether its last `session/prompt` landed, and sends it again. Folding means the answer survives exactly as long as the thing it protects. **Snapshots are paged at 512 KiB** with the next cursor in each reply, because a resumed root can hold hundreds of thousands of events and a whole-log reply would trip the transport's own frame bound *and* force a client to buffer everything before rendering a line. Adding `client/command` to the vocabulary forced its classification in both front ends, which is the mechanism working: recordless in the transcript (bookkeeping, not conversation) and a record in the trajectory (who asked for a turn, and whether they had asked before, is exactly the provenance an auditor of a daemon-driven run needs). Both halves of the gate fail with their mechanism removed — verified by disabling each. **A cleanup pass then found the row's central feature could not complete.** `session/attach` replayed the whole gap synchronously, one `session.event` frame per event, into a 1024-slot outbox with no await point — so the pump could never drain and a client reattaching to a root that had moved on raised `WouldBlock` out of its own attach, *after* the subscription was made. Measured: it failed at exactly 1 025 events. P5-01's `replay=N` had been the only bound on that path and this row removed it without replacing it; the gate test passed because its log was three events long. **So attach no longer replays.** It subscribes and reports where the live stream begins; catch-up is `session/snapshot` only — one mechanism, bounded by construction, and the page bound now applies to replay too, which it never did. The tests were rewritten to the two-call shape a real client must use anyway. **Two claims the module made and broke on landing.** `protocol.py` says "one number, in one place" — and `PROTOCOL_VERSION` was still declared in `server.py`, dead. The row's thesis is one vocabulary — and `session.event` carried `rootId` when relayed live and `sessionId` when replayed, on the same connection, so a client needed `params.get("sessionId") or params["rootId"]`; `session.status` and `_detach` were `rootId` too. Both fixed, and `describe()` stopped shipping the same fact under two names (`rootId`/`sessionId`, `events`/`cursor.sequence`). **Measured waste on the command path.** `accepted()` materialized and scanned the whole log per mutating prompt — 4.9 ms at 200 000 events, on the daemon's own event loop, stalling every other connection for that long. It is now a set folded once when the root starts, so it is still derived from the log (a resumed root knows what it already did) and O(1) per command. And `_snapshot` measured every event with its own `dumps` to fill a 512 KiB page — 8.2 ms per page, *2.2x the encode it existed to bound*, all discarded; pages are bounded by event count now, with `MAX_LINE` as the real protection against an oversized frame. **Idempotence became a property of the protocol rather than of a caller's discipline**: `client_id`/`command_id` defaulted to `""`, so it held for the gate test that hand-rolled them and for nothing else. `DaemonClient` mints its own id and counts its own commands, the key is joined once at the wire edge, and a test drives the retry a reconnect actually forces. Cursors moved into `protocol.py` as `cursor_of`/`resume_at` — they are facts about a *session*, and the stdio transport of the same "one protocol" could not otherwise reach them — and a stale generation is now *said* in the attach reply rather than inferred from sequence numbers arriving unexpectedly. Smaller: `client/command` joined `IGNORABLE_SESSION_EVENT_TYPES`, so a reader that does not know the type skips it rather than refusing the whole log; the client stopped hand-building frames without the `jsonrpc` field its own daemon had just started sending; and three test defects went with them — an assertion of the form `assert X == Y or seen` where the equality was **False** and `seen` non-empty by construction, a duplicated assertion left by the rename, and a paging test whose `while more` loop never ran because `more` was always `False`, leaving the page bound unexercised by the entire suite. Original scope: Protocol: dsh SDK shape (`initialize`, `session/prompt`, `session.event`/`status`) + capabilities (`daemon_hello`, command envelopes, `{generation, sequence}` cursors, 512 KiB snapshot chunks); mutating commands journaled by `clientId+commandId` | P5-01 | — | reattach preserves streaming position; duplicate command is idempotent |
| P5-03 | **Landed** (`Supervisor._lease`, `_starting`, `SessionBusy`, `protocol.DaemonError`, `TEARDOWN_GRACE`). **I-5's two halves answer two different questions, and the row is only right when each answers its own.** The hazard is "two writers on one JSONL", and P5-01 already measured what that produces: one file, one header, `seq` restarting at zero mid-log, A1 broken and every fold double-counting. But two *clients* naming one root are not two writers — they want the same root — while two *processes* on one log are. So the in-process half **orders** and the file half **refuses**. `Supervisor.start` checks `self.roots`, then awaits twice before it assigns, so concurrent opens of a new id both pass the membership test and both build a root; the store's own uniqueness check cannot save it, since each root gets its own `Context` and therefore its own `SessionStore` and neither knows the other exists. A per-id `anyio.Lock` serializes that, and the second racer gets the root the first one built. Both halves were mutation-proven: with the lease removed the second daemon opens the held session, and with the lock removed two clients sharing a root get `session_already_active` — *the wrong answer, arrived at correctly*, which is what shows the two mechanisms are not interchangeable. **The lease is taken where the path is already known.** It first derived its own path from `resolve_roots()` while `_session_for` derived the log's from `store.root` — two constructions of one path, which is one profile setting away from guarding a file nobody writes — so it moved inside `_session_for`, beside the `session_path` call it protects, and a profile with no persistence takes no lease because it has no log to corrupt. `timeout=0`: "somebody else holds it" is known immediately and is a refusal, not something to wait out, and blocking here would stall the event loop for every other root this supervisor runs. **`thread_local=False` is load-bearing and its absence is silent** — filelock keeps its re-entrancy counter in a thread-local, so a lease acquired on a worker thread and released from the event loop finds a counter of zero and returns *having released nothing*: no error, no warning, a lock held until the process dies. It cost three tests, two of them P5-01's, all failing as "already active" against a daemon that had cleanly shut down. **A refusal a client branches on gets a name.** `respond` returned `-32000` and a message, so telling `session_already_active` from a mistyped method meant matching prose — a contract nobody wrote down and every rewording breaks. An exception class may now declare a `code`, which `respond` puts in `data.reason` and `DaemonError` carries back, JSON-RPC's own code staying generic because these are pH's vocabulary and not the transport's. **Two bugs found on the way, neither about leases.** `start` holds its `AsyncExitStack` by hand so a root can outlive the `async with` that made it — which means a failure partway through was the one path where `mounted`'s `finally` did not run, silently reintroducing the leaked context that P5-01's cleanup pass had just removed; it now unwinds what it entered. And `serve()`'s teardown was unshielded: `shutdown` is a notification, so a cancel from whoever started `serve` routinely arrives *while* it unwinds, losing sessions unflushed, worktrees unreclaimed (F6) and leases unreleased — `move_on_after(shield=True)` rather than a bare shield, because a root that will not unwind must not become a process that will not exit. **Deferred, and named rather than papered over:** the lease is daemon-against-daemon only, so `ph -p --session x` still opens a log a daemon holds. The writer is `JsonlSessionStore`, and leasing there would cover every mode at once; I-5 names the second daemon and that is what this row gates, so the docstring says what the code does instead of what would be nicer. Four tests: the gate, the release (a lock outliving its holder makes leases unusable — the fix would be "delete a file you were never told about"), the shared root, and the refused start that strands nothing. **A cleanup pass then rewrote the lease's mechanism, its cost and one of its tests.** The lock was acquired by hand and released through a callback on the root's exit stack — making it the one external artifact in the tree invisible to `ctx.dispose()`, when `Context.effect`'s docstring names the case verbatim (*"a child process, a worktree, a temp path, **a lock** — acquired through here, so cleanup is structural rather than remembered"*) and every other acquisition site in the repo uses it. Going through `ctx.effect` also deleted the `exits` parameter that was threaded down through `_session_for` purely to reach the callback, so `_session_for`'s signature stopped advertising cleanup it never registered. **The `to_thread` hop cost twice what it saved**: measured at +340 µs on a 1.9 ms root start against the 166 µs the acquire itself takes, because real starts are seconds apart and pay a cold thread plus a cold selector wakeup every time — and at `timeout=0` the acquire is one `os.open` and a non-blocking `flock`, so there is no blocking to move off the loop. Its neighbours settled it: `path.is_file()` and `resume_session`'s whole-log read are both already on the loop thread. Inlined, with the redundant `mkdir` dropped (filelock's own `ensure_directory_exists` is the same call one statement later, and `JsonlSessionStore.track` makes a third — all three fired per start), **the lease went from 537–675 µs to 126 µs, 26% of a root start to 6%.** The per-id lock table was a second id-keyed map with a different lifetime, never pruned — `aclose` cleared `roots` and not it — and `setdefault`'s eager argument minted an `anyio.Lock` to discard on *every* call, which `prompt` makes every turn: 832 B and 0.65 µs a turn, plus 928 B retained per distinct id ever seen. One lock and a membership fast path ahead of it removes the table, the allocation, and the 6.3 µs checkpoint an uncontended `anyio.Lock` still pays on the returning-client path. **The named-refusal channel read the wrong attribute.** `respond` probed `type(error)`, but every coded error in ph-core — `HarnessError`, `SessionForkError`, `CompactionError`, the adapter errors — sets `self.code` per instance, so all four families were structurally invisible to it and a `SessionForkError` out of `sessions.create` reached the client unnamed *on this very `session/new` path*. Reading the instance fixes that, and a declared `Refusal` base replaces the duck-typed probe for the reason the seams give for their provider Protocols — a probe finds whatever happens to be called `code`, and an attribute that drifted would go out unnamed with nothing to say so. The channel also had exactly one member while its two siblings on the same dispatch stayed anonymous, so `unknown_method` and `no_such_session` are named too. `TEARDOWN_GRACE` was a second constant for a tunable the plan already assigns to this package: it is `ph.resources.GRACE_SECONDS`, same value, same `move_on_after(shield=True)` idiom, and two budgets for one tunable is how the inner one silently becomes dead code or the only one that fires. The hand-rolled `except BaseException: aclose(); raise` became `async with AsyncExitStack()` + `pop_all()` — the stdlib's own keep-on-success spelling — which matters because the hand-rolled guard covered 3 of the ~55 lines that put things on the stack: `agents.create`, the wake channel, `Root(...)` and the `session.observe` callback all sat outside it, and which side of that invisible boundary a new line lands on decided whether its failure unwound. **And the shared-root test turned out to be proving nothing.** It had started two `session/new` calls concurrently and trusted the scheduler to overlap them; it did, until inlining the acquire removed the `to_thread` suspension point that had been doing it, after which the test passed with the lock deleted. The race is still structurally there — `_start` checks `roots`, then awaits twice before assigning — so the test now parks the first caller *inside* `_start` past the membership check and releases the second only once it is there: without the lock the second must build a second root, 3/3 either way, with no ordering left for luck to supply. **Deferred, with the collision named:** moving the lease down into `JsonlSessionStore` is a row, not an edit — the natural acquisition point is `track()`, a *synchronous* listener on `session/created`, and `_publish` writes `_entries[id]` before it emits, so a refusal raised there escapes `sessions.create()` with the session already published; nothing disposes the store either. **P5-08 is what will collide with it** (a `pyturso` backend has no `.root` and no canonical file, and `_session_for` reaches for both), and P5-01's "resume rather than create" rule sits at the same wrong altitude for the same reason — `ph --session <id> -p` still appends a second header-less session to an existing log. Also left: `ph.resources.install_lifecycle` — grace, shielded dispose, self-`SIGKILL`, tested — is wired into no entrypoint, so `ph daemon` under systemd or `kill` still tears down mid-flight; the new shield only covers the cancel raised inside the process. Original scope: Leases: in-process per root; `filelock` on canonical path against a second daemon | P5-01 | I-5 | concurrent open → `session_already_active` |
| P5-04 | **Landed** (`ph_app/daemon/recovery.py`, `Supervisor._drive`/`_flush`/`_restore`, `Root.retry`/`give_up`, `agent/retry` + `agent/failed`). **The row turned on a distinction the first draft got wrong: this ladder retries the root's *task*, not a failed turn.** Building it the obvious way — read `turn/end{reason.kind == "error"}` and run again — produced something worse than no ladder, and the row's own tests are what showed it. The failed turn has already **claimed** its message from the inbox, so a second `run()` finds nothing pending and the driver ends at `phase.step == 0` with `kind="completed"`: a trivially successful empty turn that clears the ladder and reports a healthy root which answered nothing. Re-splicing the claimed message instead would append a second `user/message` and show the model the same prompt twice. And it should not be retried here anyway — `llm-retry` has already retried everything a model failure makes sense to retry (rate limits, 5xx, timeouts) while deliberately declining the rest, because "an unknown failure retried is an unknown failure billed twice", so a `turn/end{error}` arriving at the supervisor is one the layer that *understands* model failures decided to stop on. Retrying it from a layer up is that same mistake with less information. What is left is what the row names: the task crashing *around* the turn — a flush that cannot write, a disposed context, a bug — where the work is still in the inbox and running again is meaningful. **That is also the failure a supervisor exists to prevent**: `_run` is started into the supervisor's task group, so anything raising out of it cancels the group and takes every *other* root down with it, plus the listener. **Every part of the ladder's state is folded from the log**, for the reason P5-01's `Root.status` derives rather than copies: a supervisor holding the attempt count in memory comes back from each crash with a fresh ladder, so a root failing for a permanent reason retries forever — three attempts per daemon lifetime, with nothing in the trace to show it was ever tried. `recovery_of` counts `agent/retry` since the last `turn/end` of **any** kind, error included: `turn/end` means the driver reached its own containment and returned, which is precisely the thing this ladder does *not* count, and treating it as a crash would give one root two retry mechanisms disagreeing about one failure. `Root.status` reads the same fold — the agent is authoritative while it holds work, and only *between* turns is "idle" ambiguous, since a root that exhausted the ladder is idle exactly like one waiting for a prompt. **Two bugs the tests caught, both about durability.** `recovery_of` guarded with `isinstance(outcome, dict)` — but the session freezes payloads to `mappingproxy`, which is **not** a `dict` subclass, so the guard was always False and *silently*: every failure read as "no reason recorded" and nothing was ever retried. And `retry()` was flushed while `give_up()` was not — the single record that matters most, left in a buffer, so a daemon stopping just after giving up resumed, saw no `agent/failed`, and reported the root idle: a ladder that forgets it was spent starts over forever. That one is now pinned by asserting the record is **on disk while the daemon is still running**, because a clean shutdown flushes it either way and an assertion after teardown would have passed with the write-through deleted. **`_restore` is best-effort by construction**: an advisory-tier root has no worktree, and a restore that fails must not cost the retry — in both cases the attempt proceeds against the tree as it stands and the record says `restored: false`, so the transcript never implies a rollback nobody performed. It takes the *latest* `workspace/checkpoint` (P4-09's fold), since a retry against a half-mutated tree is a different attempt from the one that failed and a ladder compounding its own damage is worse than none. **A fifth appearance of the test-isolation class, and the second in the same fixture.** The resume test called `monkeypatch.undo()` to drop a patch of its own — entirely reasonable — but `monkeypatch` is one function-scoped instance shared with every fixture, so it reverted the autouse `_isolated_home` too and the second daemon resumed from the developer's real `~/.ph`, writing a session there. The fixture's own docstring claimed it was "the one place no test can route around"; it was routed around by an ordinary line of test code. `PH_HOME` is now set directly with a `finally`, which nothing a test does to its own patches can undo, and the stray was removed. Six tests, five mutation-proven — never retrying, retrying forever, dropping the give-up flush, and picking the oldest checkpoint each break exactly one. (The `min`/`max` mutation also produced a false failure that survived three re-runs: same byte length plus a same-second restore left a stale `.pyc`, which is worth knowing before trusting a mutation result that will not go away.) The vocabulary gate did its job again — `agent/retry` and `agent/failed` were refused by both front ends until each decided what to do with them, and both are records rather than `RECORDLESS`, since a reader skipping them finds a transcript resuming mid-thought with no account of why and a session that stopped working looking like one that merely went quiet. **A cleanup pass then found the ladder did not terminate.** Reviewing the diff turned up the defect the row's own narrative claims to prevent: `recovery_of` reset the count on any `turn/end`, and **the retry manufactures one** — a re-entered `run()` finds an empty inbox and appends `turn/start` + `turn/end{completed}` before the same failure happens again, so the ladder cleared the bound that was supposed to stop it. Measured against a persistently failing flush: **165 retries in two seconds, no give-up, the fold pinned at one attempt and the root reporting "idle"**, growing the log by three events an iteration. Every test here injected at `run()`'s entry, so no turn was ever completed and nothing in the suite touched the one shape that produced it. The count now resets only on `supervisor/recovered`, a record **only success writes** and the failure therefore cannot forge; the failing-flush case is now its own test, and three mutations (retry does not advance, recovered does not clear, the start-time fold is dropped) each break exactly one. **The state moved from a fold-per-read to a fold-once-and-keep**, which is `Root.commands`' arrangement and the measurement `Root.accepted` records twenty lines above it: `Root.status` called the whole-log fold, and `describe()` reads `status` for every root on every `sessions/list` — 2.3 ms per root at 100 000 events, 12.5 ms at 500 000, and 128 ms for fifty roots with no await point between them, which is precisely the 4.9 ms-at-200 000 regression that docstring says was already fixed once. Folded at start and maintained by `retry`/`give_up`/`recovered`, `Root.status` went to **0.07 µs, flat in log length** — back to P5-01's baseline. `status` also now derives `retrying`: publishing it as a notification while `sessions/list` still said `idle` gave a client attaching mid-backoff two answers to one question, which is the drift the property derives rather than copies in order to avoid. **The events moved namespace**: `agent/*` is `ph.agent`'s, declared with `owner="ph.agent"`, and `agent/failed` sat one letter from `agent/error`, which means something else entirely — they are `supervisor/retry`, `supervisor/failed`, `supervisor/recovered`, and are now in `IGNORABLE_SESSION_EVENT_TYPES` on `limits/*`'s terms, so a build without the ladder can still seed a log a P5-04 daemon touched rather than hard-refusing the session. **Three reuse fixes, one of which was a live hazard**: `_restore` called `ctx.workspace.of` raw, and `ctx.workspace` *raises* on a profile that layers no workspace row — from inside `_drive`'s `except`, whose whole job is keeping the task group alive, so the one profile without a workspace would have taken every root down through the handler written to prevent exactly that; `workspace_of` is fail-soft and is the tree's one spelling of the question. `checkpoints()` + `max()` materialised every restore point to use one (7.2 ms and 0.5 MB at 500 000 events, per retry) and is now `latest_checkpoint(session, agent_id)`, a reverse scan at 2 µs that also carries `/revert`'s rule that a restore point belongs to the agent that took it. `_reason` was a sixth private exception-to-string and is now `ph.tools.errors.error_message`, which is total by construction — the previous one called `str(error)` unguarded from inside that same handler. `Supervisor.aclose` had its own copy of the best-effort flush that additionally skipped `exits.aclose()` when the flush raised, so one root's unwritable log left its context undisposed. Smaller: `Recovery.reason` was dead, `give_up` was `async` with nothing to await, `_until`'s `what=` reached no message so five call sites described a timeout nobody saw, and `_crashing` took an instance to reach a class the four call sites were already naming. **And the `$PH_HOME` rule was still stated three times**, two of them through the `monkeypatch` route this row had just rewritten `_isolated_home` to stop trusting — `mount` and `make_tui_app` each pinned the same variable to the same value, so a change to the autouse fixture would have been silently half-applied. One statement now, through a private `MonkeyPatch.context()` that a test's own `undo()` cannot reach. **Deferred:** a *turn* that fails is still recorded and rendered but does not change `Root.status`, so a client sees "idle" for a root whose last answer was an error — the honest fix is a status the agent itself publishes, not a second opinion from the supervisor. Original scope: Root-task crash recovery: retries 250 ms / 1 s / 5 s then failed; the root's tree restored from `workspace/checkpoint` | P5-01, P4-09 | A11 | injected crash recovers; third failure reports |
| P5-05 | **Landed** (`Supervisor.passivatable`/`sweep`/`passivate`, `Root.idle_for`/`passivate_record`, `server._sweeper`, `Session.last_event`, `supervisor/passivated`, `ph daemon --passivate-after`). **Rehydration is not a mechanism this row adds — it is one P5-01 already shipped.** `Supervisor.start` resumes any root whose log exists, so waking a released root is the ordinary path rather than a second one, and the round-trip the gate asks for is a property rather than a feature. What the row actually adds is a sweeper, a predicate, and a record. **The timeout is read from the log, not from a timer.** `Root.idle_for` measures from the session's own last event, so it means "nothing has happened" rather than "no client called us", it survives a restart — a root rehydrated from a three-day-old log is immediately eligible, which is correct — and it cannot drift from what the transcript shows. That needed one accessor: `Session.last_event`, the unfiltered form of `last_event_of`, because `events[-1]` materialises a snapshot of the whole log (4 MB and 4.7 ms at 500 000 events) to read one element and the sweep asks it of every root on every pass. **Each condition is read from something that already exists.** `status` covers `running` *and* `retrying` — a root in P5-04's backoff is idle between attempts, and releasing it there would passivate a root part-way up a ladder it had already recorded, which is precisely why the cleanup pass made `status` derive `retrying` instead of only announcing it. `subscribers` is the root's own attachment set, so the fact that makes a client receive events is the fact that keeps its session alive. Live children fold from `subagent/*` (P3-13), and an **unrecognised child status counts as live on purpose**: getting that backwards loses work, getting it this way costs memory until the child settles — and a parent released while a child still runs would be rehydrated by its own child's bookkeeping, a root that puts itself back every time the sweeper lets it go. Heartbeats and cron are the row's other two conditions and have **nothing to check yet** — P5-06 owns both — which is said plainly in the predicate rather than left as an unexplained absence. **Releasing gives back the I-5 lease**, and that is the point rather than an oversight: the lease lives on the root's exit stack (P5-03), so unwinding hands the session to whoever wants it next; keeping it would make a released session one *no* process could open. Order is `aclose`'s order — record, close the wake channel so the task leaves its own loop rather than being cancelled mid-turn, flush, unwind. The record is its own event because the alternative is an unexplained gap: a transcript that stops for three days and resumes reads as a crash, and the `session/resumed` on the way back says only that something resumed, not that nothing had gone wrong. `session/attach` now wakes a passivated root through `start` — the same path `session/prompt` takes — because a session released while its watcher was away came back as `no_such_session`, which reads as "gone" for something still on disk. `--passivate-after` takes minutes or `"off"` and **refuses anything else**: a typo in a duration is a daemon that silently keeps every root it ever started, in the one process where that goes unnoticed for a week. Nine tests, eight mutation-proven — each of the four conditions, the record, the flush, the unwind that frees the lease, attach-wakes, and the sweeper task itself, which is the one that would otherwise be dead code that looks tested since every other test drives `sweep()` directly. **Three flakes found and fixed, all one mistake.** Asserting on an asynchronously delivered fact at the moment its synchronous half changed: `status` flips when `give_up` *appends*, while the flush that carries it to disk and the notification that carries it to the client are both the next await. Reading either immediately failed about one run in four (`{'failed'} <= {'', 'retrying'}`) and one in three (a resumed root reporting `retrying` because the give-up never reached disk before teardown); each now waits for the thing it is actually about, which also keeps the mutation honest — with the flush deleted the wait times out instead of passing. The fourth was mine to make and cheap to miss: `passivate_after=0` makes the sweeper eligible to release a root *during* the turn a test is waiting on, so that test attaches first, which is the same condition a real client relies on. And `_settled`'s bare `next()` turned "the root is gone" into `RuntimeError: coroutine raised StopIteration`, naming neither the session nor the reason — reachable only now that roots can leave the listing. **A cleanup pass then found the row's central condition was checking a vocabulary that does not exist.** `_SETTLED_CHILD` was hand-written in ph-app as `{completed, failed, cancelled, deleted}`. The seam declares `SubagentStatus = queued \| running \| done \| error \| cancelled` and the only producer writes four of those; deletion is a *tombstone* that sets `deleted` and leaves `status` alone. So three of the four members were strings nothing emits, both real terminal states were missing, and **a root that had ever run a child to completion could never be released** — most of what the row exists to do. The gate test passed because it appended `status: "completed"`, validating the invented vocabulary against itself; measured directly, `done`, `error` and a tombstone all reported `passivatable: False`, and all three report `True` now. "Which statuses are terminal" moved to the seam that owns them as `SETTLED_STATUSES` and `child_is_live`, beside `SubagentStatus` and `fold_subagent_event`, which are already there for the reason that two consumers must not disagree — and the unknown-status-is-live rule is stated once, where a caller destroying a mounted root can find it. Three mutations pin it. **And the class it belongs to is now closed, which is the more useful outcome.** The vocabulary was always shared and the producer was always type-checked — `_status(status: SubagentStatus)` makes it impossible to *write* `completed`. The asymmetry is that a `Literal` cannot constrain readers: the value crosses `SessionEvent.data`, which is `Any` so a log written by another build round-trips, and a consumer's `row.get("status") in {…}` then type-checks against any strings at all. `get_args` is the repo's existing answer where that has been asked before — `sandbox.py` validates `event.data.get("mode")` against `get_args(SandboxMode)` — so two gates now assert `SETTLED_STATUSES` is a subset of the declaration and that the TUI's `STATUS_GLYPHS`, a second consumer-side enumeration correct today and checked by nothing, is exactly equal to it. Adding a status to the seam fails until every consumer classifies it, which is the shape the session-event vocabulary already has. **The predicate asked the expensive question first.** `passivatable` short-circuits on status and subscribers, but the root that reaches the roster fold is *the idle unwatched one* — the steady state the sweeper is for — so a whole-log fold ran on all ninety sweeps of the default window and was discarded eighty-nine times. Measured over one idle window at 500 000 events across 50 roots: **60.9 s of event loop as written, 1.3 ms with the quiet check moved above it**, and `idle_for` costs 43 ns. The fold also went through the seam's `SessionFoldCache` (`ctx.subagents.roster`) rather than the bare function, which is what saves the root the predicate keeps returning `False` for — idle, unwatched, one unsettled child — that would otherwise re-fold every sixty seconds forever: 13.5 ms against 0.09 µs, or 16 minutes of event loop a day at 50 such roots. `passivate` now evicts that cache entry too, since the seam keys by session id and nothing else would tell it the root is gone. **Releasing a root was written twice.** `passivate` and `aclose` each carried flush-then-guarded-unwind, down to the same warning string — the shape `_flush` exists to prevent, whose docstring records that the previous copy skipped `exits.aclose()` when the flush raised. One `_release` now, so a lease or a reclaim added to teardown cannot land in only one of them. **And passivation raced `start`.** It removed the root from `roots` and *then* unwound, so a `start` arriving in between found no root, opened the session, and was refused by the P5-03 lease the outgoing root had not released — `session_already_active` for a session nobody was using. `session/attach` made it reachable by waking a passivated root through `start`; both now take the same lock `start` orders itself with. Smaller: `after` was one constant spelled in eight places, two of them positionally unchecked through `start_soon`, and is a `Supervisor` field beside `provider` and `model`; the tick loop read `stop` three times for one condition; every release was logged twice; the CLI's `"90"` and `PASSIVATE_AFTER`'s ninety minutes were two literals in two units where editing the documented one changed nothing; `idle_ms` was computed twice and threaded as a fourth hop for a number derivable from `(root, now)`; `LATER` was a sentinel that did nothing beside `after=0`; and `_settled`'s list-build replaced a `next()` whose real problem was the missing default. **The pass also caught a live breakage this row shipped.** `_passivation` was inserted directly beneath an existing `@app.command()`, which rebound it: **`ph daemon` stopped being a registered command** (exit 2) and a `-passivation` command appeared in `--help`. All 1 284 tests stayed green, because nothing anywhere asserted that a command is registered — the CLI's commands were only ever invoked through `CliRunner`, which is a different question from whether they are reachable. Fixed, and now guarded by a test that names the public commands and refuses any private one. ph-app also finally pays the **writer-side vocabulary debt** `known_event_types` records and ph-core and ph-rlm both honour: it appends five types and nothing checked they were declared, so P5-06's heartbeat would have produced a log this build writes and then refuses to seed. **Deferred:** the sweeper releases but never *pre-warms*, so the first message after a release pays a full mount (~2 ms) before the turn starts; and passivation does not yet snapshot a kernel namespace, so an `ph-rlm` root with live interpreter state loses it — P3-13's in-process variant handles the roster, not the namespace. Original scope: Passivation sweeper (90 min default, `"off"`; requires no clients/heartbeats/cron/active descendants) + rehydration | P5-01, P3-13 | A11 | round-trip |
| P5-06 | **Landed** (`ph/seams/schedule.py`, `Supervisor.tick`/`heartbeat`, `server._ticker`, four `schedule/*` event types, `croniter`). **A schedule outlives the process holding it**, which is the whole reason this is a seam over the log rather than a timer: the daemon is the one thing in pH built to run for weeks, and a machine that reboots between Tuesday and Wednesday must not lose Wednesday's run. So a schedule is `schedule/created` until a matching `schedule/cancelled`, and a firing is `schedule/tick` — **appended before the work is delivered**, which is A10's write-ahead applied to time. The asymmetry is what settles the ordering: a tick recorded and then lost to a crash costs one *skipped* run, while a tick delivered and then lost costs a *repeated* one — and repeating a scheduled prompt bills twice and puts a turn in the transcript nobody asked for. At-most-once, deliberately, with the log saying which. **Missed ticks coalesce, and the arithmetic is the row.** A five-minute schedule on a laptop that slept for three hours has thirty-six fire times behind it: delivering all of them turns a nap into a stampede, and delivering the *oldest* works through the backlog for another three hours before catching up. One tick, naming the most recent due moment — computed as a single `(elapsed // every) * every` for intervals rather than a loop that would take a step per missed tick on a log that slept for a week, and as a forward walk keeping only the final step for cron. The gap stays visible because the previous tick is still in the log. **Due, not claimed, is what the next one counts from**: a tick claimed four seconds late must not push the following one four seconds later, or a one-minute schedule becomes a sixty-four-second one and then a sixty-eight-second one. **The clock is a parameter** — `now` is passed in, the same seam `Supervisor.sweep` uses, so three hours pass in a test without three hours passing. **This closes P5-05's fourth condition**, which that row left open in so many words: a root with a live schedule has already said when it comes back, so releasing it would unwind the `Context` watching an appointment still sitting in the log. Heartbeats turned out *not* to be a fifth condition — a `schedule/heartbeat` is a **record**, not a claim on the root's life, so an operator can tell "waiting for Wednesday" from "died on Tuesday" when a monthly schedule leaves a log whose last line is a month old. What keeps a scheduled root mounted is the schedule. **Delivery goes through `prompt`**, the same path a person's message takes, so a scheduled turn is an ordinary turn with a record beside it saying why it started — rather than a second way into the loop with its own bugs and its own transcript shape. The ticker is its own task at 5 s against the sweeper's 60 s, because they mean different things (what to *start* against what to *release*) and sharing a tick would make a schedule's resolution the sweeper's interval. **A unit bug the gate caught immediately**: `spec` is seconds for `interval` — the kind a person writes by hand — against epoch milliseconds for `once`, and reading it as milliseconds made a five-minute schedule fire every 300 ms. The conversion is now on one line with the field documenting the split. Eleven seam tests and four daemon tests; four mutations pin it, and a fifth was added after the first cron test proved too weak to notice a scheduler that forgot its last tick and re-delivered the same moment. **Three completeness gates fired on the way**, all of them earning their keep: the two front ends refused the new types until each classified them, `test_the_auditor_renders_what_the_transcript_does_not` required the three bookkeeping types to be named where the transcript skips them and the auditor does not, and `test_models_round_trip_by_alias` *skipped* `Schedule` for want of a constructible sample — a silent hole that a skip rather than a failure would have kept. **A cleanup pass then found the row's anchor was never set, which made every real schedule wrong.** `created_at` was a field on the wire model that `create` did not stamp and no caller supplied, so every schedule the daemon made anchored at **epoch 0**: an hourly interval was due the instant it existed, and a cron's first claim walked forward from 1970 — measured at **391 s, six and a half minutes of blocked event loop**, for `* * * * *`. The tests missed it entirely because they passed `created=0` explicitly and used a fake clock that agreed with it. The anchor now comes from the `schedule/created` event's own `time`, which is where `last_tick` already came from, and the field is gone — a caller obligation nobody honoured is not a default, it is a bug waiting for a caller. **The cron walk became a seek.** Walking forward one `get_next` per missed moment costs 11.6 µs a step; the coalescing answer is the *last* matching moment, so one `get_prev` finds it whatever the gap: **122 ms against 40 µs on a week's gap, and 88 µs against 391 s from the epoch** — and cheaper than the walk even in the steady state, since it takes one step rather than two. It has to seek from `now + 1ms`, because `get_prev` is strictly *before* its start and a five-second ticker lands exactly on a cron boundary often enough to matter; the test caught that as a schedule firing one tick late. **The fold was uncached on the five-second path**, which is the mistake P5-05's cleanup had just finished fixing one method away — and the new `passivatable` check was inserted *directly beneath the comment* arguing against exactly that, so the paragraph measuring "0.09 µs against 13.5 ms" sat on top of a bare whole-log walk. Now `SessionFoldCache` on the service, as `SubagentService._rosters` does, with `forget_session` on disposal and on passivation. Two numbers, both measured: a root **with** schedules reads in **0.209 µs against 22.6 ms at 500 000 events**, and a root **without** any — the common case, since `schedule` is a `base.yaml` row every mounted root carries — exits on `Session.latest(CREATED)` in **0.112 µs**, a fold that used to cost the same 22.6 ms to return `{}`. Aggregate at fifty roots and 500 000 events: from 49% of a core held permanently, and 2.4 s of blocked event loop per five-second tick, to nothing. `tick` also folded a **second** time in `if claimed or schedule.live(...)`, which could only be reached when the first fold had just returned nothing — buying a `_flush` of a buffer it had itself proved empty. **`prompt` was optional with no second delivery path.** The field said "empty means the caller delivers something of its own" and the only caller had nothing to deliver: an empty-prompt schedule was claimed, recorded a tick, counted as fired, and did nothing — permanently, since at-most-once never retries it. Required now, branch deleted. **`spec` moved to milliseconds** for the numeric kinds, matching every other duration on this wire (`grace_ms`, `timeout_ms`, `duration_ms`, and this seam's own `dueAt`), which removes the conversion that had already produced one unit bug. **Three periodic loops became one primitive**: `_sweeper` and `_ticker` were the same seven lines with different bodies, and the heartbeat rode the ticker on a counter that only advanced when a tick *succeeded* — so a run of failing ticks starved the liveness record as a side effect of an unrelated failure. Three `start_soon(_every, …)` lines now, each with its own cadence. Smaller: `ScheduleService.ctx` was never read and cost seven `type: ignore`s in tests that had to lie about the constructor; `claim` returned a due moment its only caller discarded and the event already carried; `tick`'s `try` covered the one call that barely raises and excluded `prompt` and `_flush`, the two that do; the anchor rule was spelled twice under two names; `HEARTBEAT_EVERY` lived in ph-core where only ph-app read it; `croniter`'s import is now lazy, taking **25.4 ms — about what pydantic costs — off every `ph -p` and every TUI start** that never sees a cron expression; and the test helpers went from a nine-times-repeated three-line preamble to one `_sched`. **Recorded rather than fixed:** a scheduled root now never passivates, so a `once` for next month pins a mounted `Context` *and its P5-03 lease* for the interval — `passivate`'s own docstring says a released session is one another process may legitimately open, and every scheduled root silently loses that. The veto is load-bearing today because `tick` only iterates mounted roots, so without it a passivated schedule would never fire at all; waking one needs an index of which *stored* logs carry live schedules, which is a row rather than an edit. **Deferred:** nothing consumes `ctx.schedule` from the model's side yet — there is no tool or command to create one, which is P5-07's (`/autonomous`, goals) to add; and a schedule is per session, so "every repo at 9am" is as many schedules as repos. Original scope: Scheduler (`ctx.schedule`): `once \| cron \| interval` via `croniter`, ticks claimed before delivery, missed ticks coalesce; heartbeats every 5 m | P5-01, P1-23 | A10, A11 | coalescing test |
| P5-07 | **Landed** (`ph/seams/goals.py`, `ph/commands/autonomous.py`, `workspace_git.tree_hash`, four `goal/*` event types). **An autonomous run's whole job is knowing when to stop**, and it has exactly three ways: every gate passes (`achieved`), a budget runs out (`budget_limited`), or a person stops it (`abandoned`). Those are not interchangeable — a loop reporting the second as the first would be claiming work it did not do, which is the failure this layer exists to make impossible — so `Outcome` is a closed vocabulary and the settled row says which one. A run with **no** gates says so in its own outcome rather than letting the model's judgement pass for a check. **Budgets are counted, not carried.** Four limits rather than one, because they fail differently: a model looping on the same edit exhausts continuations, one that cannot stop talking exhausts turns, a long context exhausts tokens, a hung gate exhausts the clock — and one number could only catch whichever bound first. Every one is folded from the session's own events, so a run that survives a daemon restart does not come back with a fresh allowance, and `Spent.exhausted` returns the **name** of the limit that bound rather than a boolean: "it stopped" and "it ran out of turns" are different things to whoever reads the trace, and only one of them suggests raising a number. Counting turns and tokens at all depends on one open goal per session — `turn/end` and `assistant/message` carry no goal id and never will, being the agent's records rather than this seam's — so `set` refuses a second while one is open, and that refusal is what makes the attribution honest rather than a guess. **A gate is a shell command and a fingerprint**, and the fingerprint is P4-09's `git write-tree` — extracted as `tree_hash` so the restore point and the gate memo are *one derivation* and cannot disagree about whether the work changed. A gate that failed against a tree the agent has not touched cannot have changed its mind, and re-running a slow suite to learn nothing spends the very budget that decides whether the run continues; an edit anywhere changes the hash and it runs again. Two asymmetries are deliberate and both are pinned: a **passing** gate is never memoized as a reason to skip — the rule is that an unchanged *failed* gate is not re-run, and a pass is the last thing to take on trust when declaring a goal achieved, since a tree hash covers the agent's work and not the clock, the network, or a dependency that moved underneath it — and an **empty** fingerprint never matches another empty one, or every gate in every advisory-tier session would answer forever from its first run. The tree hash is taken once per pass, before any gate runs, because a suite that writes a cache file would otherwise change the fingerprint under the gate that follows it. **Tools and commands, never host handlers** (C2): `/autonomous` reaches `ctx.goals` and `ctx.shell`, the same governed surfaces a tool would use, so no path starts an autonomous run without a record. The bare form reports the spend rather than starting a second run, because "what is it doing" is what a person types on coming back to a session. Sixteen tests, five mutation-proven — the named budget, the remembered failure, the un-memoized pass, the empty fingerprint, and a tree hash that stops tracking edits. **Three house rules caught things on the way**, all of them earning their keep: the session log **refuses a tuple outright** (*"tuple would come back as a list"*), because a value that changes type across a round trip makes a resumed session differ from the live one it replaced — `gates` is a list; `assistant/message` is surface-eligible and refused an append with no `surfaceOp` marker; and `test_models_round_trip_by_alias` **skipped** `Goal` and `Budget` for want of constructible samples, the same silent hole P5-06 had just closed for `Schedule` — a skip in a completeness gate stays open, so both got samples. The outcome is read back through `get_args(Outcome)` rather than a list restated in the fold, which is the reader-side discipline the `SubagentStatus` cleanup established. **A cleanup pass then landed the driver, because the deferral was wrong about where it lived.** The row's narrative said the loop belonged with P5-10 — which is the *CLI* row — while the port plan's §6.7 says plainly that `/autonomous` **is a policy plugin on `agent/turn-stopping`**: a hook the agent loop already fires, already `serial`, with a passing test proving a listener keeps a turn alive *by steering* rather than by reaching into loop state. Not a daemon away; one `ctx.on` away. Before it, every outcome was unreachable — `achieved` and `budget_limited` needed the loop, and `abandoned` needed `/autonomous stop`, which the "a goal is already open" branch returned in front of, so the help text pointed at a verb that could not run. `run_gates`, `continued`, `settle`, `record_gate`, `Spent.exhausted` and both TUI renderers had no reachable caller at all: the command could open a goal and report the spend of a loop that did not exist. The driver asks three questions in the order that keeps the answers honest — **budget first**, so a spent run cannot smuggle in one more free gate pass; then the gates; then continue — and removing the budget check makes the test **hang**, which is the clearest statement of what the bound is for. **Continuations are steps, not turns**, because `steer` delivers at the next *step* boundary: `max_continuations` is what binds an automatic run and `max_turns` bounds the goal's whole life including turns a person starts. **The wall clock moved out of the fold.** `elapsed_ms` was folded from event timestamps, so the one budget whose stated purpose is stopping a hung gate was blind to a hung gate — the case that appends nothing. `exhausted` now takes the clock from its caller and returns a closed `Limit` rather than a bare string, since that name reaches a person through `goal/settled`'s `detail` and renaming `Budget.max_turns` would otherwise leave a literal behind with nothing to catch it. **Tokens were a third definition, and the only two-term one.** `TokenMeter.baseline` and the TUI's `_count_usage` each spell out the same four terms — the second's docstring literally says *"same four terms as `TokenMeter.baseline`"*, a comment doing a type's job — and this fold read `inputTokens + outputTokens` off the raw payload, so on a cache-heavy run most input never reached the budget and `/autonomous`'s status disagreed with the footer showing the same word. `TokenUsage.total` now exists for all three (`reasoning_tokens` deliberately excluded: providers count it inside `output_tokens`, so adding it bills twice — the mistake that model's own docstring warns about for cached input). **The fold got 17x faster and its cache started working.** No type filter meant every `assistant/chunk` paid a `data.get`, an open-goal scan, a six-branch chain and a frozen-dataclass rebuild: **597 ms against 35.7 ms** at 500 000 events. And `SessionFoldCache` keys on `session.seq`, which moves on every event — so the driver's per-turn read and `run_gates`' own `record_gate` between gates each re-folded the whole log, measured at 1 136 ms for a three-gate pass. With `fold_goal_event` factored as a step (`fold_subagent_event`'s reason: two consumers, one projection) the cache takes `extend`, and a read after one append went from a full refold to **7.5 µs**. Smaller: `Spent` is mutable and accumulated in place (the frozen version measured 259 ms against 5.2 ms per 200 000-event fold — fifty times the budget the cache exists to keep); `_last`/`open` were the same reverse scan written twice with a parallel `order` list maintained for one of them, now one `open_goal`; `memo`'s tri-state could not represent `True` and forced its one caller to write `is False`, now `unchanged_failure() -> bool`; the budgets became **row config** so `base.yaml`'s promise that a profile addresses a row by id is keepable; the syntax line is a `USAGE` constant like both sibling commands, the two copies having already drifted; and the test helper's `# type: ignore[arg-type]` had switched off argument checking for a whole call, which is how three sites passed `gates=("pytest",)` — the tuple the field docstring says the log refuses outright. **And the extraction had added a fifth git spawn per code cell**: `checkpoint` kept the two guards `tree_hash` re-derives, so every cell paid an extra `rev-parse --absolute-git-dir` — the exact waste `_cached_checkpoint`'s own docstring names as "one of four spawns spent learning a constant". **Recorded rather than fixed:** gates run through `ctx.shell`, which is the seam *beneath* the `bash` tool — it applies the sandbox policy but not `tools/pre-execute`, approval, the tool budget or the breaker, and appends no `tool/call`, so a gate leaves no argv in the log for `/revert` to account for. That is also what blocks the model-facing tool half of C2: handing a model `gates: list[str]` executed this way would be an arbitrary ungoverned shell surface, which is the hole C2 exists to close — so the tool is not "a row away", it is blocked on the gate runner going through the pipeline. Also left: budgets are scoped to a *session*, so a run whose method is fanning out to children counts nothing they spend, and a **fork** starts the fold at `seed_length` and therefore sees no open goal — the module's promise that a run cannot come back with a fresh allowance holds across resume and not across fork. **Deferred:** the loop itself is not wired to the daemon — `/autonomous` opens a goal, records spend and decides gates, but nothing yet *continues* the agent automatically when a turn ends with the goal open, which is where `Supervisor` meets `ctx.goals` and belongs with P5-10's surfaces; and there is no model-facing *tool* to set a goal, only the command, so C2's other half is a row away. Original scope: Goals (`ctx.goals`) and `/autonomous` **as tools and commands** (C2 demotion): `GoalState`, budgets `{max_continuations: 3, max_turns: 12, max_tokens: 80_000, timeout: 30 min}`, shell quality gates with worktree fingerprint | P5-06, P4-08 | A10, A11, E7 | budget exhaustion → `budget_limited`; unchanged failed gate not re-run |
| P5-08 | **Landed** (`ph/persistence/protocol.py`, `ph/persistence/turso.py`, `session-persistence-turso`, `pyturso`). **The Protocol was the row, and it did not exist.** `SessionPersistence` was named throughout the plans since D5 and declared nowhere — there was one implementation, so every consumer reached for *its* shape: four of them read `store.root` and rebuilt a filename with `session_path`, which is a JSONL fact four callers deep and exactly why a second backend could not be added without breaking all four. Declared now, with the split that makes a non-file backend possible: the write side unchanged (buffered appends drained by `session/flush`, because A1 is a property of the *seam* rather than of the storage), and a read side of four questions — `exists`, `read`, `stored`, `locate` — that a database answers as readily as a directory. **`locate` is allowed to say no, and that is the point of having it.** A caller that wants to *show* a path shows nothing; a caller that wants to *lock* one — P5-03's lease — must decline rather than invent, because a lease on a file no backend writes protects nothing while looking like it does. `resume_session`, the daemon's resume check and the print mode's `log_path` now ask instead of deriving. **One row per event, not one blob per session**, with `(session_id, seq)` as the primary key: A1's "`seq == len(log)`" becomes an index rather than a convention, so a doubled flush is a constraint violation where a JSONL append would silently write the line twice. **The gate is one suite over both backends** — a second backend tested by its own tests would agree with itself and nothing else — covering round-trip, absence (`exists` false and `read` raising, never an empty log, which is P5-01's own bug wearing a different hat), contiguity across flushes, the idle no-op, the listing, `locate`'s honest disagreement, and a mounted-profile resume through each. **Full-text search is a second Protocol, not a method on the first.** JSONL could only answer by reading every file, which is a grep wearing search's name, so `SessionSearch` is asked for with `isinstance` — `ReclaimingProvider`'s shape, and for its reason. The index needed `experimental_features="index_method"`: without it `CREATE INDEX … USING fts` is refused outright as experimental, and the row degrades rather than failing to mount, because `fts_match` **works unindexed** — it scans — so an unindexed backend still finds the right sessions and loses only the order. **And ranking is unavailable for a reason worth writing down.** `fts_score` returns the real BM25 value for a *literal* query and **0.0 for a bound parameter** — measured both ways on 0.7.2. Getting the number would mean interpolating a search phrase, supplied by a person or a model, into SQL. pH does not build a query out of it: an unranked search that finds the right sessions is useful, one with an injection hole is not. `test_ranking_is_not_available_yet_and_this_test_says_when_it_is` asserts the limitation so a version that fixes it makes the suite **fail**, which is how the constraint gets revisited instead of quietly outliving its cause. (The docs' own contradiction was settled empirically on the way: higher *is* more relevant, against the FTS reference's prose and with its example.) **A cleanup pass then redesigned the backend around three corrections, and every one of them made it smaller.** (1) **Full-text search is gone.** JSONL cannot search either, so it was never parity — it was a feature riding along because the row named tantivy, and it cost an experimental flag, a second Protocol nothing called, and an index whose *incremental maintenance made writes quadratic*: per-row insert cost grows with table size, so a ten-event checkpoint flush went from 1.0 ms to **227 ms** once a session passed six hundred events, and ten thousand events extrapolated to about **twenty-seven minutes** against 331 ms unindexed — awaited on every `session/flush`. Search over sessions is a thing built *on* a backend, not a thing one backend secretly has. (2) **One database per session, not one holding many.** A session is the unit pH creates, resumes, leases and discards, so it is the unit the storage is addressable by: deleting a session is deleting a file, the same as JSONL. It also *restores `locate()` to a real path* — and that mattered more than cleanup, because a shared database had none, answered `None`, and **silently disabled P5-03's I-5 lease**: two daemons could open one session and nothing would refuse. The listing collapses to a directory scan of the same shape as JSONL's, deleting a `COUNT(*) GROUP BY` that was 93% of the call and scaled with total history rather than page size. (3) **`seq INTEGER PRIMARY KEY` is already the ordering** — in SQLite an integer primary key *is* the rowid, so the table is clustered by it and a scan returns the log in the order it was written, which is the one property an append-only file gives for free. `WITHOUT ROWID` would say so explicitly, is gated behind another experimental flag on this build, and earns its keep for *composite* keys — which one database per session removes. After all three: ten thousand events flush in **121.8 ms**, 1.7x JSONL. **And the review found a defect the conformance suite could not see.** The write stored only the `data` column, dropping `surfaceOp`, `ignorable` and `sourceEventSeqs` — so a session holding any real `user/message` came back unmarked and `Session(seed=…)` refused it, which is *every resumable session*. The round-trip test missed it by using `turn/start` alone, an event with no envelope to lose; the suite now re-seeds a `Session` from what it read, which is the actual contract since that is what `resume_session` does. **Two more things were broken and are fixed:** the TUI's session picker still read `store.root`, an attribute only JSONL has, so opening it with the other backend raised `AttributeError` — a deferral that crashes is not a deferral, and it now goes through `locate`; and `track` opened the database *on the event-loop thread* from a synchronous `session/created` listener (102 ms against an existing database, where JSONL does a `stat`) to answer a question `INSERT OR REPLACE` made unnecessary, while re-queueing a resumed session's entire log because it indexed the log by the length of the buffer. **The Protocol got a static funnel and lost two lying fields.** Annotating the local before `ctx.provide` makes mypy check both backends *with signatures*, which the `runtime_checkable` `isinstance` gate cannot — it compares names — and it immediately caught two drifts while this pass was running. `StoredSession.size` meant bytes from one backend and an event count from the other, into a field a picker renders as a file size; `title` was set by neither, and a field that is always empty is an affordance that lies. Both return when a consumer can say what they mean. The firehose wiring became one `attach(ctx, store)` rather than a copy per backend, and the store now contributes a `ph doctor` section. **Recorded rather than fixed:** the TUI's session picker still scans a directory itself — `session_summaries` needs `text_of_wire` and `obj`, which live in ph-app, so the title half cannot move down without them; `stored()` answers the part every backend can (id, mtime, size, `cwd`, `parent` from the header) and P5-14, which owns the picker, is where the two meet. The multiprocess-WAL half of the amended gate is untouched: `.tshm` is opt-in, experimental, 64-bit Unix only and silently inert elsewhere, so "a second process reads a session the daemon is writing" needs its own row with its own refusal path. And the suite pins **correctness parity, not incrementality** — mutating the Turso flush to rewrite the whole log every time still passes, because `INSERT OR REPLACE` keyed on `(session_id, seq)` makes a rewrite correct, merely quadratic; catching that needs instrumentation the black-box suite deliberately avoids. Original scope: **`pyturso` persistence provider + full-text session search.** The second backend behind `SessionPersistence` is **Turso** via `pyturso` (`uv pip install pyturso`; import `turso`, async `turso.aio`) rather than stdlib `sqlite3`. It is DB-API 2.0 and SQLite-compatible, so the provider reads like the `sqlite3` one it replaces, and it brings three things at once that `sqlite3` does not: **a real asyncio surface** (`turso.aio`, so the store stops being the one seam that must hop to a thread to avoid blocking the loop — P5-03 measured what a thread hop costs on a path taken per root), **full-text search that is transactional with the append**, and an **optional cross-process WAL**. **FTS is tantivy, and it lives in the database file.** `CREATE INDEX … ON t USING fts (cols)` stores tantivy's inverted-index segments inside the B-tree, so index updates go through the same WAL as the row that caused them and roll back with the transaction. That is the property worth the dependency: an index maintained *beside* the log — FTS5 shadow tables, or anything external — makes A11 ("every projection equals its fold") false the moment a crash lands between the append and the index write, and a search that silently misses a session is the failure nobody notices. Tokenizers per column (`default`, `raw`, `simple`, `whitespace`, `ngram`) via `WITH tokenizer=`, weights via `WITH (weights=…)`; `fts_match` filters, `fts_score` ranks BM25, `fts_highlight` marks terms; the query syntax carries boolean, phrase, prefix, column-scoped and boosted terms, which is more than the session search needs and all of it free. **Score ordering gets pinned by a test, not a citation**: the FTS reference's prose says lower scores are more relevant while its own example orders `DESC`, and the engine's write-up agrees with the example — a contradiction in the source is exactly what a gate is for. **Two documented limitations land on us.** There is *no automatic segment merging* — tantivy's background merges are disabled because directory persistence is tied to transaction semantics — so `OPTIMIZE INDEX` is manual and segment count grows without it; a daemon built to run for weeks (P5-01) has to put it on a maintenance path or watch search degrade. And the reference's limitation list says *no read-your-writes in a transaction*, which is in tension with the engine's transactional framing (that claim is about rollback atomicity, not intra-transaction visibility — they are different properties). If the limitation holds, a search issued inside the same transaction as an append cannot see it, which the checkpoint policy has to know; settle it with a test before building on either reading. **Multi-process WAL is opt-in and buys readers, not writers.** `turso.connect(…, experimental_features="multiprocess_wal")` creates a `.tshm` sidecar every participating process mmaps, with byte-range locks (OFD on Linux, `fcntl` on macOS) gating ownership and a shared frame index so a reader in *any* process resolves a page to the latest WAL frame without scanning from the start. But **at most one process holds the writer slot**, so it does not make two writers safe — it makes cross-process *reads* safe against a live writer, which is what `ph agents`, attach and the trajectory viewer actually want and which JSONL gives us only by the luck of append-only reads. **It therefore does not retire I-5's lease** (P5-03): one writer slot is still one writer, and the lease is pH's refusal at the level a client branches on (`session_already_active`) rather than a storage error the protocol does not speak. Note also that **mixed-mode opens fail** — a daemon opening with `multiprocess_wal` and a `ph -p` opening without it collide with an error either way round — so the flag must be one setting resolved in one place, which is the same shape as the "two constructions of one path" bug P5-03 already hit once. **Recorded honestly, because these constrain the row:** multi-process WAL is *experimental*, and the docs say the on-disk format **and the public API** may change between releases. It is 64-bit Unix only — on Windows, WASM and 32-bit targets "the flag is accepted but has no effect", i.e. it fails **silently**, which is the worst possible shape for a coordination primitive. It refuses NFS, CIFS/SMB2, CephFS, GFS2, Lustre, OCFS2, AFS, CODA, NCP and 9P with `InvalidArgument` — and `$PH_HOME` on a network home directory is an ordinary university or lab deployment, so that is a refusal real users will meet, not a hypothetical. `pyturso` itself is 0.7.2, MIT, classified *Development Status :: 3 – Alpha* and explicitly pre-1.0, with wheels for manylinux x86-64/aarch64 and macOS — **and none for Windows**. So this backend is Unix-only in practice, which is precisely why **JSONL stays the default** and this stays a *provider behind the Protocol* rather than a replacement: D5's ordering ("JSONL first") is load-bearing, not incidental. **Prerequisite:** `SessionPersistence` is named throughout the plans but is not declared anywhere in code today — `JsonlSessionStore` is the only implementation and the daemon reaches straight for its `.root` and `session_path`, which P5-03 recorded as the collision this row would hit. Declaring the Protocol, and moving `_session_for` off the JSONL-shaped path, is the first half of this row rather than a surprise in the middle of it. | P0-15, P5-03 | A1, A11 | same `SessionPersistence` tests pass on both backends, including re-seeding a `Session` from what was read; a profile resumes through whichever backend it mounted |
| P5-09 | OTel telemetry sink | P1-14 | — | redaction still precedes export |
| P5-10 | `ph agents \| attach \| send \| schedule \| status \| doctor \| shutdown` | P5-02 | — | each round-trips |
| P5-11 | **Lingering detection**: doctor reports `$XDG_RUNTIME_DIR` derivation + `loginctl` state, names `enable-linger`; "socket path removed" diagnostic | P5-01 | I-6 | simulated session end → clear diagnostic, not a silent failure |
| P5-12 | Non-guarantees documented (no per-root memory cap, no rolling restart, no cross-root crash containment; one daemon per user); `dev-notes/phase-5.md` | P5-01 | I-2, N5 | doctor prints the worker model; docs reviewed |
| P5-13 | **A request direction from server to client, and a pending state that outlives both.** The protocol is one-directional for anything expecting an answer: `respond()` shapes client→server replies, and `notify` is server→client with nothing awaiting it. But `ModalHost` — the front end's whole contract — is two calls that **wait for a person**: `ask_approval(request) -> (answer, reason)` and `ask_question(question) -> str \| None`. In-process those are a direct call into the screen; over a socket they need the reverse direction, which does not exist. So: server-minted ids, a pending table on the connection, a client-side dispatch table, and the envelope shaping reused from `respond` rather than a second one. **But the question that decides this row is what happens when the human walks away, and the answer is already written down.** `ph/seams/approval.py`'s own docstring: *"Re-asking on resume falls out of the log rather than being remembered: `approval/asked` without a matching `approval/decided` **is** the pending state, so a crash between the two leaves a question a resumed session can find and put back to the human."* So a detached client does not force a resolution at all — the daemon may stop entirely, and a later attach resumes into the same unanswered question. That is strictly better than resolving on disconnect: a gate that becomes a denial because a terminal closed still loses the human's actual answer, and "wait indefinitely" only works while the process does. **Three concrete pieces of work, none of them the protocol.** (1) `pending_approvals(session)` is exported, tested, and has **no production caller** — the fold exists and nothing asks it, so resume and attach must. (2) **Crash repair contradicts it**: `repaired()` closes an open turn with a synthetic `turn/end{interrupted}` and does not mention approvals anywhere, so a session parked on an unanswered ask resumes with the ask still pending and the turn it belonged to already closed — an answer with nothing to return into. Repair has to tell *parked* from *interrupted*, which it can do from the same fold. (3) **Questions are not durable at all.** `user-question/ask` is a `ctx.waterfall` over a live `Context` and appends nothing; there is no `question/*` in the session vocabulary. A question in flight when the daemon stops is simply gone, and the turn that asked has no record it did — so questions need the same asked/answered pair approvals already have before any of this covers them. Two attached clients also need an addressing rule rather than a broadcast. **A root parked on a human is idle, and should be released**: P5-05 sweeps it, the daemon shuts down, and attaching re-asks — which is the arrangement working rather than a hole in it. | P5-02, P1-19, P5-05 | A10, A11, E1, E13 | a session parked on an unanswered approval survives the daemon stopping and re-asks on attach; repair leaves a parked turn open rather than closing it interrupted; a question survives the same round trip; two attached clients do not both get asked |
| P5-14 | **The TUI becomes a daemon client, so closing the terminal stops watching rather than stops working.** Today `ph` and `ph daemon` are two separate worlds: `HarnessSession` mounts its *own* `Context`, session and agent in-process, and `grep daemon` across `tui/` finds one comment. **P5-01's gate is phrased "TUI close leaves the root running" and was met by a socket client standing in for the TUI** (`test_a_root_keeps_running_after_its_client_disconnects` drives a `DaemonClient`); this row is where that sentence becomes true of the thing it names. **Rendering is nearly free and the interactive surfaces are not.** `TuiEventAdapter` is already fed by `SessionEvent`s rather than by the `Context`, and the daemon already ships `event.to_wire()` on `session.event`, with `session/attach` returning where the live stream begins and `session/snapshot` paging the gap from a cursor — P5-02 built exactly that catch-up. What does *not* survive the socket is everything that reads the `Context`: `status_readings()` resolves the `tui_status` seam and `config_rows` comes from `Loader.dump()`, so both become projections on the wire; and the modals need P5-13. **The session picker gains a second source and a third state.** `session_summaries(sessions_dir)` already lists what is on disk and `SessionSummary` already carries `cwd`; `sessions/list` reports what is *mounted*. Merged, a session is **running**, **passivated** (P5-05 released it; it wakes on attach) or **stored only**, and a picker that cannot tell those apart is one that offers to resume something already running. **`cwd` has to be populated first**: the field exists on `SessionHeader` and the daemon leaves it `None`, so a daemon-created session is unscopable to a repo today and the picker's `cwd` column is blank for exactly the sessions this row is about. **One daemon per user, hosting roots from many repos** — recommended over a daemon per repo, and worth stating because the phrasing invites the other reading. `$PH_RUNTIME/daemon.sock` is per boot and per user, `_clear_stale` already refuses a second daemon on it, P5-03's lease is per *session file* rather than per repo, roots already mount their own `Context` and workspace so two repos are already two deployments, and "see every running session" is then one `sessions/list` instead of discovery across N sockets. A daemon per repo buys crash isolation between repos and costs a socket-naming scheme, a discovery mechanism and a fan-out client; recorded as the alternative rather than dismissed. | P5-13, P5-05, P5-10 | I-2 | starting work in the TUI and closing it leaves the root running, and reopening finds it — the P5-01 gate, driven through the TUI; the picker distinguishes running, passivated and stored; sessions are scopable to the repo that made them |
| P5-15 | **A seam for a row to contribute front-end surface, not just front-end text.** The pattern half-exists and is worth finishing rather than inventing: `status_readings` reads a `tui_status` registry a row provides, so the footer already renders what the rows want to say without knowing who said it, and `diagnostics.contribute` (P4-12) is the same shape for `ph doctor`. What no row can contribute is a *surface* — a panel, a screen, a keybinding. **The constraint that shapes it: a contribution cannot be a widget.** Once P5-14 lands, the front end may be in another process, so a row that handed back a Textual object could only ever work in-process — which is the one arrangement this system is moving away from. A row contributes a **declarative description** the client renders, which is the same discipline that makes the contribution testable without a terminal and lets a second front end (a web client, an editor plugin, `ph agents`) render the same panel differently. That also settles where the boundary is: anything a row wants on screen crosses as data on the existing protocol, and a front end is whatever speaks it — which is why P5-02 took dsh's method names rather than inventing a vocabulary, and is the honest answer to "can we attach other front ends like a plugin": *over the protocol, and rows contribute into it*. | P5-14, P4-12 | H4 | a row contributes a panel that the TUI renders and a headless client can read as data; removing the row removes the panel with no edit to the front end |
| P5-16 | **Dropped** — built, measured, and removed, which is the outcome worth recording. **The prompt layer already delivers this list.** `_project_context` composes a `todos` section whenever it changes, and `tool-todo` is its only producer — so the nudge could only ever fire when the model *already had the list in context*. A probe of one run found the same `## Todo list` text twice in one session: at the prompt snapshot, then again in the steer. The row's premise, that the nudge is "the one moment the loop composes a message", was false; the layer below composes the same one. The cost was duplicated model-visible tokens on every continuation, against a benefit that exists only when compaction has shadowed the first copy — which nothing checks. **What the attempt was worth keeping for**, all landed independently of the feature: the nudge steered with `source={"kind": "user"}`, and the TUI draws an untagged message as a *user* row, so the transcript attributed the harness's own steering to the person reading it — it now carries a `PluginSource`, as every other plugin→inbox message in the tree does. And the review that killed the feature also established where the fold would have belonged had it moved: not `ph/seams/`, which is an index of seams ("Definition, Provider, Consumer in one module each") and where it briefly landed, but `ph/session/` beside `request_header` — a fold over the log, in the package that owns the log. `subagent_roster` was not the precedent it looked like: that fold sits *inside* the module declaring `ctx.subagents`. `todos_of` stays in `ph-stabilize` with the tool that writes it, because with the nudge gone ph-core has no reader and a fold nobody in the package reads is weight in the wrong place. **The distinction the row was written around still holds and is worth keeping written down:** a todo list is the model's claim about intent and a gate is a process exiting zero, so a goal must never settle on a checklist the model ticks itself. That is already structural rather than conventional — every `goals.settle` in `keep_going` is followed by `return`, so nothing on the deciding path can read a list — and `test_a_finished_todo_list_does_not_settle_a_goal` went with the feature, so the guard is the code shape rather than a test. Worth a sentence in P5-12's non-guarantees page, where the two "plan" surfaces should be told apart for whoever meets both. Original scope: **The autonomous nudge carries the open todos, and still never asks them whether it is done.** When gates disagree that an agent has finished, `_nudge` currently tells it the objective and the gate results and nothing else — while the model's own plan, the thing it wrote down to stay oriented, sits one fold away. Folding the open todos into that message tells the model what *it* thought was left in the same breath as the evidence it is not finished, which is the moment that information is worth most and the only moment the loop composes a message at all. **The line this row must not cross is the one `/autonomous` exists for.** The nudge *reports* todos; it never reads them to decide an outcome. The model marks its own todos `completed`, so a goal that settled on a full checklist would stop when the model **said** it was done — precisely the failure gates exist to prevent, and precisely why a goal with no gates records that fact in its own outcome rather than letting the model's judgement pass for a check. Two surfaces, two authorities: the todo list is the model's claim about intent, a gate is a process exiting zero. **The obstacle is a package boundary, and it is the interesting part.** `todos_of` lives in `ph-stabilize`; `/autonomous` lives in `ph-core`, which cannot import it. But `todo/write` is already **ph-core's** vocabulary — declared there as *required, not ignorable*, because "the difference is model-visible" — and `todos_of` is a fold over that event using nothing but `Session.latest` and `thaw_json`, both ph-core primitives. Nothing about the *reading* is stabilization-specific; only the tool, the prompt section and the one-write-per-turn rule are. So the fold moves down beside the event type it reads and the tool stays where it is — which is `subagent_roster`'s arrangement precisely, and for the reason that seam gives out loud: the fold lives in the seam "because the two consumers live in different packages", while the bundle that *produces* the events keeps producing them. **Hand-rolling a second reader in ph-core is the one option to refuse**: a whole-list-replacement fold spelled twice is two projections of one list that can disagree, which is A11's own case and a mistake this tree has already caught three times in the same shape. Render through `render_todo_list` for the same reason `TodoItem` refuses an `activeForm` — *"a second phrasing of the same task would be a field with no reader"* — so the nudge and the prompt show one list. **Absent is not empty.** `tool-todo` is an opt-in `ph-stabilize` row and `goals` is in base, so a profile can have goals without todos; the nudge must then say *nothing* about planning rather than "no todos", because an unmounted row is not a model that failed to plan, and a message implying otherwise would teach it to write a list nothing reads. | P5-07, P4-01 | A11 | dropped; the prompt layer covers it |

### Phase 6 — Hardening & docs *(ongoing)*

| ID | Work item | Depends | Delivers | Gate |
|---|---|---|---|---|
| P6-01 | Invariants registry + runtime invariant plugins (session, loop, tools, scope) incl. **"every projection equals its fold"** | P3-15, P3-16 | A11, I6 | hand-edited projection trips the invariant |
| P6-02 | `ph events` producer/consumer matrix; config catalog; 100 % coverage gate on `ph-core` | P0-04 | — | CI gates |
| P6-03 | Benchmark: prefix-cache hit rate and tokens/turn, recorded RLM session with vs without stabilization | P4-15, P3-23 | — | report checked in |
| P6-04 | **`sandbox-local`**: `bwrap` → Landlock (Linux), Seatbelt (macOS); `confine()` for the runtime and shell; `enforcement: full \| partial`; denial signatures | P1-19 | E11 | absolute-path write refused under `sandbox` (closes E13's second half) |
| P6-05 | `readonly-scratch` kind via `workspace-write` rooted at scratch; **degradation path**: `partial` → refusal under strict / reported downgrade otherwise; `access="read"` → `worktree-ephemeral` where no enforcing backend | P6-04, P4-08 | E3, E11 | read child cannot write the repo; degradation reported in doctor |
| P6-06 | Docs test: no tier described as bounding writes it does not bound | P6-04 | E1 | CI |
| P6-07 | "Running pH in a container" page (what to mount; fd 3 crosses as an inherited descriptor; `CredentialRef`; tiers layer inside) | P6-04 | N6 | reviewed |
| P6-08 | `code-runtime-quickjs` (D16) — **only if** a profile needs sandboxed code mode | P1-06 | — | conformance suite passes against it |
| P6-09 | `ph harness report` — analytics only ("these skill entries wrap the same import") | P3-16 | H4 | no scaffold command exists |
| P6-10 | Cookbook: adding a plugin / tool / adapter / seam; every seam page | all | — | every seam has a page |
| P6-11 | Windows in CI for every lifecycle and path test | P3-22, P0-16 | F3, I-1 | matrix green |
| P6-13 | **Adapter cache breakpoints**: `cache_control` on the Anthropic adapter at the stable boundaries (end of `system`, end of `tools`, and the last retained message), so A12's prefix stability and P4-03's replayed summarize envelope actually bill as prefixes. Found while landing P4-03: `_body` in `anthropic.py` sends no markers at all, and Anthropic's caching is opt-in — so every prefix-stability decision in the harness currently pays off on DeepSeek and OpenAI-compatible routes and nowhere else. The gate is not a shape assertion but a *number*: `TokenUsage.cache_read_tokens` is already carried on `assistant/message` and on `compaction/summarized`, so a recorded session can assert the second request reads cache rather than writing it. Breakpoints are a scarce resource (four at Anthropic) and placing them is the whole design — one at the end of a message list that grows every turn is a breakpoint that never hits. | P1-24, P4-03 | A12 | a recorded two-request session reports `cacheReadTokens > 0` on the second; the compaction call reports it too |
| P6-14 | **Agentic summarization**: the compaction summarizer as an agent turn rather than one call, so a model can chunk a long range, map-reduce it, or read the spilled history file — the RLM shape of the same job. Deferred to its own row because three things have to be settled first and none belongs in P4-03. **Whose loop**: compaction runs inside `agent/pre-step`, and `ReactLoopAgent.run` refuses to re-enter, so this cannot be the same agent — it is a `ctx.subagents` child or a second handle on a scratch session. **Whose log**: the summarizer's tool calls in the compacted session pollute the history they are summarizing; in a scratch session, usage attribution and `session_id` change. **Whose namespace**: under Code Mode the tool is `run_code`, so a summarizer would execute model-written Python against the session's *live* kernel — it needs a fresh namespace or it can mutate the user's variables. Needs `limits` for a budget: an unbounded summarizer is unbounded spend inside another turn's step. | P4-04, P4-03, P3-11 | G4 | a summarizer that chunks a range produces a summary within its budget; a cell it runs cannot reach the session's namespace |
| P6-15 | **Extend the open-time blob sweep past the kernel (F7).** `SpillStore.sweep` exists and is correct, but its only caller is `ph_rlm.snapshot`, which sweeps `kernel/<namespace>` owners against the locators its own events reference. P4-02's offload files and P4-03's conversation history are written under the *session id* owner and are never swept, so a crash between a blob write and the append that names it leaks a file forever. The fix is not another per-producer sweep — that is how this one ended up knowing about only one producer — but a locator fold every producer contributes to, which is the same shape `referenced_locators` already has for one type. | P4-02, P4-03, P3-14 | F7 | a blob whose event never landed is gone after the next session open; one whose event did land is not |
| P6-16 | **`hitl` should gate on a declared capability, not on a tool name.** `interrupt_on: dict[str, Rule]` filters by `execution.name` *before* any argument matching, so the arguments-not-the-name argument P4-05 makes — and makes correctly — covers only *what* is matched, never the key. Two rows in the same bundle reject that shape in their own docstrings: `compaction`'s truncation pass ('a deployment that renames them or an MCP server that adds its own would silently get no truncation, with nothing failing') and `offload`, both of which use declared capability + scope-aware `ctx.tools.get` + config as *override*. Here the silent no-op is fail-**open** on an approval gate, and it lands hardest on the motivating case: the Code Mode transport is profile-renamed, so the config key for the one surface the module docstring says motivates the design is `ipython` under `rlm-stable` and `run_code` elsewhere. `ToolDefinition.is_concurrency_safe` is already the exact shape wanted — a producer-declared, per-call, argument-inspecting predicate — so 'a cell is not a tool, one call may do anything' is the argument *for* declaration, not against it. Deferred rather than done in P4-05's cleanup because it changes the ported upstream config surface and touches every tool that would declare. | P4-05, P1-08 | G6, I-2 | a profile that renames a gated tool still gates it; a tool that declares irreversibility is asked about with no config naming it |
| P6-17 | **`_walk` rebuilds a relative path per file with `Path.relative_to`.** Pre-existing, measured while reviewing P4-06: line-by-line, `relative = path.relative_to(base).as_posix()` is **23.5 µs per file, 62% of a 430 ms walk over this repo's 11 338 files**, because `relative_to` allocates a `Path` per root segment. `os.path.join(current, name)[prefix:]` is 0.32 µs and takes the walk to **35.6 ms — 11x** — with byte-identical output including ordering, verified across `**/*`, `**/*.py`, `*.md`, `**/seams/*.py` and `packages/*/src/**/*.py`. Deferred out of P4-06's cleanup because it rewrites the walk rather than the row, and a path-handling rewrite deserves its own change with its own tests. One caveat for whoever takes it: `_walk`'s `relative` is relative to `base`, which is only `fs.root` when `glob`/`grep` are called without `root=`, so it cannot simply be handed to the concealment predicate. | P4-06, P1-17 | — | `glob`/`grep` over this repo under 50 ms; output identical to today's across the pattern set above |
| P6-18 | **The fs seam's two policy mechanisms ignore `Context.reaches`.** `Context.reaches` calls itself "the one visibility rule, shared by event dispatch and by every scoped registry", and three registries obey it (`context.py`, `compaction.notes`, `system_prompt/assembly`). Neither of P4-06's does, in opposite directions: `FsService._gate` waterfalls with no `scope=`, so an agent-scoped listener on `fs/read-intent` never fires at all — where the sibling gate `ToolRuntime` built passes `scope=execution.scope` — while `FsService.hide` records no owner, so an agent-scoped `permissions-fs` conceals from *every* agent's `glob`/`grep`. The identity needed is already on the intents (`ReadIntent`/`WriteIntent`/`EditIntent` all carry `agent`, and `agent.ctx` exists); the enumeration half additionally needs an agent threaded through `fs.glob`/`fs.grep`, which have no such parameter. Deferred out of P4-06's cleanup because scoping the dispatch changes behaviour for two pre-existing waterfalls. **Blocks P4-10**, whose whole point is a `permissions-fs` row per worktree over P4-08's per-agent roots — and the same reasoning applies to `FsPermissions.root`, snapshotted at mount, which a per-agent worktree root would silently invalidate. | P4-06, P4-08 | E2 | an agent-scoped rule conceals from that agent alone; a root-scoped one still reaches every agent |
| P6-19 | **`hide` can skip a file but cannot prune a tree.** `_walk` already prunes directories before descent for `_IGNORED_PARTS`, and its docstring makes the argument ("pruning `dirs` in place means an ignored tree is never entered") — but the concealment predicate is consulted only over `files`, so `deny secrets/**` still descends into `secrets/` and pays a call per file, and `permissions-fs` cannot express "never enter this tree" even though the walk knows how. The deeper form is one contribution point for what the walk skips, taking `yield | skip | prune` over any path including directories, with `_IGNORED_PARTS` registered through it as the built-in: two ways to drop a path collapse into one, the ignore list stops being a module constant, and the `.gitignore` row has somewhere to land instead of arriving as mechanism three. Lower priority than it looked when filed — P4-06's `_spellings` fix cut the per-file cost 21x, so this is now a capability and tidiness argument rather than a performance one. | P4-06 | — | a deny rule on a directory stops the walk entering it; the ignore list is deployment config |
| P6-12 | **`claim_key`'s owner defaults to the activating row, not to the seam.** Found while reviewing P4-17. `claim_key`/`claim_slot` take `scope or self.ctx`, and `self.ctx` is the *service's* context — so a registration made by a row is an effect of the **seam** and outlives the row that made it. **1 of 38 registration call sites in `packages/*/src` passes `scope=`**, and it is the one P4-17 added: every `ctx.tools.register(...)` in ph-rlm's `bindings`, `messaging`, `context_loader`, `skills` and `kernel/manager` rows, every `ctx.commands.register(...)`, `ctx.skills.register(...)` and `ctx.code_runtime.register_code_namespace(...)` included — so I2 currently holds only where someone remembered. Reproduced in fifteen lines: mount a row that registers a command, dispose the row's fork, reconcile, and the command is still there. **P0-02's gate ("disposal unwinds every effect") passed because it exercises `ctx.effect` and `add_disposer` directly** — the seam path, which is how nearly every real registration is made, was never its subject; the same shape of unfalsifiable gate this plan has now hit at P3-23 and P3-24. The fix belongs at the one choke point every seam already funnels through rather than in six modules: cordis constructs exactly the right owner in `Context._activation_scope` and hands it to `apply`, so set a `ContextVar` around that `await`, expose it as `Context.current_scope()`, and default `claim_key`/`claim_slot`'s owner to it — falling back to today's behaviour outside any activation, which makes the change strictly additive. `scope=` then means what it should, *"register on someone else's lifetime"* (the agent-shadowing case), instead of a rule six modules have to remember. **Why it is its own row and not a line in the P4-17 diff**: it changes what unwinds when for every seam in the tree; contextvars propagate into tasks spawned from `apply`, so a registration made from a background task acquires an owner it does not have today; and Phase 5's unmount paths — passivation, root-task crash recovery, lease disposal — are the first consumers that depend on the answer, so it wants to land before them | P0-02 | I2 | a row that registers through a seam loses that registration when the row unmounts, **enumerated over every seam exposing a `scope=` parameter** rather than a hand-written list, so a new seam cannot join unchecked; the same test fails against the pre-fix build; a registration made outside any activation still lands on the service's context; an agent-scoped registration still shadows a global one by name |
| P6-20 | **A rung between `worktree` and `sandbox`, because an overlay is neither.** `ContainmentTier` is `advisory \| worktree \| sandbox` and `WorkspaceKind` is `shared \| worktree \| worktree-ephemeral \| readonly-scratch`; both are closed `Literal`s whose consumers (`project_access`, `fresh_root`, `TIERS`) `match` exhaustively, so adding a member fails to type-check until every site classifies it — the same pressure the session vocabulary applies, and the reason this row precedes P6-21 rather than being folded into it. **The honest column is the whole point, and the boundary is in two halves.** AgentFS v0.6.4 states its own contract on entry — *"The following directories are writable: &lt;cwd&gt; (copy-on-write) … Everything else is read-only"* — so a write inside the working tree **succeeds into the delta layer** (the agent sees it, the host does not), a write to an allowed path passes through for real, and a write anywhere else is **refused**, not captured. That is two different failure modes under one rung, and both have to appear: `bounds` is every write including absolute paths; `does_not_bound` is network and process, *plus* — for the tree itself — the agent's belief that its work landed anywhere durable. `sandbox` says "refused at the kernel" and an overlay says "refused outside, invented inside", which is a different bargain and must read that way in `TIERS` and `ph doctor` or E1 is false in the one place a person looks to check it. `buys` is host isolation plus instant branch and discard. (The read-only half is taken from the tool's own banner; it is not yet exercised against a working host — see P6-21 for why that could not be done on the development machine, and treat it as a claim to verify rather than a fact until a test asserts it.) **Checkpointing stops being git-shaped.** `workspace_git.checkpoint` gates on `kind in ("worktree", "worktree-ephemeral")` and captures with `git write-tree` against `GIT_INDEX_FILE`; an `overlay` workspace's restore point is the overlay's own delta (`agentfs diff`), so capture/restore has to dispatch on the provider the way `acquire`/`reclaim` already do. P5-04's `_restore` already returns `False` for a workspace with no restore point, so the degraded path exists before this row needs it — but `/revert` would silently do nothing on an overlay, which is exactly the class E1 forbids. | P4-09, P6-04 | E1, E13 | P6-06's docs test extended: the new rung's description matches what it bounds, and a workspace kind with no restore mechanism refuses `/revert` rather than no-oping |
| P6-21 | **`workspace-agentfs`: Turso's AgentFS as a second containment provider.** A copy-on-write overlay in which **the host filesystem is the read-only base** and every change lands in a delta layer, so an agent gets an isolated view of the whole tree without a worktree — `agentfs run --session <id> --allow <path>… <cmd>` (or `exec` for one-shot), driven through `ctx.subprocess` because AgentFS ships as a **CLI, not a library**. Linux uses FUSE plus user *and mount* namespaces — which is what makes it fail closed on hardened hosts rather than degrade — macOS uses NFS plus Apple's Sandbox; **Windows is unsupported**, so P6-11's matrix skips this row the way the `pyturso` backend (P5-08) is skipped for the same reason. **`--allow` is load-bearing, not a convenience.** A contained agent must still write its own trace: `$PH_HOME/sessions` and `$PH_RUNTIME/daemon.sock` have to be allowed through to the real filesystem, or the session log lands in a delta layer that is discarded — and a log that does not reach disk breaks A10's write-ahead ordering and A1 with it, silently, since the agent sees its own writes succeed. Under the standing rule that **a child may never hold more than its parent**, a subagent's allow-list must be a subset of its parent's, computed the way `Grant` already narrows skills and tools rather than assembled independently. **The probe must gate registration, not acquisition** — this is the structural finding, and it is easy to get backwards. `register_provider` is `claim_slot`: exclusive, one provider, *"a provider is already registered"* if a second tries. So a row that claims the slot and then declines every `acquire` does not fall back to `worktree` — it falls back to `shared`, i.e. **advisory**, and a deployment that asked for more isolation silently gets none. AgentFS also ships **no detection command** (availability is "implicitly determined by attempting mount operations"), so the probe is a real `agentfs run … true` against a temp directory, run once at mount, cached, and contributed to `ph doctor` through the diagnostics seam (P4-12) so "why am I on worktrees" has an answer. **Nothing is installed, ever** — the probe reports a missing binary or an unavailable backend and declines; that is the standing provisioning rule, and it is also why the suite must stay green on a machine without working AgentFS: a fake provider in `ph.testing` for the seam contract, and a real integration test skipped when containment is unavailable, the shape `test_adapters.py`'s `no DEEPSEEK_API_KEY` skip already uses. **The probe asserts containment, not availability, and that is not pedantry — it was measured.** With `agentfs v0.6.4` installed, `agentfs run --experimental-sandbox` **exits 0, prints no error, and writes straight through to the host**: it sandboxes only its own `/agent` mount, so a canary written inside it was mutated on the host. A probe that checked the exit code would have claimed the `overlay` tier while the agent had no containment at all — E13 violated by a mode whose name says experimental and whose exit status says fine. So the probe writes a canary into a throwaway base, runs the command, and claims the tier **only if the host copy is unchanged**. **Two independent environment failures, both mainstream, both found on the first machine tried:** `agentfs run` needs user and mount namespaces and dies with `Failed to make mounts private: Permission denied` under `kernel.apparmor_restrict_unprivileged_userns=1`, which is the **Ubuntu 23.10+ default** (`unshare -Urm` fails there too); and `agentfs init --base` + `agentfs exec`, the FUSE path that needs no namespaces, fails reproducibly with `connection pool timeout: no connections available` on a **ZFS** root — the same class of backing-store sensitivity P5-08 records for `pyturso`'s `.tshm` sidecar, which refuses a list of filesystems outright. So the development machine has the binary, has `/dev/fuse` and `fusermount3`, and still cannot run a contained agent by either path — which is the case the probe exists for, and a reminder that "is it installed" answers the wrong question. Encryption (`--key`/`--cipher`) is available and worth a line for a shared host; remote sync is a non-goal and is mutually exclusive with local encryption anyway. | P6-20, P1-19 | E1, E13, I-7 | with the backend absent, the row declines and the effective tier is reported as `worktree`/`advisory` — never claimed as `overlay`; with it present, two agents writing the same path do not collide, and each one's session log still reaches `$PH_HOME` |
| P6-22 | **Measure worktrees against AgentFS before preferring either.** AgentFS publishes **no benchmarks**, so "copy-on-write is faster" is a hypothesis with a direction, not a result — and the direction cuts both ways. Acquire should favour the overlay: P4-09 measured `git worktree add` re-hashing an 11 000-file index at **2.6 s**, paid per agent, which is why the checkpoint index is seeded from the worktree's real one. Steady-state I/O should favour the worktree, especially on macOS, where AgentFS's backend is **NFS** — a loopback network filesystem in the path of every syscall — against native files. So the comparison is a pair, not a number: **acquire, first checkpoint, restore, and a real build/test workload**, per platform, at a realistic tree size. The result decides the *default per platform*, which is a profile row rather than a code branch — the point of making this a provider at all. Recorded like P6-03, in `dev-notes/`, with the tree size and platform stated, because a ratio without them is not a finding. | P6-21, P6-03 | — | report checked in; the chosen default per platform cites it |

### Phase 7 — Multimodal *(new; scoped in review of P4-03)*

Reached from a P4-03 question — why compaction renders an image as `[image]` — which turned out to be the
smallest of three holes. `ImageBlock` was ported as *vocabulary* from dsh's `ContentBlock` union (port plan
§ "Vocabulary ported from `packages/llm/llm/src/types.ts`") and never given a producer, an adapter
serializer, or a renderer. Meanwhile pH's *other* media path — the kernel's `display` frame — is MIME-typed
and media-general, and prime-agent's `attach_image` is slated to port onto it. So the harness holds two
answers to one question, one raster-specific and dead, one general and half-built. This phase reconciles
them.

**The vocabulary is one MIME-keyed block, not a family.** Providers do branch on the wire (Anthropic
`image` vs `document`; OpenAI `image_url` vs `input_audio` vs `file`), and that branching belongs in the
adapter where the differences live rather than in a union that grows a member per medium. `ImageBlock` is
retired rather than deprecated: nothing produces it, so nothing breaks.

**Not everything is a content block, and saying so is half the design.** What a provider *ingests as
content* — images everywhere, PDFs at Anthropic and Gemini, audio at a few — needs a block, serialization
and capability negotiation. What is merely a file — a CSV, an archive, a log — belongs in the workspace with
a path the model can `read`, which pH already answers three ways (`ctx.spill_store`, the `read` tool, and
`rlm-context-loader` for a corpus). A block type that accepted anything would encode a capability no
provider has.

| ID | Work item | Depends | Delivers | Gate |
|---|---|---|---|---|
| P7-01 | **Landed in full** for the human door; the model-initiated tool and `ph attachments gc` remain. Ingest is `ph -p "…" --attach <file>`, reading paths directly with the harness's own permissions (I-9's human half) and building one `user/message` carrying the text and the media; a profile with no store refuses **loudly**, because a person who passed `--attach` and got a plain text turn would have no way to tell their file was never sent. **The accept/degrade decision moved above every adapter** into `media-degrade`, a `ph-core` row on `llm/stream`, and the reason is a bug the first shape had: answering per adapter meant the *fake* adapter answered not at all, so a route declaring no media received media anyway — silently, which is the one thing this row exists to prevent. Rewriting the request there is legitimate and the invariant says so itself: `agent-loop-invariant` is registered `prepend=True` on the same waterfall precisely because it is about what the *loop* built, not what middleware makes of it. What is left per adapter is what genuinely differs — `image` against `document`, `image_url` against `input_audio` — and each renderer is now **total over its own vocabulary**: a MIME it has no shape for becomes a pointer rather than being dressed as an image, which is not a second copy of the accept policy but the narrower honesty an adapter still owes. `attachment/degraded` records the refusal for the person, once per distinct refusal rather than once per request (`Session.latest`, not a scan), and the TUI draws it beside the media labels now shown on the message that carried them. Original: **Foundation landed** (`ph/seams/attachments.py`, `AttachmentRef`/`MediaBlock`, `ResolvedModel.accepts`, both adapters, media token estimates). What remains in this row: the ingest producers (CLI/TUI attach, a tool, the kernel `display` frame), the **session-visible** degradation notice, the TUI card, and `ph attachments gc`. **A route declares its own media capability**: `accepts` and `max_attachment_bytes` are row config (`Config` on the Anthropic row, `ProviderProfile` on each OpenAI-compatible route) rather than module constants, because one `openai-compatible` row serves many routes — a hosted gateway, Groq, Together, a local llama.cpp — that do not agree about media, and a constant made every one of them promise images and a 20 MB ceiling. A text-only route sets `accepts: []` and gets honest pointers instead of requests the far end rejects. Two findings while landing it. **`ctx.fs` has no binary read**, so a model-initiated attach cannot go through the door I-9 requires — the tool producer has to add one rather than reach around `permissions-fs`, and `save_path` is documented as the human door only. And the OpenAI renderer **reintroduced the very silent drop this row exists to end**: it chose the string form when no rendered part was non-text, which is true of a *degraded* attachment, so the pointer fell through to `text_of` and vanished — caught by the gate, and the condition now keys on the message carrying media rather than on what the parts ended up looking like. Original scope: **`ctx.attachments` + `MediaBlock`, end to end.** A content-addressed store at `$PH_HOME/attachments/` — the mechanics `ctx.spill_store` already has (`locator_for` before the write, digest-named files), but **its own seam and its own directory**, because the lifecycles differ in the way that matters: a spilled tool result is a forwarding address for content the log still holds, while an attachment is content the log only *points at*. Someone will eventually clear the spill directory to reclaim space and that must not delete conversation. Addressed **globally, with no owner subdirectory** — a digest is a digest, two sessions attaching one photo share one file — which also settles forks, since a child's log references the same digests as its parent rather than a directory the parent owns. Never auto-collected; collection is an explicit `ph attachments gc` folding every session, deliberately unlike F7's sweep. `MediaBlock {type: "media", mime, attachment}` joins the content-block union and `ImageBlock` leaves it. Ingest is split by who asked (I-9). `ResolvedModel` grows **`accepts`** and per-route **limits** (max bytes, max dimension) — limits and not just types, because the transform row is optional and the no-plugin path has to degrade honestly rather than fail at the provider. A block a route cannot take **degrades to a text pointer with a logged notice**, never a silent drop and never a hard failure: a session started on a vision model and resumed on a text one must still open. Both adapters serialize inline base64 today — and this row is what makes their current behaviour a bug rather than an absence, since `_to_anthropic` omits any block it does not recognize and `_to_openai`'s user branch is `text_of(...)`, so a media-only message reaches the wire as an empty text block. **Media stops costing zero tokens**: `TokenMeter.measure` reads `.text`/`.arguments`/nested `.content` and a media block has none, so today a conversation full of images reports no pressure and G2/G3's character thresholds never fire on it — a per-MIME estimate (images ≈ `w×h/750`, audio per second, PDFs per page) lands with the block. Producers wired from day one: CLI/TUI attach, a tool result, and the kernel `display` frame — that last one is where the two media routes finally become one. | P1-03, P4-06 | I-9 | a PDF attached from outside the repo reaches an accepting route and is refused-with-a-pointer on one that cannot take it; a media-only message is never sent as empty text; the same file attached twice stores one blob |
| P7-02 | **`media-transform` (optional row)**: downscale and transcode to a route's declared limits. Its own plugin because it carries a backend — `ffmpeg`, ImageMagick or Pillow — that a deployment running pH as a headless task agent has no reason to install, and the seam already has the shape for an optional provider (`ctx.sandbox`, `ctx.code_runtime`). **The store keeps originals; the transform runs per request**, so a route with a tighter cap never makes the archived artifact lossy and a later, better model can be given the full-resolution image. Without the row mounted, an over-limit block degrades through P7-01's pointer path rather than silently being sent and rejected — which is what makes the limits, not just the accepted types, part of `ResolvedModel`. | P7-01 | — | with the row absent an over-limit image degrades and says so; with it mounted the same image is sent within the cap and the stored blob is untouched |
| P7-03 | **Provider file APIs and video** (second pass). Upload-and-reference instead of inline: Anthropic's Files API, OpenAI `file_id`, Gemini's Files API — which video effectively requires everywhere, and which is why it is a separate row rather than one more MIME type. Brings the one piece of genuinely non-derivable state in this phase: a `(provider, digest) → handle` cache with a TTL, expiry detection and a re-upload path. Where that cache lives is the row's real question — it is refreshable, so it is not obliged to be in the log, but "state lives in the log" is the harness's default and departing from it needs the argument written down. | P7-01 | — | a video reaches a route that accepts one; an expired handle is re-uploaded rather than failing the turn |
| P7-04 | **Compaction and offload learn about media** (P4-03's loose end). A media block in a shadowed range writes its `attachment` id into the history file rather than the string `[image]`, so the file the replacement message calls "the full conversation history" actually is one; deepagents' `_MEDIA_REFERENCE_SUMMARY_PROMPT` becomes portable rather than inapplicable and is spliced at the same `<messages>` seam as the state notes; and G2/G3's thresholds read the per-MIME estimate rather than a character count over text. | P7-01, P4-03 | G2, G3, G4 | a compacted range's media is recoverable from the history file; an image-heavy conversation triggers compaction |

---

## 5. Engineering rules that hold across every phase

1. **Declare, never derive.** Aliases are fixed at class definition (`WireModel`); a name is never reconstructed from a wire string. Same for event modes (`events.declare`) and tool outputs (mandatory `output`).
2. **Log first, act second.** `tool/call` before execution; the snapshot event before the blob; `rlm/child-admitted` before the handle returns; `workspace/checkpoint` before the mutating run.
3. **Every artifact through `ctx.effect()`.** A lint forbids `subprocess.Popen`, `tempfile.mkdtemp`, `git worktree add` and lock acquisition outside the seam.
4. **The program and the child are hostile.** Every inbound fd-3 frame is rebuilt; every binding argument is snapshotted as lossless JSON; the guest trusts the host, the host trusts nothing.
5. **Fail closed at the seam.** `SANDBOX_UNAVAILABLE` rather than unconfined; `unavailable` approval denies; a `partial` backend under `strict` refuses; an unresolvable `reference` is rejected.
6. **State what is not enforced, next to where it would be assumed.** The tier table, the permission row's validation, `ph doctor`, `/revert`'s output and the first-run notice all carry their own caveat. A caveat only in the docs is a defect.
7. **Constants come from Appendix D, exposed as row config.** No hidden `??` defaults inside a `run()`.
8. **Every phase ends with its gate module green on three OSes** and a `dev-notes/phase-N.md` recording what was traded.

---

## 6. Definition of done, per phase

| Phase | Done when |
|---|---|
| 0 | P0-14's invariant fires on a bypass; the wire round-trip property passes; `ph doctor` prints three resolved roots on all three OSes; a print-mode run writes a JSONL that dsh tooling reads |
| 1 | dsh's pipeline ordering invariants pass; crash-repair fixtures resume; the prefix-stability test passes on a recorded session; registering a namespace provider without a snapshot hook fails at `provide()`; `SIGTERM` unwinds within grace |
| 2 | pilot tests drive every modal; resume rebuilds the transcript from events alone |
| 3 | **P3-21 (governance gate) and P3-22 (runtime conformance) both green on three OSes**; fork restores the boundary namespace; harness re-derives byte-identical; the fixture-replay report is checked in with its diffs triaged; the trajectory view opens a stored log with nothing mounted and accounts for every known event type |
| 4 | P4-16 green; a denied run reverts exactly; an inside-worktree cell prompts zero times; `containment.strict` refuses on a backend-less host; doctor's tier output matches the table |
| 5 | reattach preserves position; concurrent open is refused; lingering state reported; non-guarantees documented and asserted |
| 7 | a file attached outside the repo reaches an accepting route, degrades with a pointer on one that cannot take it, and is never silently dropped; media contributes tokens; `ph attachments gc` collects only what no session references |
| 6 | 100 % coverage on `ph-core`; the docs test passes; `sandbox` refuses an absolute-path write; every seam has a page; Windows in the matrix |
